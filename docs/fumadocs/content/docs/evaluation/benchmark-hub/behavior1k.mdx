---
title: BEHAVIOR-1K
description: 1,000 everyday household activities in the OmniGibson simulator, imported into WorldFoundry via official result normalization.
---

## About

**Publisher:** Stanford University (Stanford Vision & Learning Lab)

BEHAVIOR-1K is Stanford's human-centered embodied-AI benchmark: 1,000 everyday activities (cleaning, cooking, tidying) selected from human time-use surveys, defined in the BDDL predicate language and simulated in OmniGibson with realistic physics, fluids, and object states. Success is measured by satisfying the activity's logical goal conditions, with subgoal satisfaction as a partial-credit signal.

WorldFoundry integrates BEHAVIOR-1K as an official-results runner: rollout exports produced with the upstream OmniGibson tooling are normalized into the unified scorecard.

Official references:

- Paper: [arxiv.org/abs/2403.09227](https://arxiv.org/abs/2403.09227)
- Official repository: [StanfordVL/OmniGibson](https://github.com/StanfordVL/OmniGibson)
- License: `upstream_terms`

## Evaluation protocol

WorldFoundry integrates this benchmark through the unified result-import path: `worldfoundry-eval zoo benchmark-run --mode official-validation` normalizes a caller-provided official result export (JSON/CSV rollout summaries) into the WorldFoundry scorecard without executing the simulator. Simulator execution, policy loading, and rollout collection remain explicit upstream steps performed with the official tooling.

Primary metric: `success_rate`.

| Metric | Meaning | Direction |
| --- | --- | --- |
| `success_rate` | Primary episode success rate over evaluated rollouts. | Higher is better |
| `task_success` | Per-task success aggregate. | Higher is better |
| `episode_success` | Per-episode success indicator aggregate. | Higher is better |
| `subgoal_success` | BDDL subgoal satisfaction rate (partial credit). | Higher is better |

## Data and artifact layout

In-tree paths:

```text
worldfoundry/data/benchmarks/tasks/external/behavior1k.yaml
```

```text
<official_results.json>                 # rollout summary export from the official tooling
${WORLDFOUNDRY_BEHAVIOR1K_RESULTS_PATH}   # default import path
```
Simulator runtime, benchmark assets, and policy checkpoints follow the upstream [StanfordVL/OmniGibson](https://github.com/StanfordVL/OmniGibson) documentation.

## Run the benchmark

Catalog-recorded validation command:

```bash
worldfoundry-eval zoo benchmark-run \
  --benchmark-id behavior1k \
  --mode official-validation \
  --official-results-path <official_results.json> \
  --output-dir <out> \
  --json
```

## Checkpoints and environment variables

- `WORLDFOUNDRY_BEHAVIOR1K_ROOT`: Optional override pointing at a local benchmark runtime/repository root.
- `WORLDFOUNDRY_BEHAVIOR1K_RESULTS_PATH`: Default path the importer reads official result exports from when `--official-results-path` is not passed.

## Outputs

The run writes into `--output-dir`:

- `scorecard.json`: WorldFoundry scorecard with the metric values recorded above.
- `raw_results.jsonl`: Normalized rows parsed from the official result export.
- `evaluation/summary.json`: Aggregated evaluation summary.

Scorecard primary key: `success_rate` (leaderboard key `success_rate`), stored in `scorecard.json`.

## Limitations

- WorldFoundry normalizes supplied rollout results; it does not run the official simulator or policies in tree.
- Full leaderboard parity is not claimed until official rollouts are executed with staged simulator assets and policy checkpoints, then imported.
- Imported scorecards are integration evidence, not leaderboard submissions.

[← Benchmark Hub](/docs/evaluation/benchmark-hub)
