---
title: CALVIN
description: Long-horizon language-conditioned manipulation benchmark (RA-L 2022); WorldFoundry normalizes official sequence-evaluation exports.
---

## About

**Publisher:** University of Freiburg

CALVIN (Composing Actions from Language and Vision) from the University of Freiburg evaluates long-horizon, language-conditioned manipulation: a policy in a PyBullet tabletop environment must chain up to five language instructions in sequence, across four environment configurations (A/B/C/D) that test zero-shot transfer. The signature protocol reports success at each chain length and the average number of completed instructions.

WorldFoundry integrates CALVIN as an official-results runner: exports from the official long-horizon sequence evaluator are normalized into the unified scorecard.

Official references:

- Paper: [ieeexplore.ieee.org/document/9788026](https://ieeexplore.ieee.org/document/9788026)
- Project page: [calvin.cs.uni-freiburg.de](http://calvin.cs.uni-freiburg.de/)
- Official repository: [mees/calvin](https://github.com/mees/calvin)
- License: `mit`

## Evaluation protocol

WorldFoundry integrates this benchmark through the unified result-import path: `worldfoundry-eval zoo benchmark-run --mode official-validation` normalizes a caller-provided official result export (JSON/CSV rollout summaries) into the WorldFoundry scorecard without executing the simulator. Simulator execution, policy loading, and rollout collection remain explicit upstream steps performed with the official tooling.

Primary metric: `success_rate`.

| Metric | Meaning | Direction |
| --- | --- | --- |
| `success_rate` | Primary episode success rate over evaluated rollouts. | Higher is better |
| `sequence_success` | Success over five-instruction chains (long-horizon protocol). | Higher is better |
| `task_success` | Per-task success aggregate. | Higher is better |
| `completion` | Average number of completed instructions per sequence. | Higher is better |

## Data and artifact layout

In-tree paths:

```text
worldfoundry/data/benchmarks/tasks/external/calvin.yaml
```

```text
<official_results.json>                 # rollout summary export from the official tooling
${WORLDFOUNDRY_CALVIN_RESULTS_PATH}   # default import path
```
The upstream checkout is mirrored at `cache/vla_va_wam/repos/mees--calvin`; dataset roots and policy checkpoints follow the official CALVIN documentation.

## Run the benchmark

Catalog-recorded validation command:

```bash
worldfoundry-eval zoo benchmark-run \
  --benchmark-id calvin \
  --mode official-validation \
  --official-results-path <official_results.json> \
  --output-dir <out> \
  --json
```

## Checkpoints and environment variables

- `WORLDFOUNDRY_CALVIN_RESULTS_PATH`: Default path the importer reads official result exports from when `--official-results-path` is not passed.

## Outputs

The run writes into `--output-dir`:

- `scorecard.json`: WorldFoundry scorecard with the metric values recorded above.
- `raw_results.jsonl`: Normalized rows parsed from the official result export.

Scorecard primary key: `success_rate` (leaderboard key `success_rate`), stored in `scorecard.json`.

## Limitations

- WorldFoundry normalizes supplied rollout results; it does not run the official simulator or policies in tree.
- Full leaderboard parity is not claimed until official rollouts are executed with staged simulator assets and policy checkpoints, then imported.
- Imported scorecards are integration evidence, not leaderboard submissions.

[← Benchmark Hub](/docs/evaluation/benchmark-hub)
