---
title: Benchmark runtime environments
description: Environment, service, checkpoint, and verification boundaries for every Video Generation and World Model benchmark.
---

## The artifact boundary

The generator and the evaluator are separate runtimes. A model may generate videos in its own conda environment, exit, and hand ordinary files to a benchmark running in another environment. A benchmark must not import the candidate model or force evaluator packages into the model environment.

For the 42 Video Generation and World Model entries below, `needs_new_env: false` means the WorldFoundry wrapper or result importer is expected to start from the unified environment. It does **not** mean that every official metric checkpoint, API credential, native extension, or complete dataset is already installed. `environment_id` names the executable base environment; runner roles belong in status/capability metadata. A separate conda environment is required only when `needs_new_env: true` and a setup command are both documented.

Environment classes used below:

- **U** — `worldfoundry-unified-<cuda-tier>`.
- **U + service** — unified client plus a user-provided API or model server. The server may use another machine or environment.
- **U + metric stack** — unified baseline plus dimension-specific checkpoints, packages, or CUDA extensions. Only a dimension that passes its own preflight is usable.
- **Dedicated** — a repository-managed benchmark conda profile with a public install entrypoint.

## Baseline setup

Create the unified environment once:

```bash
bash scripts/setup/bootstrap_worldfoundry.sh
source tmp/worldfoundry_unified_env.sh
```

Print the data, checkpoint, environment-variable, and command plan for one benchmark:

```bash
python scripts/setup/prepare_benchmark_assets.py \
  --benchmark-id <benchmark-id> \
  --json
```

This command is an **asset plan**, not runtime evidence. A benchmark is verified only at the scope recorded in its catalog and scorecard after a real evaluator path runs. An import-only or fixture-only pass does not prove raw-video recomputation.

Run the fail-closed dependency check declared by a runtime profile before real scoring:

```bash
PYTHONPATH=. "${WORLDFOUNDRY_UNIFIED_PYTHON:-python}" -m \
  worldfoundry.evaluation.tasks.execution.orchestration.runtime_preflight \
  --profile <benchmark-id> \
  --manifest worldfoundry/data/benchmarks/runtime_profiles/official/<benchmark-id>.yaml \
  --output-dir tmp/worldfoundry_preflight/<benchmark-id> \
  --json
```

`ready: true` means only that the profile's declared Python imports, paths, environment variables, and device requirements are ready. It does **not** mean the official benchmark or full metric suite has been executed or verified.

EvalCrafter is the only currently managed benchmark-specific conda exception. Keep the environment root explicit so the install, preflight, and later commands resolve the same prefix:

```bash
export WORLDFOUNDRY_HOME="${WORLDFOUNDRY_HOME:-${XDG_CACHE_HOME:-$HOME/.cache}/worldfoundry}"
export WORLDFOUNDRY_CONDA_ENVS_ROOT="${WORLDFOUNDRY_CONDA_ENVS_ROOT:-$WORLDFOUNDRY_HOME/conda_envs}"

bash scripts/setup/model_env_install.sh \
  --model evalcrafter \
  --env-root "$WORLDFOUNDRY_CONDA_ENVS_ROOT"
bash scripts/setup/model_env_install.sh \
  --model evalcrafter \
  --env-root "$WORLDFOUNDRY_CONDA_ENVS_ROOT" \
  --verify-only

EVALCRAFTER_PYTHON="$WORLDFOUNDRY_CONDA_ENVS_ROOT/worldfoundry-evalcrafter-metrics/bin/python"
test -x "$EVALCRAFTER_PYTHON"
```

Calling `"$EVALCRAFTER_PYTHON"` directly is the environment switch; shell activation is not required. This profile now mirrors the official pinned revision: Python 3.9.17, pip 23.2.1, Torch 2.0.1, torchvision 0.15.2, TensorFlow 2.12.0, PaddlePaddle-GPU 2.5.1, MMCV 2.0.1, and MMAction2 1.1.0. The official RAFT native extension and checkpoint tree are separate preparation steps. Environment installation or import verification is not evidence that the full 700-video suite passed. See the [EvalCrafter page](/docs/evaluation/benchmark-hub/evalcrafter) for the pinned upstream checkout, CUDA 11.7 note, checkpoint layout, bounded raw-video command, and official-result import.

## Video generation benchmarks — 30

<KvCatalog heading="Benchmark" vars rows={[
    { name: <><a href="/docs/evaluation/benchmark-hub/vbench">VBench</a></>, description: <><strong>U + metric stack</strong>{"; stage checkpoints for the selected dimension. Detectron2-backed dimensions may need a CUDA/Torch-compatible build."}</>, reads: [{ label: "Verified scope", value: <>{"One real-video "}<code>aesthetic_quality</code>{" component is verified. Full 16-dimension and leaderboard coverage are not."}</> }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/vbench-plus-plus">VBench++</a></>, description: <><strong>U + metric stack</strong>{"; I2V, long-video, and trustworthiness tracks have different assets."}</>, reads: [{ label: "Verified scope", value: "A bounded long-video component is verified; the complete suite is not." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/vbench-2.0">VBench-2.0</a></>, description: <><strong>U + metric stack</strong>{"; requires VBench2 checkpoints/data, with optional RetinaFace for relevant dimensions."}</>, reads: [{ label: "Verified scope", value: "A bounded diversity component is verified; the full suite is not." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/video-bench">Video-Bench</a></>, description: <><strong>U + service</strong>{"; provide generated videos, annotations, and "}<code>OPENAI_API_KEY</code>{" for the official GPT judge."}</>, reads: [{ label: "Verified scope", value: "Credential-gated runner path is present; no credentialed release run is claimed." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/vmbench">VMBench</a></>, description: <><strong>U + metric stack</strong>{"; full recomputation needs metric checkpoints and packages such as MMDetection/MMPose/DeepSpeed for their respective metrics."}</>, reads: [{ label: "Verified scope", value: "Result/runtime wiring is present; full CUDA metric recomputation is unverified." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/t2v-compbench">T2V-CompBench</a></>, description: <><strong>U + metric stack</strong>{"; the verified numeracy path needs local GroundingDINO/BERT assets, while the complete suite also needs LLaVA, depth/SAM, and DOT assets."}</>, reads: [{ label: "Verified scope", value: <>{"The strict artifact bridge plus a real one-sample official "}<code>generative_numeracy</code>{" run are verified. The 1,400-video suite and leaderboard parity are not."}</> }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/videoscore">VideoScore</a></>, description: <><strong>U + metric stack</strong>{"; stage VideoScore/Mantis weights and the expected sampled-frame tree."}</>, reads: [{ label: "Verified scope", value: "A bounded official scorer GPU run is verified; SPCC/pairwise full-suite parity is not." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/chronomagic-bench">ChronoMagic-Bench</a></>, description: <><strong>U + metric stack</strong>{"; CHScore needs the CoTracker/checkpoint stack. MTScore additionally needs its judge/API assets."}</>, reads: [{ label: "Verified scope", value: "A bounded CHScore component is verified; the complete benchmark is not." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/evalcrafter">EvalCrafter</a></>, description: <><strong>U</strong>{" runs the three official CLIP-derived metrics and imports "}<code>final_result.txt</code>{". The dedicated "}<code>worldfoundry-evalcrafter-metrics</code>{" profile mirrors the official legacy environment for preparing the external full scorer."}</>, reads: [{ label: "Verified scope", value: "Three bounded raw-video metrics and result import are available. Complete 17-metric raw-video production remains unintegrated and unverified." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/fetv">FETV</a></>, description: <><strong>U + metric stack</strong>{"; full FID/FVD/CLIP/BLIP evaluation needs reference frames and the official model/package set."}</>, reads: [{ label: "Verified scope", value: "Frame preparation and result normalization are available; full metric execution remains unverified." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/aigcbench">AIGCBench</a></>, description: <><strong>U</strong>{" for importing already-produced metric artifacts; full scoring additionally needs official prompts, references, and metric outputs."}</>, reads: [{ label: "Verified scope", value: "Importer only; no in-tree raw-video official recomputation claim." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/mirabench">MiraBench</a></>, description: <><strong>U</strong>{" for result normalization; a full run needs AMT, ViCLIP, MUSIQ, FVD/FID/KID, and 3D-consistency assets."}</>, reads: [{ label: "Verified scope", value: "Normalizer only; complete upstream scorer execution is pending." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/devil-dynamics">DEVIL Dynamics</a></>, description: <><strong>U</strong>{" plus generated artifacts and any official metric result files/checkpoints selected by the protocol."}</>, reads: [{ label: "Verified scope", value: "Official-result path is integrated; full-suite/leaderboard evidence is pending." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/genai-bench">GenAI-Bench</a></>, description: <><strong>U</strong>{" for result normalization; raw scoring needs official human labels and GPU VQAScore checkpoints."}</>, reads: [{ label: "Verified scope", value: "Normalizer only; raw-video scorer execution is pending." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/t2v-safety-bench">T2VSafetyBench</a></>, description: <><strong>U + service</strong>{"; install the declared OpenAI client packages and provide an OpenAI-compatible judge endpoint/key."}</>, reads: [{ label: "Verified scope", value: "API failures are fail-closed. No real judge result is claimed from local fixture tests." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/camerabench">CameraBench</a></>, description: <><strong>U</strong>{" for score normalization; full camera evaluation needs benchmark data and camera-geometry/VLM score outputs."}</>, reads: [{ label: "Verified scope", value: "Normalizer/component import is available; raw-video evaluator parity is pending." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/videoverse">VideoVerse</a></>, description: <><strong>U + service</strong>{"; use Gemini credentials or an explicitly configured local VLM, plus generated videos and prompt manifests."}</>, reads: [{ label: "Verified scope", value: "Official path is integrated, but a full credentialed suite is not release-verified." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/physvidbench">PhysVidBench</a></>, description: <><strong>U + service</strong>{"; prepare eight AuroraCap caption tracks and a Gemini/Google GenAI client for real QA judging."}</>, reads: [{ label: "Verified scope", value: "Result import is available; mock runs are not evidence and raw caption production is outside the runner." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/phygenbench">PhyGenBench</a></>, description: <><strong>U</strong>{" for result normalization; full evaluation needs all stage-specific judges, checkpoints/API access, and generated artifacts."}</>, reads: [{ label: "Verified scope", value: "Normalizer only; full official stage execution is pending." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/videophy">VideoPhy</a></>, description: <><strong>U + metric stack</strong>{"; stage the VideoCon-Physics checkpoint and generated videos."}</>, reads: [{ label: "Verified scope", value: "Official scoring path is present; full 688-prompt reproduction is pending." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/videophy2">VideoPhy2</a></>, description: <><strong>U + metric stack</strong>{"; stage the VideoPhy-2-AutoEval checkpoint."}</>, reads: [{ label: "Verified scope", value: "Official scoring path is present; full 200-prompt reproduction is pending." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/physics-iq">Physics-IQ</a></>, description: <><strong>U</strong>{"; provide the original benchmark root and generated videos with official view naming."}</>, reads: [{ label: "Verified scope", value: "A real bounded three-view official score is verified; it is not a full benchmark reproduction." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/physics-iq-verified">Physics-IQ Verified</a></>, description: <><strong>U</strong>{"; provide the verified dataset root and generated videos."}</>, reads: [{ label: "Verified scope", value: "Runtime is integrated, but the currently prepared local dataset lacks the complete benchmark media." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/ipv-bench">IPV-Bench</a></>, description: <><strong>U</strong>{" for result normalization; full scoring needs official labels, judge outputs, and generated artifacts."}</>, reads: [{ label: "Verified scope", value: "Normalizer only; official judge runtime is pending." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/videoscience-bench">VideoScience-Bench</a></>, description: <><strong>U + service</strong>{" for real VLM judging; configure the selected provider/model and its credential."}</>, reads: [{ label: "Verified scope", value: "Result normalization and judge wiring exist; no complete credentialed upstream run is claimed." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/phyeduvideo">PhyEduVideo</a></>, description: <><strong>U</strong>{" for result import; full scoring needs the upstream judge/checkpoint runtime and complete prompt artifacts."}</>, reads: [{ label: "Verified scope", value: "Importer only; raw-video judging is pending." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/phyground">PhyGround</a></>, description: <><strong>U + service</strong>{" to consume PhyJudge/vLLM or API judge scores, plus the official data root."}</>, reads: [{ label: "Verified scope", value: <><code>scores.json</code>{" import is available; the full judge run is not verified in-tree."}</> }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/phyfps-bench-gen">PhyFPS-Bench-Gen</a></>, description: <><strong>U + metric stack</strong>{"; stage the Visual Chronometer FPS predictor checkpoint."}</>, reads: [{ label: "Verified scope", value: "Official runtime path is integrated; full benchmark evidence is pending." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/visual-chronometer">Visual Chronometer</a></>, description: <><strong>U + metric stack</strong>{"; stage "}<code>vc_common_10_60fps.ckpt</code>{" or import a compatible "}<code>results.csv</code>{"."}</>, reads: [{ label: "Verified scope", value: "Runtime/normalizer surfaces exist; no release-verified full official run is claimed." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/memobench">MemoBench</a></>, description: <><strong>U + service/metric stack</strong>{"; Step 1 uses local metrics, while full ORS/VQA needs SAM-3 and Gemini-compatible judge access."}</>, reads: [{ label: "Verified scope", value: "Step 1 only is runnable; this must not be reported as the full MemoBench suite." }] }
  ]} />

## World model benchmarks — 12

<KvCatalog heading="Benchmark" vars rows={[
    { name: <><a href="/docs/evaluation/benchmark-hub/worldmodelbench">WorldModelBench</a></>, description: <><strong>U + metric stack</strong>{"; provide the 350-instance data root and a VILA-compatible judge checkpoint/model ID."}</>, reads: [{ label: "Verified scope", value: "Result import and manifest coverage checks are verified; full judge checkpoint execution is not." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/worldscore">WorldScore</a></>, description: <><strong>U + metric stack</strong>{"; full metrics need DROID/"}<code>lietorch</code>{", GroundingDINO, SAM, optical-flow/interpolation, Mamba, and matching checkpoints/extensions."}</>, reads: [{ label: "Verified scope", value: "A bounded dynamic component is verified; full metric-tree execution is not." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/worldbench">WorldBench</a></>, description: <><strong>U</strong>{" for official-result normalization only."}</>, reads: [{ label: "Verified scope", value: <>{"No confirmed redistributable official raw-video evaluator is in tree; "}<code>official-run</code>{" from videos is unavailable."}</> }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/wbench">WBench</a></>, description: <><strong>U + metric stack</strong>{"; full multi-turn scoring needs the WBench metric assets and official case layout."}</>, reads: [{ label: "Verified scope", value: "A real single-video temporal-flicker component is verified; the 289-case multi-turn protocol is not." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/iworld-bench">iWorld-Bench</a></>, description: <><strong>U + in-tree ViPE CUDA build</strong>{" for pixel and trajectory components. "}<code>WORLDFOUNDRY_IWORLD_BENCH_PYTHON</code>{" is an optional compatible-Python override, not a separate-environment requirement."}</>, reads: [{ label: "Verified scope", value: <>{"One real 16-frame video passed official "}<code>memory_ability</code>{". Bundled three-prompt coverage and all nine component metrics remain incomplete; full-suite and leaderboard validity are false."}</> }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/t2vworldbench">T2VWorldBench</a></>, description: <><strong>U</strong>{" for result normalization; full execution needs staged Drive data and the upstream VLM runtime."}</>, reads: [{ label: "Verified scope", value: "Official result fields are normalized; full raw-video/VLM evaluation is pending." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/worldarena">WorldArena</a></>, description: <><strong>U</strong>{" for the prepared Track 1 hierarchy; the full benchmark also needs metric checkpoints and embodied runtime assets."}</>, reads: [{ label: "Verified scope", value: "One real prepared-data PSNR component is verified. Full Track 1 and the embodied tracks are not." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/world-in-world">World-in-World</a></>, description: <><strong>U</strong>{" for result normalization; full evaluation needs closed-loop rollout assets and the upstream evaluator stack."}</>, reads: [{ label: "Verified scope", value: "Normalizer only; closed-loop runtime parity is pending." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/ewmbench">EWMBench</a></>, description: <><strong>U</strong>{" for result normalization; full evaluation needs official judge assets, result files, and complete generated outputs."}</>, reads: [{ label: "Verified scope", value: "Normalizer only; upstream judge execution is pending." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/4dworldbench">4DWorldBench</a></>, description: <><strong>U + metric stack/service</strong>{"; selected dimensions may need Keye-VL, DROID/"}<code>lietorch</code>{", FAST-VQA, Qwen utilities, or an OpenAI-compatible judge."}</>, reads: [{ label: "Verified scope", value: "Official runtime path exists, but real-data full-suite validation is pending." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/worldreasonbench">WorldReasonBench</a></>, description: <><strong>U + service</strong>{"; real QA/reward evaluation needs restricted official assets and an OpenAI-compatible multimodal endpoint. The server has its own environment."}</>, reads: [{ label: "Verified scope", value: "Result normalization/QA component code exists; no complete official judge run is claimed." }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/worldolympiad">WorldOlympiad</a></>, description: <><strong>U + metric stack/services</strong>{"; the official evaluator is checked in, so no external checkout is needed. Scored runs additionally need the DA3 source tree ("}<code>--da3-src</code>{"), DA3/SAM3/QwenVL/CLIP weights ("}<code>--weights-dir</code>{"), and running QwenVL, SAM3, and DA3 reward services."}</>, reads: [{ label: "Verified scope", value: <>{"The in-tree batch plan ("}<code>--run-official --dry-run</code>{") and judge-output normalization are verified; no scored triathlon run is claimed."}</> }] },
    { name: <><a href="/docs/evaluation/benchmark-hub/wrbench">WRBench</a></>, description: <><strong>U + metric stack</strong>{"; full D1–D6 scoring needs a runtime config pointing to VGGT-Omega, DINOv2, Qwen3.5, and Qwen3-VL checkpoints."}</>, reads: [{ label: "Verified scope", value: "Natural-25 request materialization and result normalization are available; full D1–D6 execution is unverified." }] }
  ]} />

## Rules for additional environments

Do not create a benchmark environment merely because one import is missing. Add or use a separate environment only when the runtime profile records a real ABI/framework conflict and provides all of the following:

1. A reproducible public setup command and Python executable.
2. The CUDA/Torch/Python compatibility boundary.
3. Required data, checkpoints, native builds, and service variables.
4. A real verified scope and an explicit remaining blocker.

If those are absent, the affected metric remains blocked. It must not silently fall back to a fixture, mock judge, zero-match score, or imported aggregate while being reported as an official run.

[← Benchmark Hub](/docs/evaluation/benchmark-hub)
