---
title: "A1"
description: "A1: vla and vla policy rollout; Runner parity pending; dedicated `worldfoundry-vla-a1`."
modelId: "a1"
pageSource: authored
---

A1 (AffordVLA) is an affordance-guided vision-language-action policy: it grounds manipulation in visual affordances before predicting robot actions. WorldFoundry runs AffordVLA fully in-tree — Molmo and CLIP ViT encoders, L1/DiT/flow action heads, and preprocessing — with two confirmed checkpoints: spatialtemporal-ai/a1-libero for tasks and a1-pretrain as the pretrained policy.

<ModelPaperFigures modelId="a1" locale="en" />

The recorded paper is [arXiv 2604.05672](https://arxiv.org/abs/2604.05672).

Code: [ATeam-Research/A1](https://github.com/ATeam-Research/A1).

The official paper is on [arXiv 2604.05672](https://arxiv.org/abs/2604.05672). Code: [ATeam-Research/A1](https://github.com/ATeam-Research/A1) (recorded revision `2fcb07da`). Two Hugging Face lines are confirmed: task `spatialtemporal-ai/a1-libero` and pretrained `spatialtemporal-ai/a1-pretrain`. The catalog does not record a license or a publishing institution.

The released **LIBERO-Spatial** checkpoint completed real Workspace inference on an **A100** and returned a finite **10×7** action chunk. Catalog runner status is still unset — treat that as numerical Workspace evidence, not a closed-loop LIBERO scorecard.

<ModelCommandBuilder modelId="a1" locale="en" />

## What it is for

Use `a1` for vision-language-action inference on this card. The recipe exists so you can launch the recorded policy profile.

The stack couples Molmo (VLM) and CLIP ViT visual encoding with selectable action heads (L1 regression, DiT, or flow). The runtime derives action dimensions from checkpoint tensors and validates every action-token boundary, so mismatched checkpoints fail fast.

Catalog tasks include vla, vla policy rollout, and robot policy. Catalog aliases include `affordvla-a1`, `spatialtemporal-ai/a1-libero`, and `spatialtemporal-ai/a1-pretrain`.

Recorded evaluation entry points include [LIBERO](/docs/evaluation/benchmark-hub/libero) (The a1-libero checkpoint targets LIBERO manipulation tasks).

## Runtime contract

Required `image`, `prompt`, and `state`; optional `model_id` (a1), `camera_views`, `policy_family`, and `action_representation`; artifacts `action_trace` and `a1_action_trace.json`.

Recorded task profiles: `vla`, `vla.policy_rollout`, and `robot_policy`. WorldFoundry binds `A1Pipeline`.

Family status: **Runner parity pending**.

Stage tokenizer and checkpoint assets locally before running. Assets are local-only and checkpoint-provided Python is never executed; runtime loading uses strict key matching.

AffordVLA, Molmo, CLIP ViT, L1/DiT/flow action heads, preprocessing, and safe local checkpoint loading run in tree.

## Running in WorldFoundry

Launch `worldfoundry-eval run a1 --pipeline.task-profile vla` to execute the policy. The command builder above switches `--pipeline.task-profile` among recorded tasks and still emits `worldfoundry-eval run`.

It runs in the dedicated environment `worldfoundry-vla-a1` (Python 3.11, CUDA 12.8, torch>=2.8).

Recorded weight repositories include `spatialtemporal-ai/a1-libero` and `Qwen/Qwen2-7B`. Stage assets with `bash scripts/inference/prepare_model_infer.sh a1`.

Status: **Runner parity pending**.

<ModelRelatedRecipes modelId="a1" locale="en" />
