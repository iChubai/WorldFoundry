---
title: "Astronex-World"
description: "Astronex-World exposes causal and bidirectional camera/action-conditioned video inference."
pageSource: generated
---

**发表机构:** Astronex-Robotics · 研究机构
text · image → video

Astronex-World 支持相机和动作条件下的因果及双向视频推理。

<ModelCommandBuilder modelId="astronex-world" locale="zh" />

## 架构

基于 Wan2.2 TI2V-5B，加入 PRoPE 相机条件、64 维动作和 embodiment 条件。调用外部官方采样器，本次集成不复制 Wan 骨干。

## 运行时契约

选择 mode=causal 或 bidirectional，并提供 source_root 和 checkpoint_dir。execute=True 执行推理。latent_frames 为生成潜帧数；因果模式要求 (latent_frames + reference_count) 能被 8 整除。

<ModelRelatedRecipes modelId="astronex-world" locale="zh" />
