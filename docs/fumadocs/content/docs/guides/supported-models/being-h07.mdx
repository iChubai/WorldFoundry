---
title: "Being-H0.7"
description: "Being-H0.7 is the successor line in BeingBeyond's Being-H family, recorded here as a latent world-action model (WAM) plus robot policy. WorldFoundry routes it through the in-tree official-policy action runtime from the Being-H07 subdirecto…"
pageSource: generated
---

**Publisher:** BeingBeyond · Company

Being-H0.7 is the successor line in BeingBeyond's Being-H family, recorded here as a latent world-action model (WAM) plus robot policy. WorldFoundry routes it through the in-tree official-policy action runtime from the Being-H07 subdirectory of the official repository; checkpoints are not yet recorded.

<ModelCommandBuilder modelId="being-h07" locale="en" />

## Architecture

Task tags record a latent world-action-model design — the policy predicts in a learned latent world space rather than raw pixels. Architecture internals beyond the official repository subdirectory pointer are not recorded in this repository.

## Usage notes

Checkpoint absence is reported by the runtime missing_assets plan and is not a catalog blocker; stage weights once BeingBeyond releases them, or use the GPU-validated being-h05 entry meanwhile.

<ModelRelatedRecipes modelId="being-h07" locale="en" />
