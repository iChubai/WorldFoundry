---
title: "Depth Anything 3 Metric Prior"
description: "Depth Anything 3 Metric Prior（ByteDance (Seed)）：单目深度估计、米制深度估计；已验证。"
modelId: "depth-anything-v3-prior"
pageSource: authored
---

Depth Anything 3 Metric Prior 是字节 Seed 团队 Depth Anything 3 的米制深度头，作为独立几何先验集成，runner 状态已验证。记录的 checkpoint 为 depth-anything/DA3METRIC-LARGE，本仓库中 Prior-Depth-Anything 细化器的验证正是以它的输出为条件。

<ModelPaperFigures modelId="depth-anything-v3-prior" locale="zh" />

记录论文为[arXiv 2511.10647](https://arxiv.org/abs/2511.10647)。

项目页：[depth-anything-3.github.io](https://depth-anything-3.github.io/)。代码：[ByteDance-Seed/Depth-Anything-3](https://github.com/ByteDance-Seed/Depth-Anything-3)。

与导出卡同一篇论文：[arXiv 2511.10647](https://arxiv.org/abs/2511.10647)。项目页：[depth-anything-3.github.io](https://depth-anything-3.github.io/)。Runner 经共享先验契约**已验证**。运行时配置在 `worldfoundry/data/models/runtime/configs/depth_anything_v3`，**与 ViPE 分开**。

这**不是** SOH 导出的 [depth-anything-v3](/zh/docs/guides/supported-models/depth-anything-v3)。

<ModelCommandBuilder modelId="depth-anything-v3-prior" locale="zh" />

## 用来做什么

做前馈几何时选 `depth-anything-v3-prior`：视图进、重建/轨迹/点云出。需要 catalog 的 3D 路由、而不是仅深度先验时用它。

DA3 扩展了 Depth Anything 的训练配方并增加米制与几何头。本先验条目接入 metric-large checkpoint；运行时配置位于 runtime/configs/depth_anything_v3，与 VIPE 配置分离。该模型由字节跳动（Seed 团队）发布。

catalog 记录的任务包括 单目深度估计、米制深度估计、几何先验。别名包括 `dav3-prior`、`da3metric-large-prior`。模态上，它接受图像，输出深度。

给其他 pipeline 用的度量深度先验。不是完整 SOH 导出卡。

## 运行时契约

必填 `image`；可选 `prompt`、`video`、`actions`；产物 `depth-anything-v3-prior.npz`。

记录的任务 profile：`monocular-depth-estimation`、`metric-depth-estimation`、`geometry-prior`。绑定的 pipeline 类是 `DepthAnythingV3PriorPipeline`。

系列状态：**已验证**。

它作为独立几何先验集成在 worldfoundry/base_models 下，通过共享的 geometry-prior pipeline/operator 契约对外暴露。 上层管线（如 ViPE）从这一规范位置导入它，而不是各自保留私有副本。 官方 SOH 导出路径请使用兄弟条目 depth-anything-v3。

仅先验用途：完整的 DA3 重建能力在 depth-anything-v3 条目上。

记录的 checkpoint：`depth-anything/DA3METRIC-LARGE`（`model.safetensors`）。运行时配置在 `worldfoundry/data/models/runtime/configs/depth_anything_v3`，**与 ViPE 分开**。Runner 在共享 prior 契约上 **已验证**。

## 在 WorldFoundry 中运行

catalog 启动命令是 `worldfoundry-eval run depth-anything-v3-prior --pipeline.task-profile monocular-depth-estimation`。上方命令构建器按已记录任务切换 `--pipeline.task-profile`，发出的仍是 `worldfoundry-eval run`。

运行环境是统一 profile `worldfoundry-unified-cu128`（Python 3.11，CUDA 12.8）。

记录的权重仓库包括 `depth-anything/DA3METRIC-LARGE`。就位权重用 `bash scripts/inference/prepare_model_infer.sh depth-anything-v3-prior`。

状态：**已验证**。

<ModelRelatedRecipes modelId="depth-anything-v3-prior" locale="zh" />
