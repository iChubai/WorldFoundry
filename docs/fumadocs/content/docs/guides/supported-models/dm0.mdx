---
title: "DM0"
description: "DM0 from Dexmal: vla and vla action prediction; Runner parity pending."
modelId: "dm0"
pageSource: authored
---

DM0 is Dexmal's Dexbotic-family flow-matching VLA: a Qwen3 flow action expert with merged prefix/suffix attention and triple-view preprocessing. Base and LIBERO checkpoints are recorded (Dexmal/DM0-base, Dexmal/DM0-libero, CC license) and the full inference loop — normalization plus Euler sampler — runs in process from local checkpoints.

<ModelPaperFigures modelId="dm0" locale="en" />

The recorded paper is [arXiv 2510.23511](https://arxiv.org/abs/2510.23511).

Code: [Dexmal/dexbotic](https://github.com/Dexmal/dexbotic).

Same Dexbotic paper lineage: [arXiv 2510.23511](https://arxiv.org/abs/2510.23511). Code: [Dexmal/dexbotic](https://github.com/Dexmal/dexbotic). Weights: `Dexmal/DM0-base`, `Dexmal/DM0-libero` (**CC** — review before commercial use). Code license recorded as **MIT**. Runtime: `checkpoint_ready`. **No GPU rollout validation** is recorded.

<ModelCommandBuilder modelId="dm0" locale="en" />

## What it is for

Use `dm0` for vision-language-action inference on this card. The recipe exists so you can launch the recorded policy profile.

A Qwen3-based flow action expert decodes actions with an Euler sampler; the Perception Encoder and multimodal base are reused from the compatible in-tree DB-CogACT implementation, keeping the Dexbotic family consistent.

Catalog tasks include vla, vla action prediction, and vla policy rollout. Catalog aliases include `dexbotic-dm0` and `dm0-base`.

Recorded evaluation entry points include [LIBERO](/docs/evaluation/benchmark-hub/libero) (Dedicated DM0-libero policy checkpoint).

## Runtime contract

Required `image`, `prompt`, and `state`; optional `model_id` (dm0), `max_image_slots` (3), and `action_representation`; artifacts `action_trace` and `dm0_action_trace.json`.

Recorded task profiles: `vla`, `vla.action_prediction`, and `vla.policy_rollout`. WorldFoundry binds `DM0Pipeline`.

Family status: **Runner parity pending**.

Use DM0-libero for LIBERO evaluation and DM0-base for initialization. Training, datasets, servers, benchmark launchers, and the optional Triton realtime backend are excluded.

The Qwen3 flow action expert, merged prefix/suffix attention, triple-view preprocessing, normalization, and Euler sampler run in process from local checkpoints.

## Running in WorldFoundry

The catalog launch is `worldfoundry-eval run dm0 --pipeline.task-profile vla`. The command builder above switches `--pipeline.task-profile` among recorded tasks and still emits `worldfoundry-eval run`.

It runs in the unified environment `worldfoundry-unified-cu128` (Python 3.10, CUDA 12.8, torch>=2.6.0).

Recorded weight repositories include `Dexmal/DM0-libero` and `Dexmal/DM0-base`. Stage assets with `bash scripts/inference/prepare_model_infer.sh dm0`.

Status: **Runner parity pending**.

<ModelRelatedRecipes modelId="dm0" locale="en" />
