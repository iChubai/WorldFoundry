---
title: "DreamX-World 5B"
description: "Amap causal AR world: resident KV/VAE caches, 4-step AR-Forcing. A100 validated one 1280×704 nine-frame block. Apache-2.0. Not a long-horizon scorecard."
modelId: "dreamx-world-5b"
pageSource: authored
---

DreamX-World 5B AR is Amap's **causal autoregressive** world: latent, attention, cross-attention, and VAE caches stay **resident** while video rolls forward in **three-latent** blocks under camera actions. Decoded RGB is **output-only** — never fed back as conditioning. Distilled **four-step AR-Forcing**. Pipeline: `DreamXWorld5BARPipeline`. Alias `dreamx-world-5b-ar` resolves here.

<ModelPaperFigures modelId="dreamx-world-5b" locale="en" />

Paper: [arXiv 2606.16993](https://arxiv.org/abs/2606.16993). Project: [amap-ml.github.io/DreamX_World](https://amap-ml.github.io/DreamX_World). Code: [AMAP-ML/DreamX-World](https://github.com/AMAP-ML/DreamX-World). License: **apache-2.0**.

A **2026-07-28** single-A100 run produced a finite, decodable **nine-frame 1280×704** block from the official example image (Verified). That is a **two-control** resident-path check, not long-horizon quality parity.

<ModelCommandBuilder modelId="dreamx-world-5b" locale="en" />

## What it is for

Roll a resident causal AR world block, not a long-horizon scorecard.

Launch `worldfoundry-eval run dreamx-world-5b` with `--pipeline.task-profile world_model`.

Score on [WorldModelBench](/docs/evaluation/benchmark-hub/worldmodelbench) — general world-rollout quality.

## Runtime contract

Required: `image` and `prompt`; optional: `video`; `actions`: camera_navigation; artifacts: `generated_world` and `dreamx-world-5b.mp4`.

| Knob | Recorded |
| --- | --- |
| Validated block | **1280×704**, **9** frames, single A100 on **2026-07-28** |
| Schedule | distilled **4-step** AR-Forcing |
| Block | **three** latents; KV / cross-attn / VAE caches stay resident |
| RGB | output-only — never fed back |
| Causal ckpt | `GD-ML/DreamX-World-5B` (`config.json`, `model.safetensors`) |
| Support | `Wan-AI/Wan2.2-TI2V-5B` (VAE, T5, tokenizer) |

## Run in WorldFoundry

Status: **Verified**. Unified environment `worldfoundry-unified-cu128` (Python 3.11, CUDA 12.8). Stage `GD-ML/DreamX-World-5B` and `Wan-AI/Wan2.2-TI2V-5B` locally — missing files fail at load, nothing is downloaded. Run `worldfoundry-eval run dreamx-world-5b` with `--pipeline.task-profile world_model`. Catalog VRAM is unset.

<ModelRelatedRecipes modelId="dreamx-world-5b" locale="en" />
