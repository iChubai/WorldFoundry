---
title: "Déjà View Looping Transformer"
description: "Déjà View Looping Transformer from NVIDIA: 3d-reconstruction and multi-view-reconstruction; Runner parity pending."
modelId: "dvlt"
pageSource: authored
---

Déjà View Looping Transformer (DVLT) is an NVIDIA multi-view reconstruction model covering 3D reconstruction, camera pose estimation, and depth from image collections, exporting GLB scenes. The WorldFoundry wrapper is lazy: it runs the external DVLT GLB export path only when CUDA, dependencies, source, inputs, and weights are all staged locally.

<ModelPaperFigures modelId="dvlt" locale="en" />

The recorded paper is [Déjà View: Looping Transformers for Multi-View 3D Reconstruction](https://arxiv.org/abs/2605.30215) (2026).

Project page: [research.nvidia.com/labs/dvl/projects/dvlt](https://research.nvidia.com/labs/dvl/projects/dvlt/). Code: [nv-tlabs/dvlt](https://github.com/nv-tlabs/dvlt).

Paper: [arXiv 2605.30215](https://arxiv.org/abs/2605.30215). Project: [research.nvidia.com/labs/dvl/projects/dvlt](https://research.nvidia.com/labs/dvl/projects/dvlt/). Code: [nv-tlabs/dvlt](https://github.com/nv-tlabs/dvlt). Checkpoint license: **NVIDIA non-commercial research/evaluation**. Runtime source is **externalized** from the public tree — you stage it. `DVLTSynthesis` fails fast on missing prerequisites. GPU parity is **pending**.

<ModelCommandBuilder modelId="dvlt" locale="en" />

## What it is for

`dvlt` is the recorded reconstruction line. Start here to emit geometry from the documented views.

The catalog records a looping-transformer design for multi-view geometry (paper arXiv 2605.30215); the runtime source is externalized from the public release tree, so architecture internals beyond the recorded tasks are not stored here.

Catalog tasks include 3d-reconstruction, multi-view-reconstruction, camera-pose-estimation, and depth-estimation. Catalog aliases include `deja-view`, `dejaview`, `deja-view-looping-transformer`, and `nvidia/dvlt`.

## Runtime contract

Required `image` and `video`; optional `actions` (view_sequence, reference_frame, reconstruction_options) and `prompt`; artifacts `dvlt_reconstruction.glb`.

Recorded task profiles: `3d-reconstruction`, `multi-view-reconstruction`, `camera-pose-estimation`, and `depth-estimation`. WorldFoundry binds `DVLTPipeline`.

Family status: **Runner parity pending**.

Runner registration is wired; GPU parity is pending checkpoint and environment validation. The nvidia/dvlt checkpoint carries the NVIDIA non-commercial research/evaluation license — check terms before use. DVLTSynthesis fails fast on missing prerequisites.

Runtime source is externalized from the public release tree; checkpoints remain external assets.

## Running in WorldFoundry

Launch `worldfoundry-eval run dvlt --pipeline.task-profile 3d-reconstruction` to write geometry. The command builder above switches `--pipeline.task-profile` among recorded tasks and still emits `worldfoundry-eval run`.

It runs in the unified environment `worldfoundry-unified-cu128` (Python 3.11, CUDA 12.8).

Recorded weight repositories include `nvidia/dvlt`. Stage assets with `bash scripts/inference/prepare_model_infer.sh dvlt`.

Recorded licenses include nvidia-noncommercial-research-evaluation; status: **Runner parity pending**.

<ModelRelatedRecipes modelId="dvlt" locale="en" />
