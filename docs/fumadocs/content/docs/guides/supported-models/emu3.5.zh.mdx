---
title: "Emu3.5"
description: "Emu3.5 is BAAI's native multimodal generation model: a single autoregressive model that reads and writes interleaved text and images. The catalog records it for multimodal generation, image generation, and VLA-style reasoning. The WorldFou…"
pageSource: generated
---

**发表机构:** 北京智源人工智能研究院（BAAI） · 研究机构

Emu3.5 是北京智源（BAAI）的原生多模态生成模型：单一自回归模型即可读写交错的文本与图像。 catalog 将其记录为多模态生成、图像生成与 VLA 式推理任务。WorldFoundry 路径已完备，缺失的 checkpoint/资产会在运行时报告；输出以 model_response 产物落盘，而非视频文件。

<ModelCommandBuilder modelId="emu3.5" locale="zh" />

## 架构

Emu 系列把图像与文本统一为一个 token 流做下一个 token 预测——按 BAAI 的公开发布，推理环节没有独立的扩散解码器。本仓库记录了 prompt+image 输入契约与 model_response 产物；3.5 版的参数规模与内部细节仓库未记录。

## 使用要点

Runner parity 为 pending——本工作区尚未做过 checkpoint 加载的产物一致性验证。先就位 BAAI/Emu3.5 权重；资产缺失时路径会给出明确报错。预期输出是文本/图像响应，而不是视频。

<ModelRelatedRecipes modelId="emu3.5" locale="zh" />
