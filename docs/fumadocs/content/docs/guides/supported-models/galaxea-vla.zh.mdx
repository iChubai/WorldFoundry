---
title: "Galaxea G0Plus"
description: "星海图 PaliGemma-3B flow VLA。静态运行时已验证。非商用 G0-Plus 社区许可。GPU 数值 rollout 待验证。"
modelId: "galaxea-vla"
pageSource: authored
---

Galaxea G0Plus 是星海图（Galaxea AI）的开放 VLA：RGB、语言和状态进，动作轨迹出。发布的 checkpoint 架在 **PaliGemma-3B** 上，配 **flow-matching** 动作采样器和 KV 缓存解码。WorldFoundry 把架构、KV cache、flow 采样器、严格 checkpoint 加载器、tokenizer 适配器、预处理和归一化都放在**树内**。恢复按官方参数名严格对齐。运行时只接受本地就位资产，从不执行 checkpoint 自带的 Python。

<ModelPaperFigures modelId="galaxea-vla" locale="zh" />

论文：[arXiv 2608.11739](https://arxiv.org/abs/2608.11739)。项目页：[opengalaxea.github.io/G05](https://opengalaxea.github.io/G05/)。代码：[OpenGalaxea/GalaxeaVLA](https://github.com/OpenGalaxea/GalaxeaVLA)。权重：`OpenGalaxea/G0-VLA` 加 `google/paligemma-3b-pt-224`。别名：`g0plus`、`open-galaxea/g0-vla`。许可：**`g0-plus-community-license-non-commercial`**。集成：仅静态加载。Runner：**仅静态加载**。GPU 数值 rollout 仍待验证。

发布的 G0Plus 基座面向**下游微调**，不声称是任务就绪策略。必须配对精确的 `dataset_stats.json`——归一化从不编造数值。

<ModelCommandBuilder modelId="galaxea-vla" locale="zh" />

## 用来做什么

Galaxea / G0-VLA 静态加载路径。GPU 数值 rollout 仍待验证。

启动 `worldfoundry-eval run galaxea-vla --pipeline.task-profile vla`。

## 运行时契约

必填 `image`、`prompt`、`state`；可选 `model_id`、`policy_family`；产物 `action_trace`、`galaxea_vla_action_trace.json`。

| 变体 | 相机 | 状态 / 动作 | 块 |
| --- | --- | --- | --- |
| `r1lite_mobile` | 3 | 21 / 26 | 32 |
| `r1lite_arm` | 3 | 14 / 14 | 32 |
| `libero` | 2 | 8 / 7 | 32 |

动作块是 **32** 步。就位的 transformer 在 `OpenGalaxea/G0-VLA` 的 `G0Plus_3B_base` 下。catalog 示例本体（这里没有 GPU 验证）：

## 在 WorldFoundry 中运行

状态：**仅静态加载**。独立环境 `worldfoundry-vla-galaxea`（Python 3.11, Cu124 Or Newer）。先把 `OpenGalaxea/G0-VLA`、`google/paligemma-3b-pt-224` 就位到本地，缺文件加载失败，运行时不下载。在该环境执行 `worldfoundry-eval run galaxea-vla --pipeline.task-profile vla`。catalog 未记录 VRAM。

<ModelRelatedRecipes modelId="galaxea-vla" locale="zh" />
