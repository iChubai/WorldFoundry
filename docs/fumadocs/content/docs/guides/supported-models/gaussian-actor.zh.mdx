---
title: "Gaussian Actor"
description: "Gaussian Actor is a reinforcement-learning actor policy recorded against the concrete LeRobot gaussian_actor implementation — no separate official repository was identified upstream. WorldFoundry routes it through an in-tree action runtime…"
pageSource: generated
---

**发表机构:** Hugging Face（LeRobot 参考实现） · 企业

Gaussian Actor 是强化学习 actor 策略，以 LeRobot 中具体的 gaussian_actor 实现为参考记录——上游未识别到独立官方仓库。WorldFoundry 通过树内动作运行时适配器路由；未记录 checkpoint，运行输出运行时计划。

<ModelCommandBuilder modelId="gaussian-actor" locale="zh" />

## 架构

面向连续控制的高斯策略头：actor 输出动作分布的均值与方差，遵循 LeRobot 参考实现。更多内部细节仓库未记录。

## 使用要点

checkpoint 缺失由运行时 missing_assets 报告，不视为 catalog 阻塞；就位训练好的 actor checkpoint 即可运行真实预测。

<ModelRelatedRecipes modelId="gaussian-actor" locale="zh" />
