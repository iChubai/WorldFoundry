---
title: "GigaWorld-Policy-0.5"
description: "GigaWorld-Policy-0.5 is GigaAI's world-action policy: an action-centered Mixture-of-Transformers released as a pretrained initialization (Apache-2.0), riding on Wan2.2-TI2V-5B components for VAE, text encoder, and tokenizer. WorldFoundry e…"
pageSource: generated
---

**发表机构:** 极佳科技（GigaAI） · 企业

GigaWorld-Policy-0.5 是极佳科技（GigaAI）的世界-动作策略：以动作为中心的 Mixture-of-Transformers，作为预训练初始化发布（Apache-2.0），VAE、文本编码器与 tokenizer 复用 Wan2.2-TI2V-5B 组件。WorldFoundry 在进程内执行精确的已发布 MoT 与官方仅动作扩散环路。

<ModelCommandBuilder modelId="giga-world-policy-0.5" locale="zh" />

## 架构

Wan2.2-TI2V-5B 基座上的动作中心 Mixture-of-Transformers：发布的分片 safetensors transformer 用于策略初始化，扩散只在动作流上进行。

## 使用要点

发布的 transformer 是预训练初始化：任务部署还需本地后训练统计（任务专用 norm_stats_delta.json），以及适用时的任务微调 transformer 目录。所有 Hub 资产需本地就位；运行时禁用网络访问。

<ModelRelatedRecipes modelId="giga-world-policy-0.5" locale="zh" />
