---
title: "Hailuo 2.3"
description: "Hailuo 2.3 from MiniMax: audio-video and image-to-video; Runner parity pending."
modelId: "hailuo-2p3"
pageSource: generated
---

Hailuo 2.3 is MiniMax's hosted video generation service, recorded in this catalog for image-to-video and joint audio-video generation. You hand it a start image (plus an optional prompt) and it returns a finished clip rendered on MiniMax's infrastructure — WorldFoundry never runs the model itself. The catalog also lists the aliases minimax-i2v and minimax_i2v for this entry, so older configs keep resolving. Use it when you want MiniMax's production quality inside WorldFoundry's Studio workflow without managing GPUs.

<ModelPaperFigures modelId="hailuo-2p3" locale="en" />

Project page: [minimax.io](https://www.minimax.io/).

<ModelCommandBuilder modelId="hailuo-2p3" locale="en" />

## What it is for

Pick `hailuo-2p3` when this card is a hosted API rather than a local checkpoint: call the recorded provider route instead of staging weights.

Catalog tasks include audio-video, image-to-video, and hosted-api. Catalog aliases include `minimax-i2v` and `minimax_i2v`. Documented modalities: image in, video out.

Recorded evaluation entry points include [VBench](/docs/evaluation/benchmark-hub/vbench) (Standard multi-dimension quality suite for comparing hosted text/image-to-video outputs), [Physics-IQ Original](/docs/evaluation/benchmark-hub/physics-iq) (Probes whether provider-side video models respect basic physical plausibility).

## Runtime contract

Inputs, knobs, and artifacts are not recorded on this card.

Recorded task profiles: `audio-video`, `image-to-video`, and `hosted-api`. WorldFoundry binds `Hailuo2p3Pipeline`.

Family status: **Runner parity pending**.

The catalog marks this entry as integrated: the hosted-API Studio pipeline is available, but you must supply your own provider API key and account. No weights are downloaded and no GPU environment is provisioned locally — generation happens on the provider side, so cost, quota, and content policy follow your provider account. Point the pipeline at your MiniMax platform key before launching jobs.

Hosted API Studio pipeline is available; users must provide their own provider API key.

## Running in WorldFoundry

Launch `worldfoundry-eval run hailuo-2p3 --pipeline.task-profile audio-video` to call the hosted route. The command builder above switches `--pipeline.task-profile` among recorded tasks and still emits `worldfoundry-eval run`.

The environment profile is not recorded.

Stage assets with `bash scripts/inference/prepare_model_infer.sh hailuo-2p3`.

Status: **Runner parity pending**.

<ModelRelatedRecipes modelId="hailuo-2p3" locale="en" />
