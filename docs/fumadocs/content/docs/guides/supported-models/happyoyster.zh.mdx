---
title: "HappyOyster"
description: "HappyOyster（HappyOyster (commercial product)）：世界模型、api-product；Runner 一致性待确认。"
modelId: "happyoyster"
pageSource: generated
---

HappyOyster 是商业世界模型 API 产品，本仓库以产品官网（happyoyster.cn）而非代码仓库记录。本 catalog 中仅注册了 API 路径元数据——WorldFoundry 出于生态完整性跟踪该产品，未注册可运行的 API 客户端或生成 pipeline。

<ModelPaperFigures modelId="happyoyster" locale="zh" />

项目页：[happyoyster.cn](https://www.happyoyster.cn)。

<ModelCommandBuilder modelId="happyoyster" locale="zh" />

## 用来做什么

需要动作条件世界步进时选 `happyoyster`：记录动作与帧进、滚动生成出。用来做交互世界推理，不是把 prompt 倒进文生视频。

这是托管的商业产品：厂商未公开其架构，本仓库亦未记录。仅记录 world-model/API 产品的任务标签。

catalog 记录的任务包括 世界模型、api-product、世界生成。

## 运行时契约

必填 `image`、`prompt`、`video`；可选 `actions`；产物 `generated_world`、`happyoyster_runtime_plan.json`。

记录的任务 profile：`world-model`、`api-product`、`world-generation`。绑定的 pipeline 类是 `HappyOysterPipeline`。

系列状态：**Runner 一致性待确认**。

API 凭据与端点受运行时限制。 真实使用需要厂商账号；当前条目仅输出运行时计划。

WorldFoundry tracks HappyOyster 计划中; no runnable API client or generation pipeline is registered.

## 在 WorldFoundry 中运行

启动 `worldfoundry-eval run happyoyster --pipeline.task-profile world-model` 推进世界。上方命令构建器按已记录任务切换 `--pipeline.task-profile`，发出的仍是 `worldfoundry-eval run`。

运行环境是统一 profile `worldfoundry-unified-cu128`（Python 3.11，CUDA 12.8）。

本卡片未记录公开 checkpoint id；按使用说明就位权重。就位权重用 `bash scripts/inference/prepare_model_infer.sh happyoyster`。

状态：**Runner 一致性待确认**。

<ModelRelatedRecipes modelId="happyoyster" locale="zh" />
