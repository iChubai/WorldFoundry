---
title: "Hunyuan Game Craft"
description: "Hunyuan Game Craft from Tencent (Hunyuan): interactive-world-model and image-to-video; Verified; dedicated `HYGameCraft`."
modelId: "hunyuan-game-craft"
pageSource: generated
---

Hunyuan GameCraft turns a single game scene image into interactive world video: send keyboard-style actions (forward/backward/left/right) and it generates high-dynamic game footage that follows them. In this catalog the runner is verified — the official village image-conditioned demo runs with 129 frames at 704x1216, 50 steps, CFG 2.0, over 8 torchrun ranks.

<ModelPaperFigures modelId="hunyuan-game-craft" locale="en" />

The recorded paper is [arXiv 2506.17201](https://arxiv.org/abs/2506.17201).

Project page: [hunyuan-gamecraft.github.io](https://hunyuan-gamecraft.github.io/). Code: [Tencent-Hunyuan/Hunyuan-GameCraft-1.0](https://github.com/Tencent-Hunyuan/Hunyuan-GameCraft-1.0).

<ModelCommandBuilder modelId="hunyuan-game-craft" locale="en" />

## What it is for

`hunyuan-game-craft` is the recorded camera-control route. Start here to follow a catalog camera path.

Per the upstream release, GameCraft builds on HunyuanVideo with unified continuous action-space injection and history-conditioned autoregressive extension. The infer-only runtime is retained in-tree and rejects external source roots; its Python 3.10 / torch 2.5.1 stack stays isolated in the documented HYGameCraft environment (license: tencent-hunyuan-community). It is published by Tencent (Hunyuan).

Catalog tasks include interactive-world-model, image-to-video, camera-control, and official-interactive-video. Catalog aliases include `hunyuan-gamecraft` and `gamecraft`.

Recorded evaluation entry points include [WorldModelBench](/docs/evaluation/benchmark-hub/worldmodelbench) (Action-following world-rollout quality).

## Runtime contract

Required `prompt` and `image`; optional `video` and `actions` (camera_path, navigation_action); artifacts `hunyuan-game-craft.mp4`.

Recorded task profiles: `interactive-world-model`, `image-to-video`, `camera-control`, and `official-interactive-video`. WorldFoundry binds `HunyuanGameCraftPipeline`.

Family status: **Verified**.

Recorded `official-interactive-video` defaults: frames=129, fps=24, height=704, width=1216, guidance_scale=2.0, seed=250160.

Official inference uses the HYGameCraft conda profile and torchrun dispatch — plan for a multi-GPU host.

## Running in WorldFoundry

Launch `worldfoundry-eval run hunyuan-game-craft --pipeline.task-profile official-interactive-video` to follow the camera path. The command builder above switches `--pipeline.task-profile` among recorded tasks and still emits `worldfoundry-eval run`.

It runs in the dedicated environment `HYGameCraft` (Python 3.10, CUDA 12.4, torch==2.5.1).

Recorded weight repositories include `tencent/Hunyuan-GameCraft-1.0`. Stage assets with `bash scripts/inference/prepare_model_infer.sh hunyuan-game-craft`.

Status: **Verified**.

<ModelRelatedRecipes modelId="hunyuan-game-craft" locale="en" />
