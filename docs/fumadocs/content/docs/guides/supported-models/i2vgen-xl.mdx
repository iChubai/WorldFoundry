---
title: "I2VGen-XL"
description: "I2VGen-XL from Alibaba (Tongyi Lab): image-to-video and text-to-video; Runner parity pending."
modelId: "i2vgen-xl"
pageSource: generated
---

I2VGen-XL is Alibaba ali-vilab's cascaded image-to-video model, one of the earlier high-resolution open I2V releases. It turns a still image (with optional text) into a short video through a two-stage generate-then-refine cascade. The WorldFoundry route is complete and reports missing checkpoint assets at runtime.

<ModelPaperFigures modelId="i2vgen-xl" locale="en" />

The recorded paper is [arXiv 2311.04145](https://arxiv.org/abs/2311.04145).

Project page: [i2vgen-xl.github.io](https://i2vgen-xl.github.io/). Code: [ali-vilab/i2vgen-xl](https://github.com/ali-vilab/i2vgen-xl).

<ModelCommandBuilder modelId="i2vgen-xl" locale="en" />

## What it is for

Use `i2vgen-xl` for the family's joint T2V/I2V inference path. The catalog records both task profiles on this page.

Per the upstream publication, I2VGen-XL uses a base stage for semantic coherence and a refinement stage for detail/resolution, both latent diffusion models. This repository records the prompt+image contract and environment/checkpoint assumptions in the runtime profile; run readiness is kept in release run manifests, not here. It is published by Alibaba (Tongyi Lab).

Catalog tasks include image-to-video and text-to-video. Documented modalities: image and text in, video out.

Recorded evaluation entry points include [AIGCBench](/docs/evaluation/benchmark-hub/aigcbench) (I2V-focused benchmark suite).

## Runtime contract

Required `prompt` and `image`; artifacts `i2vgen-xl.mp4`.

Recorded task profiles: `image-to-video` and `text-to-video`. WorldFoundry binds `I2VGenXLPipeline`.

Family status: **Runner parity pending**.

Integration is complete; no runner parity record exists yet in this manifest. Both the official GitHub and ali-vilab HF repos are confirmed reachable. Treat it as a legacy-quality baseline next to newer DiT-based I2V models in this catalog.

Official GitHub and ali-vilab HF repo were both reachable.

## Running in WorldFoundry

Launch `worldfoundry-eval run i2vgen-xl --pipeline.task-profile image-to-video` to sample the clip. The command builder above switches `--pipeline.task-profile` among recorded tasks and still emits `worldfoundry-eval run`.

It runs in the unified environment `worldfoundry-unified-cu128` (Python 3.11, CUDA 12.8).

Recorded weight repositories include `ali-vilab/i2vgen-xl`. Stage assets with `bash scripts/inference/prepare_model_infer.sh i2vgen-xl`.

Recorded licenses include mit; status: **Runner parity pending**.

<ModelRelatedRecipes modelId="i2vgen-xl" locale="en" />
