---
title: "IRASim"
description: "IRASim（ByteDance）：机器人世界模型、视频生成；计划中。"
modelId: "irasim"
pageSource: generated
---

IRASim（交互式真实机器人动作模拟器）从初始帧加动作轨迹生成逼真的机械臂视频——机器人数据的学习型模拟器。本 catalog 中其模型、扩散与推理 pipeline 模块已内置树内；真实生成待手动就位官方 checkpoint 归档。

<ModelPaperFigures modelId="irasim" locale="zh" />

记录论文为[arXiv 2406.14540](https://arxiv.org/abs/2406.14540)。

项目页：[gen-irasim.github.io](https://gen-irasim.github.io/)。代码：[bytedance/IRASim](https://github.com/bytedance/IRASim)。

<ModelCommandBuilder modelId="irasim" locale="zh" />

## 用来做什么

需要动作条件世界步进时选 `irasim`：记录动作与帧进、滚动生成出。用来做交互世界推理，不是把 prompt 倒进文生视频。

按上游发布，DiT 视频扩散模型以逐帧动作 token 为条件，生成轨迹精确的机器人视频。打包在 synthesis/visual_generation/irasim/irasim_runtime 的运行时已裁剪训练数据加载器、评估脚本与交互 demo。该模型由字节跳动发布。

catalog 记录的任务包括 机器人世界模型、视频生成、action-conditioned-generation。别名包括 `ira-sim`、`ira_sim`。输出模态为视频。

记录的评测入口包括[EWMBench](/zh/docs/evaluation/benchmark-hub/ewmbench)（具身世界模型视频预测质量）。

## 运行时契约

必填 `image`、`prompt`；可选 `actions`；产物 `irasim.mp4`。

记录的任务 profile：`robotics-world-model`、`video-generation`、`action-conditioned-generation`。绑定的 pipeline 类是 `IRASimPipeline`。

系列状态：**计划中**。

发布的 checkpoint 归档不是当前下载器支持的 HF 模型仓库——期望得到真实视频前请手动提供 checkpoint 与任务 fixture。

checkpoint 需手动就位（非 HF 归档）。

## 在 WorldFoundry 中运行

catalog 启动命令是 `worldfoundry-eval run irasim --pipeline.task-profile robotics-world-model`。上方命令构建器按已记录任务切换 `--pipeline.task-profile`，发出的仍是 `worldfoundry-eval run`。

运行环境是统一 profile `worldfoundry-unified-cu128`（Python 3.10，Prepare Only）。

本卡片未记录公开 checkpoint id；按使用说明就位权重。就位权重用 `bash scripts/inference/prepare_model_infer.sh irasim`。

状态：**计划中**。

<ModelRelatedRecipes modelId="irasim" locale="zh" />
