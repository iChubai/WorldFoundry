---
title: "Kling API"
description: "Kling API（Kuaishou）：文生视频、图生视频；Runner 一致性待确认。"
modelId: "kling-api"
pageSource: generated
---

Kling API 是快手「可灵」视频生成系列的托管接入点，catalog 记录了文生视频与图生视频两类任务。提示词或参考图会提交到快手的服务端，渲染好的视频以 WorldFoundry 产物的形式返回。设立该条目的目的，是让托管的可灵结果与本地运行的开源模型共用同一套 Studio 结果格式，方便直接对比。

<ModelPaperFigures modelId="kling-api" locale="zh" />

项目页：[app.klingai.com](https://app.klingai.com/)。

<ModelCommandBuilder modelId="kling-api" locale="zh" />

## 用来做什么

`kling-api` 是托管推理线。卡片上没有本地 checkpoint 时从这里起步。

这是由服务方托管运行的模型：厂商未公开其架构、参数规模或训练细节，本仓库也未记录这些信息。WorldFoundry 记录的只是集成层——一条向服务方端点提交请求并回收生成视频产物的 Studio pipeline。该模型由快手（Kuaishou）发布。

catalog 记录的任务包括 文生视频、图生视频、托管 API。模态上，它接受文本、图像，输出视频。

记录的评测入口包括[VBench](/zh/docs/evaluation/benchmark-hub/vbench)（对比托管文/图生视频输出的标准多维质量评测）、[Physics-IQ Original](/zh/docs/evaluation/benchmark-hub/physics-iq)（检验服务方视频模型是否遵循基本物理合理性）。

## 运行时契约

本卡片未记录输入、旋钮或产物。

记录的任务 profile：`text-to-video`、`image-to-video`、`hosted-api`。绑定的 pipeline 类是 `KlingApiPipeline`。

系列状态：**Runner 一致性待确认**。

Catalog 将该条目标记为 integrated：托管 API 的 Studio pipeline 已可用，但你必须自备服务方的 API key 与账号。 本地不会下载任何权重、也不会创建 GPU 环境——生成在服务方完成，费用、配额与内容政策均以你的服务方账号为准。 凭据需在可灵开放平台（app.klingai.com）申请。

需要付费的服务方账号与 API key；WorldFoundry 不提供任何凭据。

## 在 WorldFoundry 中运行

启动 `worldfoundry-eval run kling-api --pipeline.task-profile text-to-video` 调托管接口。上方命令构建器按已记录任务切换 `--pipeline.task-profile`，发出的仍是 `worldfoundry-eval run`。

运行环境未记录。

就位权重用 `bash scripts/inference/prepare_model_infer.sh kling-api`。

状态：**Runner 一致性待确认**。

<ModelRelatedRecipes modelId="kling-api" locale="zh" />
