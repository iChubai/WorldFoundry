---
title: "LaST-R1"
description: "LaST-R1: vla and reinforcement learning; Runner parity pending."
modelId: "last-r1"
pageSource: generated
---

LaST-R1 is an MIT-licensed VLA policy trained with reinforcement learning, recorded against the confirmed chenhao01/LaST-R1 checkpoint. WorldFoundry routes it through the in-tree official-policy runtime adapter; weights are not yet staged,…

<ModelPaperFigures modelId="last-r1" locale="en" />

The recorded paper is [arXiv 2604.28192](https://arxiv.org/abs/2604.28192).

Project page: [siriyep.github.io/last-r1](https://siriyep.github.io/last-r1/). Code: [CHEN-H01/LaST-R1](https://github.com/CHEN-H01/LaST-R1).

<ModelCommandBuilder modelId="last-r1" locale="en" />

## What it is for

Use `last-r1` for vision-language-action inference on this card. The recipe exists so you can launch the recorded policy profile.

Task tags record a VLA plus reinforcement-learning design. Architecture internals beyond the official repository pointer (CHEN-H01/LaST-R1, pinned head ba4e9396) are not recorded in this repository.

Catalog tasks include vla, reinforcement learning, and policy rollout.

## Runtime contract

Required `image`, `prompt`, and `state`; optional `model_id` (last-r1), `policy_family`, and `action_representation`; artifacts `action_trace` and `last-r1_action_trace.json`.

Recorded task profiles: `vla`, `reinforcement_learning`, and `policy_rollout`. WorldFoundry binds `LastR1Pipeline`.

Family status: **Runner parity pending**.

Checkpoint absence is reported by runtime missing_assets and is not a catalog blocker; runs emit a runtime plan until weights are staged.

WorldFoundry routes LaST-R1 through the in-tree official-policy runtime adapter.

## Running in WorldFoundry

Launch `worldfoundry-eval run last-r1 --pipeline.task-profile vla` to execute the policy. The command builder above switches `--pipeline.task-profile` among recorded tasks and still emits `worldfoundry-eval run`.

It runs in the unified environment `worldfoundry-unified-cu128` (Python 3.11, CUDA 12.8).

Recorded weight repositories include `chenhao01/LaST-R1`. Stage assets with `bash scripts/inference/prepare_model_infer.sh last-r1`.

Status: **Runner parity pending**.

<ModelRelatedRecipes modelId="last-r1" locale="en" />
