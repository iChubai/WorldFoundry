---
title: "LaST-R1"
description: "LaST-R1：视觉-语言-动作（VLA）、策略执行；Runner 一致性待确认。"
modelId: "last-r1"
pageSource: generated
---

LaST-R1 是 MIT 许可、以强化学习训练的 VLA 策略，对应已确认的 chenhao01/LaST-R1 checkpoint。WorldFoundry 通过树内官方策略运行时适配器路由；权重尚未就位，运行输出运行时计划。

<ModelPaperFigures modelId="last-r1" locale="zh" />

记录论文为[arXiv 2604.28192](https://arxiv.org/abs/2604.28192)。

项目页：[siriyep.github.io/last-r1](https://siriyep.github.io/last-r1/)。代码：[CHEN-H01/LaST-R1](https://github.com/CHEN-H01/LaST-R1)。

<ModelCommandBuilder modelId="last-r1" locale="zh" />

## 用来做什么

用 `last-r1` 做本卡的视觉-语言-动作推理。这条配方用来启动已记录的策略 profile。

任务标签记录其为 VLA 加强化学习设计。官方仓库指针（CHEN-H01/LaST-R1，锁定 head ba4e9396）之外的架构内部细节仓库未记录。

catalog 记录的任务包括 视觉-语言-动作（VLA）、reinforcement_learning、策略执行。

## 运行时契约

必填 `image`、`prompt`、`state`；可选 `model_id` (last-r1)、`policy_family`、`action_representation`；产物 `action_trace`、`last-r1_action_trace.json`。

记录的任务 profile：`vla`、`reinforcement_learning`、`policy_rollout`。绑定的 pipeline 类是 `LastR1Pipeline`。

系列状态：**Runner 一致性待确认**。

checkpoint 缺失由运行时 missing_assets 报告，不视为 catalog 阻塞；权重就位前运行输出运行时计划。

仓库未记录 GPU 验证。

## 在 WorldFoundry 中运行

启动 `worldfoundry-eval run last-r1 --pipeline.task-profile vla` 执行策略。上方命令构建器按已记录任务切换 `--pipeline.task-profile`，发出的仍是 `worldfoundry-eval run`。

运行环境是统一 profile `worldfoundry-unified-cu128`（Python 3.11，CUDA 12.8）。

记录的权重仓库包括 `chenhao01/LaST-R1`。就位权重用 `bash scripts/inference/prepare_model_infer.sh last-r1`。

状态：**Runner 一致性待确认**。

<ModelRelatedRecipes modelId="last-r1" locale="zh" />
