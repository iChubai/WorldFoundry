---
title: "LiveWorld"
description: "LiveWorld is a camera-controlled video-to-video world model with a monitor-centric workflow: it consumes a video plus a geometry NPZ and re-renders the scene under new camera behavior."
pageSource: generated
---

**发表机构:** 开源研究发布（仓库未记录正式机构） · 研究机构
video → video

LiveWorld 是相机可控的视频到视频世界模型，采用以监控画面为中心的工作流：输入视频加几何 NPZ，在新的相机行为下重新渲染场景。本 catalog 中官方源码以固定 revision 内置，专用几何 NPZ 路径已接通；预测待四个缺失的 checkpoint snapshot 就位。

<ModelCommandBuilder modelId="liveworld" locale="zh" />

## 架构

记录的栈固定 Wan2.1-T2V-14B 与 SAM3，两者均已本地结构验证（apache-2.0）。除已记录结构外的架构内部细节仓库未存。

## 使用要点

运行时解析仅限本地；在四个缺失的 checkpoint snapshot 就位前预测保持受限。输入为提示词、视频与 geometry_npz。

## 评测 Benchmark

- [CameraBench](/zh/docs/evaluation/benchmark-hub/camerabench) — 重渲染视频的相机行为贴合度。

<ModelRelatedRecipes modelId="liveworld" locale="zh" />
