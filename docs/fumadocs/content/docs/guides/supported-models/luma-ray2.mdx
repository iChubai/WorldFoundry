---
title: "Luma Ray2"
description: "Luma Ray2 from Luma AI: text-to-video and image-to-video; Runner parity pending."
modelId: "luma-ray2"
pageSource: generated
---

Luma Ray2 is Luma AI's hosted video generation model, cataloged for text-to-video and image-to-video. WorldFoundry's Studio pipeline wraps the Luma API so a prompt or a start frame turns into a downloadable clip inside the normal WorldFoundry result layout. Nothing about the model runs locally; this entry is the bookkeeping and plumbing for the hosted route.

<ModelPaperFigures modelId="luma-ray2" locale="en" />

Project page: [lumalabs.ai](https://lumalabs.ai/).

<ModelCommandBuilder modelId="luma-ray2" locale="en" />

## What it is for

Pick `luma-ray2` when this card is a hosted API rather than a local checkpoint: call the recorded provider route instead of staging weights.

Catalog tasks include text-to-video, image-to-video, and hosted-api. Documented modalities: text and image in, video out.

Recorded evaluation entry points include [VBench](/docs/evaluation/benchmark-hub/vbench) (Standard multi-dimension quality suite for comparing hosted text/image-to-video outputs), [Physics-IQ Original](/docs/evaluation/benchmark-hub/physics-iq) (Probes whether provider-side video models respect basic physical plausibility).

## Runtime contract

Inputs, knobs, and artifacts are not recorded on this card.

Recorded task profiles: `text-to-video`, `image-to-video`, and `hosted-api`. WorldFoundry binds `LumaRay2Pipeline`.

Family status: **Runner parity pending**.

The catalog marks this entry as integrated: the hosted-API Studio pipeline is available, but you must supply your own provider API key and account. No weights are downloaded and no GPU environment is provisioned locally — generation happens on the provider side, so cost, quota, and content policy follow your provider account. Keys are issued through Luma's developer platform (lumalabs.ai).

Hosted API Studio pipeline is available; users must provide their own provider API key.

## Running in WorldFoundry

Launch `worldfoundry-eval run luma-ray2 --pipeline.task-profile text-to-video` to call the hosted route. The command builder above switches `--pipeline.task-profile` among recorded tasks and still emits `worldfoundry-eval run`.

The environment profile is not recorded.

Stage assets with `bash scripts/inference/prepare_model_infer.sh luma-ray2`.

Status: **Runner parity pending**.

<ModelRelatedRecipes modelId="luma-ray2" locale="en" />
