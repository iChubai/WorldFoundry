---
title: "MagicWorld"
description: "MagicWorld is vivo's interactive world model: from an image it generates camera-controlled world video, with a MagicWorld-Fast camera path wired in this catalog. The official source is vendored at a pinned revision; runner parity waits on…"
pageSource: generated
---

**发表机构:** vivo（影像研究团队） · 企业
image → video

MagicWorld 是 vivo 的交互式世界模型：从一张图生成相机可控的世界视频，本 catalog 已接通 MagicWorld-Fast 相机路径。官方源码以固定 revision 内置；runner parity 待 checkpoint 就位。

<ModelCommandBuilder modelId="magicworld" locale="zh" />

## 架构

除已记录任务外的架构内部细节仓库未存。注意分离的许可：代码为 apache-2.0，而上游 README 对发布的模型材料适用 CC BY-NC-SA 4.0 条款。

## 使用要点

输入为提示词、图像与 native_rows 相机控制。模型材料保留上游非商用相同方式共享条款——商用前请先审查。

## 评测 Benchmark

- [CameraBench](/zh/docs/evaluation/benchmark-hub/camerabench) — 交互滚动生成的相机控制贴合度。

<ModelRelatedRecipes modelId="magicworld" locale="zh" />
