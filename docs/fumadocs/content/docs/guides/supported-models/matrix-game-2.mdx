---
title: "Matrix-Game 2.0"
description: "Matrix-Game 2.0 from Skywork AI (Kunlun Tech): `image` and `official_bench_actions` in; `matrix-game-2-universal-ref-image-seed42-15f.mp4` and `matrix_game2_official_universal_seed42_15f.mp4` out; runner verified…"
modelId: "matrix-game-2"
pageSource: authored
---

Matrix-Game 2.0 is Skywork's real-time-oriented interactive world model generation: an auto-regressive diffusion design that follows keyboard/mouse actions across game scenes. In this catalog the runner is verified with the official inference_universal defaults — 150 frames at 12 fps, 352x640, official bench actions — through the in-tree runtime.

<ModelPaperFigures modelId="matrix-game-2" locale="en" />

The paper is [arXiv 2508.13009](https://arxiv.org/abs/2508.13009). Project page: [matrix-game-v2.github.io](https://matrix-game-v2.github.io). Code: [SkyworkAI/Matrix-Game](https://github.com/SkyworkAI/Matrix-Game).

<ModelCommandBuilder modelId="matrix-game-2" locale="en" />

## What it is for

Start on `matrix-game-2-universal-action-validation` with official bench actions. Not Matrix-Game 1 or 3.

Launch `worldfoundry-eval run matrix-game-2-universal-action-validation` with `--pipeline.task-profile official-universal-image`.

Score on [WorldModelBench](/docs/evaluation/benchmark-hub/worldmodelbench) — action-conditioned game-world rollout quality.

## Runtime contract

Required: `image` and `official_bench_actions`; optional: `prompt` and `video`; artifacts: `matrix-game-2-universal-ref-image-seed42-15f.mp4`, `matrix_game2_official_universal_seed42_15f.mp4`, and `matrix_game2_official_universal_seed42_15f.json`.

Defaults: 150 frames, 12 fps, 352×640, seed 42.

## Run in WorldFoundry

Status: **Verified**. Unified environment `worldfoundry-unified-cu128` (Python 3.11, CUDA 12.8). Stage `Skywork/Matrix-Game-2.0` locally — missing files fail at load, nothing is downloaded. Run `worldfoundry-eval run matrix-game-2-universal-action-validation` with `--pipeline.task-profile official-universal-image`. License: MIT. Minimum VRAM is recorded at 24 GB.

<ModelRelatedRecipes modelId="matrix-game-2" locale="en" />
