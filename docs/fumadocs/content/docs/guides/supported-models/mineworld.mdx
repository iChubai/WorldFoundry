---
title: "MineWorld"
description: "MineWorld from Microsoft: interactive-world-model and minecraft-world-model; Runner parity pending."
modelId: "mineworld"
pageSource: generated
---

MineWorld is Microsoft's real-time open-source interactive world model for Minecraft: an autoregressive transformer that plays forward Minecraft scenes from image/video context and actions (arXiv 2504.08388, MIT license). In this catalog the source is vendored in-tree; runnable execution still needs the official dependency environment, checkpoints, and validation/demo data.

<ModelPaperFigures modelId="mineworld" locale="en" />

The recorded paper is [MineWorld: a Real-Time and Open-Source Interactive World Model on Minecraft](https://arxiv.org/abs/2504.08388) (2025).

Code: [microsoft/mineworld](https://github.com/microsoft/mineworld).

<ModelCommandBuilder modelId="mineworld" locale="en" />

## What it is for

Use `mineworld` for catalog world-model inference. Choose it when you need action-conditioned future frames rather than a standalone clip generator.

Per the paper, a visual tokenizer plus an autoregressive transformer over interleaved state-action tokens, with parallel decoding for real-time speed. Internals beyond this are not recorded here.

Catalog tasks include interactive-world-model, minecraft-world-model, and action-conditioned-video.

Recorded evaluation entry points include [WorldModelBench](/docs/evaluation/benchmark-hub/worldmodelbench) (Action-conditioned game-world rollout quality).

## Runtime contract

Required `prompt`, `image`, and `video`; optional `actions` (minecraft_action, world_action); artifacts `mineworld.mp4`.

Recorded task profiles: `interactive-world-model`, `minecraft-world-model`, and `action-conditioned-video`. WorldFoundry binds `MineWorldPipeline`.

Family status: **Runner parity pending**.

The pipeline resolves the official inference entrypoint and returns blocked runtime diagnostics until checkpoints and validation data are staged.

WorldFoundry registers an in-tree runtime manifest pipeline for the vendored MineWorld source.

## Running in WorldFoundry

The catalog launch is `worldfoundry-eval run mineworld --pipeline.task-profile interactive-world-model`. The command builder above switches `--pipeline.task-profile` among recorded tasks and still emits `worldfoundry-eval run`.

It runs in the unified environment `worldfoundry-unified-cu128` (Python 3.11, CUDA 12.8).

No public checkpoint id is recorded on this card; follow the usage notes for staging. Stage assets with `bash scripts/inference/prepare_model_infer.sh mineworld`.

Status: **Runner parity pending**.

<ModelRelatedRecipes modelId="mineworld" locale="en" />
