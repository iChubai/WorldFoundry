---
title: "MineWorld"
description: "MineWorld（Microsoft）：交互式世界模型、Minecraft 世界模型；Runner 一致性待确认。"
modelId: "mineworld"
pageSource: generated
---

MineWorld 是微软面向 Minecraft 的实时开源交互世界模型：自回归 Transformer，从图像/视频上下文与动作向前推演 Minecraft 场景（arXiv 2504.08388，MIT 许可）。本 catalog 中源码已内置树内；可运行执行仍需官方依赖环境、checkpoint 与验证/demo 数据。

<ModelPaperFigures modelId="mineworld" locale="zh" />

记录论文为[MineWorld: a Real-Time and Open-Source Interactive World Model on Minecraft](https://arxiv.org/abs/2504.08388)（2025）。

代码：[microsoft/mineworld](https://github.com/microsoft/mineworld)。

<ModelCommandBuilder modelId="mineworld" locale="zh" />

## 用来做什么

用 `mineworld` 做 catalog 世界模型推理。需要动作条件未来帧、而不是独立片段生成器时选它。

按论文：视觉 tokenizer 加在「状态-动作」交错 token 上的自回归 Transformer，配并行解码实现实时速度。除此之外的内部细节此处未记录。该模型由微软（Microsoft）发布。

catalog 记录的任务包括 交互式世界模型、Minecraft 世界模型、动作条件视频。

记录的评测入口包括[WorldModelBench](/zh/docs/evaluation/benchmark-hub/worldmodelbench)（动作条件游戏世界滚动生成质量）。

## 运行时契约

必填 `prompt`、`image`、`video`；可选 `actions` (minecraft_action, world_action)；产物 `mineworld.mp4`。

记录的任务 profile：`interactive-world-model`、`minecraft-world-model`、`action-conditioned-video`。绑定的 pipeline 类是 `MineWorldPipeline`。

系列状态：**Runner 一致性待确认**。

pipeline 解析官方推理入口，在 checkpoint 与验证数据就位前返回 blocked 运行时诊断。

执行受 checkpoint/环境/数据限制。

## 在 WorldFoundry 中运行

catalog 启动命令是 `worldfoundry-eval run mineworld --pipeline.task-profile interactive-world-model`。上方命令构建器按已记录任务切换 `--pipeline.task-profile`，发出的仍是 `worldfoundry-eval run`。

运行环境是统一 profile `worldfoundry-unified-cu128`（Python 3.11，CUDA 12.8）。

本卡片未记录公开 checkpoint id；按使用说明就位权重。就位权重用 `bash scripts/inference/prepare_model_infer.sh mineworld`。

状态：**Runner 一致性待确认**。

<ModelRelatedRecipes modelId="mineworld" locale="zh" />
