---
title: "MMAudio"
description: "MMAudio generates soundtracks for silent video: given a video (and optionally a text prompt), it synthesizes audio that is semantically and temporally aligned with the visuals."
pageSource: generated
---

**发表机构:** 索尼 AI / 伊利诺伊大学厄巴纳-香槟分校 · 企业
text · video → audio

MMAudio 为无声视频生成配音：给定一段视频（可附文本提示词），它合成与画面语义和时间对齐的音频。在本 catalog 中它是为视频模型产物补配音的配套工具——产物类型是 generated_audio，而非视频。

<ModelCommandBuilder modelId="mmaudio" locale="zh" />

## 架构

MMAudio 是多模态联合训练的流匹配（flow-matching）模型，以视频帧与文本为条件生成音频（见其论文）。本仓库记录 prompt/video 输入契约与 generated_audio 产物；模型尺寸选项与内部细节仓库未记录。

## 使用要点

Runner parity 为 pending——本工作区尚未做 checkpoint 加载的产物一致性验证。典型工作流：先用任一文生/图生视频条目出片，再把视频交给 MMAudio 生成配音。

<ModelRelatedRecipes modelId="mmaudio" locale="zh" />
