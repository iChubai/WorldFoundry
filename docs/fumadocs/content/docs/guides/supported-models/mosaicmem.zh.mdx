---
title: "MosaicMem"
description: "MosaicMem：世界模型、视频生成；Runner 一致性待确认。"
modelId: "mosaicmem"
pageSource: generated
---

MosaicMem 是记忆增强的世界模型组件：摄取关键帧 patch、检索目标视图、汇总覆盖度并组合潜空间画布——产物是记忆索引而非视频。本 catalog 中其源码路径按记忆计划产物注册，并通过 worldfoundry.core.memory 暴露；生成器资产保持运行时限制。

<ModelPaperFigures modelId="mosaicmem" locale="zh" />

记录论文为[arXiv 2603.17117](https://arxiv.org/abs/2603.17117)。

项目页：[mosaicmem.github.io/mosaicmem](https://mosaicmem.github.io/mosaicmem/)。代码：[AbdelStark/mosaicmem](https://github.com/AbdelStark/mosaicmem)。

<ModelCommandBuilder modelId="mosaicmem" locale="zh" />

## 用来做什么

`mosaicmem` 是记录的交互世界线。要用动作推进世界时从这里起步。

架构内部细节仓库未记录（MIT 许可）；源码绑定在固定 commit。记录的产物类型为 memory_index。

catalog 记录的任务包括 世界模型、memory-augmented-generation、视频生成。输出模态为视频。

记录的评测入口包括[MemoBench](/zh/docs/evaluation/benchmark-hub/memobench)（长时程记忆一致性评测）。

## 运行时契约

必填 `image`、`prompt`、`video`；可选 `actions` (memory_update, interaction)；产物 `memory_index`、`mosaicmem_runtime_plan.json`。

记录的任务 profile：`world-model`、`memory-augmented-generation`、`video-generation`。绑定的 pipeline 类是 `MosaicMemPipeline`。

系列状态：**Runner 一致性待确认**。

配置解析与 pipeline 导入预期可通过；在所需 checkpoint/资产就位前，真实生成会输出运行时计划。 缺失资产由计划报告，不视为 catalog 阻塞。 请把它当作世界模型栈的记忆侧组件使用，而非独立生成器。

产物是记忆索引，不是视频。

## 在 WorldFoundry 中运行

catalog 启动命令是 `worldfoundry-eval run mosaicmem --pipeline.task-profile world-model`。上方命令构建器按已记录任务切换 `--pipeline.task-profile`，发出的仍是 `worldfoundry-eval run`。

运行环境是统一 profile `worldfoundry-unified-cu128`（Python 3.11，CUDA 12.8）。

本卡片未记录公开 checkpoint id；按使用说明就位权重。就位权重用 `bash scripts/inference/prepare_model_infer.sh mosaicmem`。

状态：**Runner 一致性待确认**。

<ModelRelatedRecipes modelId="mosaicmem" locale="zh" />
