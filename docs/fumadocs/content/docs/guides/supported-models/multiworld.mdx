---
title: "MultiWorld"
description: "MultiWorld (\"It Takes Two\") is a multi-agent interactive video generator: two players' actions drive one shared generated game world."
pageSource: generated
---

**Publisher:** Open-source research release (institution not recorded) · Research lab
video

MultiWorld ("It Takes Two") is a multi-agent interactive video generator: two players' actions drive one shared generated game world. In this catalog the in-tree ItTakesTwo wrapper is registered as the model-zoo entry; full generation remains dependency/checkpoint gated.

<ModelCommandBuilder modelId="multiworld" locale="en" />

## Architecture

Architecture internals beyond the recorded tasks are not stored in this repository. The upstream license is custom and flagged for manual review.

## Usage notes

Full generation is dependency/checkpoint gated — treat this entry as planned until assets and environment parity are staged. Review the custom upstream license first.

## Evaluation benchmarks

- [WorldModelBench](/docs/evaluation/benchmark-hub/worldmodelbench) — Multi-agent world-rollout quality.

<ModelRelatedRecipes modelId="multiworld" locale="en" />
