---
title: "MVDiffusion"
description: "MVDiffusion（Simon Fraser University）：3D 与 4D；Runner 一致性待确认。"
modelId: "mvdiffusion"
pageSource: generated
---

MVDiffusion 一次性生成一致的多视角图像——从文本或透视图生成全景，或为场景生成纹理视图——方法是让多个扩散分支相互注意。本 catalog 中它经树内运行时子进程适配器运行；runner parity 待运行时资产就位。

<ModelPaperFigures modelId="mvdiffusion" locale="zh" />

记录论文为[MVDiffusion: Enabling Holistic Multi-view Image Generation with Correspondence-Aware Diffusion](https://arxiv.org/abs/2307.01097)（NeurIPS 2023 (spotlight), 2023）。

项目页：[mvdiffusion.github.io](https://mvdiffusion.github.io/)。代码：[Tangshitao/MVDiffusion](https://github.com/Tangshitao/MVDiffusion)。

<ModelCommandBuilder modelId="mvdiffusion" locale="zh" />

## 用来做什么

做前馈几何时选 `mvdiffusion`：视图进、重建/轨迹/点云出。需要 catalog 的 3D 路由、而不是仅深度先验时用它。

按 NeurIPS 2023 论文（arXiv 2307.01097），多个并行扩散分支借助「对应感知注意力」同时生成所有视图并保持全局一致。源码、checkpoint 或 CUDA 依赖缺失会在推理时报告。该模型由西蒙菲莎大学（Simon Fraser University）发布。

catalog 记录的任务包括 correspondence-aware multiview image generation and scene texturing。别名包括 `MVDiffusion: Enabling Holistic Multi-view Image Generation`。

## 运行时契约

必填 `prompt`、`image`；可选 `video`、`actions`；产物 `generated_3d_asset`、`mvdiffusion.png`。

记录的任务 profile：`correspondence-aware multiview image generation and scene texturing`。绑定的 pipeline 类是 `MVDiffusionPipeline`。

系列状态：**Runner 一致性待确认**。

当前集成已接通 plan-only 推理；完整产物一致性需要本地就位上游源码与 checkpoint。 期望得到真实多视角输出前请先完成资产就位。

训练面向全景场景：自由相机轨迹不在其范围内。

## 在 WorldFoundry 中运行

启动 `worldfoundry-eval run mvdiffusion --pipeline.task-profile 'correspondence-aware multiview image generation and scene texturing'` 写出几何。上方命令构建器按已记录任务切换 `--pipeline.task-profile`，发出的仍是 `worldfoundry-eval run`。

运行环境是统一 profile `worldfoundry-unified-cu128`（Python 3.11，CUDA 12.8）。

本卡片未记录公开 checkpoint id；按使用说明就位权重。就位权重用 `bash scripts/inference/prepare_model_infer.sh mvdiffusion`。

状态：**Runner 一致性待确认**。

<ModelRelatedRecipes modelId="mvdiffusion" locale="zh" />
