---
title: "NeoVerse"
description: "NeoVerse from CreateAI (formerly TuSimple): 4D world modeling from in-the-wild monocular videos; Verified."
modelId: "neoverse"
pageSource: generated
---

NeoVerse is a 4D world model built as a native assembly over canonical Wan components plus model-specific control and reconstruction roles: it enhances world generation with 4D-aware conditioning. In this catalog the runner is verified for the README-style tilt_up video demo with local NeoVerse assets; the official video-input path uses static_scene=false.

<ModelPaperFigures modelId="neoverse" locale="en" />

The recorded paper is [NeoVerse: Enhancing 4D World Model with in-the-wild Monocular Videos](https://arxiv.org/abs/2601.00393) (CVPR 2026, 2026).

Project page: [neoverse-4d.github.io](https://neoverse-4d.github.io/). Code: [IamCreateAI/NeoVerse](https://github.com/IamCreateAI/NeoVerse).

<ModelCommandBuilder modelId="neoverse" locale="en" />

## What it is for

`neoverse` is the recorded interactive world line. Start here when the job is stepping a world with actions.

Per the upstream release (arXiv 2601.00393), NeoVerse layers 4D reconstruction and control roles over a Wan video backbone. WorldFoundry expresses this as a native assembly over shared Wan infrastructure — real inference stays checkpoint/GPU gated with no metadata-only runner shim. It is published by CreateAI (formerly TuSimple).

Catalog tasks include 4D world modeling from in-the-wild monocular videos and official-tilt-up-video. Catalog aliases include `NeoVerse: Enhancing 4D World Model`.

Recorded evaluation entry points include [WorldScore](/docs/evaluation/benchmark-hub/worldscore) (Scores navigable generated worlds), [4DWorldBench](/docs/evaluation/benchmark-hub/4dworldbench) (Matches the entry's 4D world-model focus).

## Runtime contract

Required `prompt`, `image`, and `video`; optional `actions` (camera_actions, predefined_trajectory, trajectory_file); artifacts `generated_world`, `generated_4d_scene`, and `neoverse.mp4`.

Recorded task profiles: `4D world modeling from in-the-wild monocular videos` and `official-tilt-up-video`. WorldFoundry binds `NeoVersePipeline`.

Family status: **Verified**.

Recorded `official-tilt-up-video` defaults: num_frames=81, height=336, width=560, num_inference_steps=4, seed=42.

Runtime execution requires NeoVerse, Wan, reconstructor, LoRA, and CUDA assets.

## Running in WorldFoundry

The catalog launch is `worldfoundry-eval run neoverse --pipeline.task-profile official-tilt-up-video`. The command builder above switches `--pipeline.task-profile` among recorded tasks and still emits `worldfoundry-eval run`.

It runs in the unified environment `worldfoundry-unified-cu128` (Python 3.11, CUDA 12.8).

Recorded weight repositories include `Yuppie1204/NeoVerse`. Stage assets with `bash scripts/inference/prepare_model_infer.sh neoverse`.

Status: **Verified**.

<ModelRelatedRecipes modelId="neoverse" locale="en" />
