---
title: "OmniForcing"
description: "OmniForcing：实时生成、蒸馏；Runner 一致性待确认。"
modelId: "omniforcing"
pageSource: generated
---

OmniForcing 是以蒸馏构建的实时音视频世界模型：目标是交互速度下的音视频联合生成。本 catalog 中官方源码路径已注册、源码绑定在固定 commit；checkpoint 与运行时资产保持运行时限制。

<ModelPaperFigures modelId="omniforcing" locale="zh" />

记录论文为[arXiv 2603.11647](https://arxiv.org/abs/2603.11647)。

代码：[OmniForcing/OmniForcing](https://github.com/OmniForcing/OmniForcing)。

<ModelCommandBuilder modelId="omniforcing" locale="zh" />

## 用来做什么

需要动作条件世界步进时选 `omniforcing`：记录动作与帧进、滚动生成出。用来做交互世界推理，不是把 prompt 倒进文生视频。

除已记录任务（音视频世界模型、实时生成、蒸馏）外的架构内部细节仓库未存。上游仓库缺少根许可文件，已标记需人工审查。

catalog 记录的任务包括 audio-visual-world-model、实时生成、蒸馏。

## 运行时契约

必填 `audio`、`image`、`prompt`、`video`；可选 `actions` (forcing_signal, interaction)；产物 `generated_video`、`omniforcing_runtime_plan.json`。

记录的任务 profile：`audio-visual-world-model`、`real-time-generation`、`distillation`。绑定的 pipeline 类是 `OmniForcingPipeline`。

系列状态：**Runner 一致性待确认**。

配置解析与 pipeline 导入预期可通过；在所需 checkpoint/资产就位前，真实生成会输出运行时计划。 缺失资产由计划报告，不视为 catalog 阻塞。

WorldFoundry tracks upstream source and checkpoint 计划中; no runnable pipeline is registered.

## 在 WorldFoundry 中运行

catalog 启动命令是 `worldfoundry-eval run omniforcing --pipeline.task-profile audio-visual-world-model`。上方命令构建器按已记录任务切换 `--pipeline.task-profile`，发出的仍是 `worldfoundry-eval run`。

运行环境是统一 profile `worldfoundry-unified-cu128`（Python 3.11，CUDA 12.8）。

本卡片未记录公开 checkpoint id；按使用说明就位权重。就位权重用 `bash scripts/inference/prepare_model_infer.sh omniforcing`。

状态：**Runner 一致性待确认**。

<ModelRelatedRecipes modelId="omniforcing" locale="zh" />
