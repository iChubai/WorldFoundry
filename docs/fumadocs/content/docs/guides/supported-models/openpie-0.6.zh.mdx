---
title: "OpenPIE 0.6"
description: "OpenPIE 0.6 is Exla AI's open-weights pi-0.6-style policy, tracked as its own catalog entry separate from OpenPI pi0/pi0.5. The exla-ai/openpie-0.6 checkpoint is confirmed on Hugging Face; WorldFoundry routes it through the in-tree officia…"
pageSource: generated
---

**发表机构:** Exla AI · 企业

OpenPIE 0.6 是 Exla AI 的开放权重 pi-0.6 风格策略，作为独立 catalog 条目跟踪，与 OpenPI pi0/pi0.5 分开。exla-ai/openpie-0.6 checkpoint 已在 Hugging Face 确认；WorldFoundry 通过树内官方策略运行时以 FLA 策略工厂路由。

<ModelCommandBuilder modelId="openpie-0.6" locale="zh" />

## 架构

运行时契约为 exla-ai/fla 仓库的 FLA 策略工厂（锁定 head 1ac45102）。架构内部细节仓库未记录。

## 使用要点

checkpoint 缺失由运行时 missing_assets 报告，不视为 catalog 阻塞；权重就位前运行输出运行时计划。

<ModelRelatedRecipes modelId="openpie-0.6" locale="zh" />
