---
title: "Pandora"
description: "Pandora（Maitrix.org (UC San Diego / MBZUAI)）：世界生成、文生视频；计划中。"
modelId: "pandora"
pageSource: generated
---

Pandora 是较早的通用世界模型，采用自然语言动作与视频状态：把世界状态模拟成视频，并允许在任意步骤用自由文本动作操控模拟。本 catalog 中其模型、ChatUniVi 与 DynamiCrafter 运行时模块已内置树内；由于官方权重因数据许可限制未公开，完整生成保持 blocked。

<ModelPaperFigures modelId="pandora" locale="zh" />

记录论文为[arXiv 2406.09401](https://arxiv.org/abs/2406.09401)。

项目页：[world-model.maitrix.org](https://world-model.maitrix.org/)。代码：[maitrix-org/Pandora](https://github.com/maitrix-org/Pandora)。

<ModelCommandBuilder modelId="pandora" locale="zh" />

## 用来做什么

需要动作条件世界步进时选 `pandora`：记录动作与帧进、滚动生成出。用来做交互世界推理，不是把 prompt 倒进文生视频。

按上游发布，自回归 LLM 主干（ChatUniVi 系）连接视频扩散解码器（DynamiCrafter 系），使语言动作操控视频状态滚动。内置运行时位于 synthesis/visual_generation/pandora/pandora_runtime。该模型由Maitrix.org（加州大学圣迭戈分校 / MBZUAI）发布。

catalog 记录的任务包括 diffusion-world-model、世界生成、文生视频。别名包括 `pandora-world`。模态上，它接受文本，输出视频。

记录的评测入口包括[WorldModelBench](/zh/docs/evaluation/benchmark-hub/worldmodelbench)（语言动作世界滚动生成质量）。

## 运行时契约

必填 `prompt`；可选 `image`；产物 `pandora.mp4`。

记录的任务 profile：`diffusion-world-model`、`world-generation`、`text-to-video`。绑定的 pipeline 类是 `PandoraPipeline`。

系列状态：**计划中**。

PandoraSynthesis 提供运行时计划封装；由于官方权重隐藏或不可得，真实生成需要调用方提供官方兼容的 checkpoint。

官方权重因数据许可限制未公开——需调用方提供 checkpoint。

## 在 WorldFoundry 中运行

catalog 启动命令是 `worldfoundry-eval run pandora --pipeline.task-profile diffusion-world-model`。上方命令构建器按已记录任务切换 `--pipeline.task-profile`，发出的仍是 `worldfoundry-eval run`。

运行环境是统一 profile `worldfoundry-unified-cu128`（Python 3.10，Prepare Only）。

本卡片未记录公开 checkpoint id；按使用说明就位权重。就位权重用 `bash scripts/inference/prepare_model_infer.sh pandora`。

状态：**计划中**。

<ModelRelatedRecipes modelId="pandora" locale="zh" />
