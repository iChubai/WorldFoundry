---
title: "Pi3"
description: "Pi3：3D 重建、轨迹；Runner 一致性待确认; $π^3$。"
modelId: "pi3"
pageSource: generated
---

Pi3（π³）是前馈式视觉几何模型，重建 3D 场景、相机轨迹与点云，且不依赖固定参考视图——其置换等变设计对所有输入视图一视同仁。本 catalog 中树内点云导航 pipeline 已按重建与轨迹渲染注册。

<ModelPaperFigures modelId="pi3" locale="zh" />

记录论文为[$π^3$: Permutation-Equivariant Visual Geometry Learning](https://arxiv.org/abs/2507.13347)（2025）。

项目页：[yyfz.github.io/pi3](https://yyfz.github.io/pi3/)。代码：[yyfz/Pi3](https://github.com/yyfz/Pi3)。

<ModelCommandBuilder modelId="pi3" locale="zh" />

## 用来做什么

`pi3` 是记录的重建线。从记录的视图写出几何时从这里起步。

按论文（arXiv 2507.13347），Pi3 用置换等变 Transformer 预测仿射不变的相机位姿与尺度不变的局部点图，摆脱了参考视图启发式。权重为 apache-2.0；除已记录任务外的内部细节此处未存。

catalog 记录的任务包括 3D 重建、轨迹、点云。输出模态为点云。

## 运行时契约

本卡片未记录输入、旋钮或产物。

记录的任务 profile：`3d-reconstruction`、`trajectory`、`point-cloud`。绑定的 pipeline 类是 `Pi3Pipeline`。

系列状态：**Runner 一致性待确认**。

期望得到真实重建输出前请先就位上游 Pi3 权重。

## 在 WorldFoundry 中运行

启动 `worldfoundry-eval run pi3 --pipeline.task-profile 3d-reconstruction` 写出几何。上方命令构建器按已记录任务切换 `--pipeline.task-profile`，发出的仍是 `worldfoundry-eval run`。

运行环境未记录。

记录的权重仓库包括 `yyfz233/Pi3X`。就位权重用 `bash scripts/inference/prepare_model_infer.sh pi3`。

状态：**Runner 一致性待确认**。

<ModelRelatedRecipes modelId="pi3" locale="zh" />
