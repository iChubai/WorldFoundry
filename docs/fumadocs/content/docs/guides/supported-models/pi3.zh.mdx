---
title: "Pi3"
description: "Pi3 (π³) is a feed-forward visual-geometry model that reconstructs 3D scenes, camera trajectories, and point clouds without relying on a fixed reference view — its permutation-equivariant design treats all input views symmetrically."
pageSource: generated
---

**发表机构:** 开源研究发布（仓库未记录正式机构） · 研究机构
point cloud
[$π^3$: Permutation-Equivariant Visual Geometry Learning](https://arxiv.org/abs/2507.13347) (2025)

Pi3（π³）是前馈式视觉几何模型，重建 3D 场景、相机轨迹与点云，且不依赖固定参考视图——其置换等变设计对所有输入视图一视同仁。本 catalog 中树内点云导航 pipeline 已按重建与轨迹渲染注册。

<figure className="wf-recipe-media is-diagram">
  <img src="/models/pi3/paper.png" alt="$π^3$: Permutation-Equivariant Visual Geometry Learning" />
  <figcaption>$π^3$: Permutation-Equivariant Visual Geometry Learning</figcaption>
</figure>

<ModelCommandBuilder modelId="pi3" locale="zh" />

## 架构

按论文（arXiv 2507.13347），Pi3 用置换等变 Transformer 预测仿射不变的相机位姿与尺度不变的局部点图，摆脱了参考视图启发式。权重为 apache-2.0；除已记录任务外的内部细节此处未存。

## 使用要点

集成为 runtime_ported，runner parity 待验证；每次运行的验证证据在 catalog 外跟踪。期望得到真实重建输出前请先就位上游 Pi3 权重。

<ModelRelatedRecipes modelId="pi3" locale="zh" />
