---
title: "Runway Gen-4.5"
description: "Runway Gen-4.5 from Runway: image-to-video and hosted-api; Runner parity pending."
modelId: "runway-gen4p5"
pageSource: generated
---

Runway Gen-4.5 is Runway's hosted image-to-video model. Give it a start image and it returns an animated clip generated on Runway's service; the catalog keeps the legacy aliases gen_3_i2v and gen-3-i2v pointing at this entry so earlier WorldFoundry configs still resolve. Like every hosted_api entry, it exists so provider-rendered results flow through the same Studio pipeline and artifact format as local models.

<ModelPaperFigures modelId="runway-gen4p5" locale="en" />

Project page: [runwayml.com](https://runwayml.com/).

<ModelCommandBuilder modelId="runway-gen4p5" locale="en" />

## What it is for

Pick `runway-gen4p5` when this card is a hosted API rather than a local checkpoint: call the recorded provider route instead of staging weights.

Catalog tasks include image-to-video and hosted-api. Catalog aliases include `gen_3_i2v` and `gen-3-i2v`. Documented modalities: image in, video out.

Recorded evaluation entry points include [VBench](/docs/evaluation/benchmark-hub/vbench) (Standard multi-dimension quality suite for comparing hosted text/image-to-video outputs), [Physics-IQ Original](/docs/evaluation/benchmark-hub/physics-iq) (Probes whether provider-side video models respect basic physical plausibility).

## Runtime contract

Inputs, knobs, and artifacts are not recorded on this card.

Recorded task profiles: `image-to-video` and `hosted-api`. WorldFoundry binds `RunwayGen4p5Pipeline`.

Family status: **Runner parity pending**.

The catalog marks this entry as integrated: the hosted-API Studio pipeline is available, but you must supply your own provider API key and account. No weights are downloaded and no GPU environment is provisioned locally — generation happens on the provider side, so cost, quota, and content policy follow your provider account. Runway API credentials are managed in your Runway workspace settings.

Hosted API Studio pipeline is available; users must provide their own provider API key.

## Running in WorldFoundry

Launch `worldfoundry-eval run runway-gen4p5 --pipeline.task-profile image-to-video` to call the hosted route. The command builder above switches `--pipeline.task-profile` among recorded tasks and still emits `worldfoundry-eval run`.

The environment profile is not recorded.

Stage assets with `bash scripts/inference/prepare_model_infer.sh runway-gen4p5`.

Status: **Runner parity pending**.

<ModelRelatedRecipes modelId="runway-gen4p5" locale="en" />
