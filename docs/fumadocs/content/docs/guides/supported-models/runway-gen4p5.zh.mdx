---
title: "Runway Gen-4.5"
description: "Runway Gen-4.5（Runway）：图生视频、托管 API；Runner 一致性待确认。"
modelId: "runway-gen4p5"
pageSource: generated
---

Runway Gen-4.5 是 Runway 的托管图生视频模型。提供一张起始图，它在 Runway 服务端生成动态视频； catalog 保留了 gen_3_i2v、gen-3-i2v 两个历史别名指向本条目，保证早期 WorldFoundry 配置继续可用。与所有 hosted_api 条目一样，它的意义在于让服务方渲染的结果走进与本地模型一致的 Studio pipeline 与产物格式。

<ModelPaperFigures modelId="runway-gen4p5" locale="zh" />

项目页：[runwayml.com](https://runwayml.com/)。

<ModelCommandBuilder modelId="runway-gen4p5" locale="zh" />

## 用来做什么

本卡是托管 API 而不是本地 checkpoint 时选 `runway-gen4p5`：走记录的提供商路由，而不是就位权重。

这是由服务方托管运行的模型：厂商未公开其架构、参数规模或训练细节，本仓库也未记录这些信息。WorldFoundry 记录的只是集成层——一条向服务方端点提交请求并回收生成视频产物的 Studio pipeline。

catalog 记录的任务包括 图生视频、托管 API。别名包括 `gen_3_i2v`、`gen-3-i2v`。模态上，它接受图像，输出视频。

记录的评测入口包括[VBench](/zh/docs/evaluation/benchmark-hub/vbench)（对比托管文/图生视频输出的标准多维质量评测）、[Physics-IQ Original](/zh/docs/evaluation/benchmark-hub/physics-iq)（检验服务方视频模型是否遵循基本物理合理性）。

## 运行时契约

本卡片未记录输入、旋钮或产物。

记录的任务 profile：`image-to-video`、`hosted-api`。绑定的 pipeline 类是 `RunwayGen4p5Pipeline`。

系列状态：**Runner 一致性待确认**。

Catalog 将该条目标记为 integrated：托管 API 的 Studio pipeline 已可用，但你必须自备服务方的 API key 与账号。 本地不会下载任何权重、也不会创建 GPU 环境——生成在服务方完成，费用、配额与内容政策均以你的服务方账号为准。 Runway 的 API 凭据在其工作区设置中管理。

需要付费的服务方账号与 API key；WorldFoundry 不提供任何凭据。

## 在 WorldFoundry 中运行

启动 `worldfoundry-eval run runway-gen4p5 --pipeline.task-profile image-to-video` 调托管接口。上方命令构建器按已记录任务切换 `--pipeline.task-profile`，发出的仍是 `worldfoundry-eval run`。

运行环境未记录。

就位权重用 `bash scripts/inference/prepare_model_infer.sh runway-gen4p5`。

状态：**Runner 一致性待确认**。

<ModelRelatedRecipes modelId="runway-gen4p5" locale="zh" />
