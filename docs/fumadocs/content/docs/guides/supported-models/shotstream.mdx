---
title: "ShotStream"
description: "ShotStream from Kuaishou (Kling Team): interactive-video-generation and real-time-video-generation; Runner parity pending."
modelId: "shotstream"
pageSource: generated
---

ShotStream is Kling AI Research's real-time interactive streaming video generator: it targets continuous, interactively steered video streams rather than fixed clips. In this catalog the official inference route is registered with the source bound at a pinned commit; checkpoint and streaming assets stay runtime-gated.

<ModelPaperFigures modelId="shotstream" locale="en" />

The recorded paper is [arXiv 2603.25746](https://arxiv.org/abs/2603.25746).

Project page: [luo0207.github.io/ShotStream](https://luo0207.github.io/ShotStream/). Code: [KlingAIResearch/ShotStream](https://github.com/KlingAIResearch/ShotStream).

<ModelCommandBuilder modelId="shotstream" locale="en" />

## What it is for

Pick `shotstream` when you want this family's text-to-video line in WorldFoundry: a prompt in, a generated clip out. Use it as the catalog T2V start rather than an editor or a world-model stepper.

Architecture internals beyond the recorded tasks are not stored in this repository (weights at KlingTeam/ShotStream). The upstream repository lacks a root license and is flagged for manual review. It is published by Kuaishou (Kling Team).

Catalog tasks include interactive-video-generation, real-time-video-generation, streaming-video, and causal-video. Catalog aliases include `interactive-video-generation`. Documented outputs include video.

## Runtime contract

Required `image`, `prompt`, and `video`; optional `actions` (stream_control, edit); artifacts `generated_video` and `shotstream.mp4`.

Recorded task profiles: `interactive-video-generation`, `real-time-video-generation`, `streaming-video`, and `causal-video`. WorldFoundry binds `ShotStreamPipeline`.

Family status: **Runner parity pending**.

Recorded `causal-video` defaults: seed=42, fps=16.

Config resolution and pipeline import are expected to pass; real generation emits a runtime plan until required checkpoints/assets are staged. Missing assets are reported by the plan and are not treated as catalog blockers.

## Running in WorldFoundry

Launch `worldfoundry-eval run shotstream --pipeline.task-profile causal-video` to sample the clip. The command builder above switches `--pipeline.task-profile` among recorded tasks and still emits `worldfoundry-eval run`.

It runs in the unified environment `worldfoundry-unified-cu128` (Python 3.11, CUDA 12.8).

Recorded weight repositories include `KlingTeam/ShotStream` and `Wan-AI/Wan2.1-T2V-1.3B`. Stage assets with `bash scripts/inference/prepare_model_infer.sh shotstream`.

Status: **Runner parity pending**.

<ModelRelatedRecipes modelId="shotstream" locale="en" />
