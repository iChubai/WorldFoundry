---
title: "ShotStream"
description: "ShotStream is Kling AI Research's real-time interactive streaming video generator: it targets continuous, interactively steered video streams rather than fixed clips."
pageSource: generated
---

**发表机构:** 快手（可灵团队） · 企业
video

ShotStream 是快手可灵研究团队的实时交互流式视频生成器：目标是连续、可交互操控的视频流，而非固定片段。本 catalog 中官方推理路径已注册、源码绑定在固定 commit；checkpoint 与流式资产保持运行时限制。

<ModelCommandBuilder modelId="shotstream" locale="zh" />

## 架构

除已记录任务外的架构内部细节仓库未存（权重位于 KlingTeam/ShotStream）。上游仓库缺少根许可文件，已标记需人工审查。

## 使用要点

配置解析与 pipeline 导入预期可通过；在所需 checkpoint/资产就位前，真实生成会输出运行时计划。缺失资产由计划报告，不视为 catalog 阻塞。

<ModelRelatedRecipes modelId="shotstream" locale="zh" />
