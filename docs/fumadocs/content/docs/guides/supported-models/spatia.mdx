---
title: "Spatia"
description: "Spatia: camera-controlled-video and video-to-video; Runner parity pending."
modelId: "spatia"
pageSource: generated
---

Spatia is a camera-controlled video-to-video world model: given source video and a target camera trajectory it re-renders the scene along the new viewpoint path. WorldFoundry vendors the official source in-tree (revision f75b10c9) and exposes it through the shared pipeline runner with the Jinjing713/Spatia checkpoint reference, released under Apache-2.0.

<ModelPaperFigures modelId="spatia" locale="en" />

The recorded paper is [arXiv 2512.15716](https://arxiv.org/abs/2512.15716).

Project page: [zhaojingjing713.github.io/Spatia](https://zhaojingjing713.github.io/Spatia/). Code: [ZhaoJingjing713/Spatia](https://github.com/ZhaoJingjing713/Spatia). Code: [facebookresearch/map-anything](https://github.com/facebookresearch/map-anything). Code: [castacks/UniCeption](https://github.com/castacks/UniCeption). Code: [facebookresearch/dinov2](https://github.com/facebookresearch/dinov2).

<ModelCommandBuilder modelId="spatia" locale="en" />

## What it is for

Pick `spatia` when you need camera-controlled video: a camera trajectory in, a steered clip out. Use it instead of unconstrained T2V when viewpoint must follow a path.

The in-tree route wires Spatia inference with a dense world-to-camera (W2C) trajectory input: per-frame camera extrinsics drive the re-rendering. Architecture internals beyond the vendored source are not recorded in this repository.

Catalog tasks include camera-controlled-video, video-to-video, and world-model. Documented modalities: video in, video out.

Recorded evaluation entry points include [CameraBench](/docs/evaluation/benchmark-hub/camerabench) (Camera-trajectory adherence for controlled re-rendering).

## Runtime contract

Required `prompt`, `image_or_video`, `w2c_trajectory_file`, and `intrinsics`; artifacts `spatia.mp4`.

Recorded task profiles: `camera-controlled-video`, `video-to-video`, and `world-model`. WorldFoundry binds `SpatiaPipeline`.

Family status: **Runner parity pending**.

Provide source video plus a dense per-frame W2C camera trajectory; the in-tree route validates both before execution.

Official source is vendored at revision f75b10c9bb5f6b0cd779ec8c41ab33dd8382dba3.

## Running in WorldFoundry

Launch `worldfoundry-eval run spatia --pipeline.task-profile camera-controlled-video` to follow the camera path. The command builder above switches `--pipeline.task-profile` among recorded tasks and still emits `worldfoundry-eval run`.

It runs in the unified environment `worldfoundry-unified-cu128` (Python 3.11, CUDA 12.8).

Recorded weight repositories include `Jinjing713/Spatia`, `Wan-AI/Wan2.2-TI2V-5B`, and `facebook/map-anything`. Stage assets with `bash scripts/inference/prepare_model_infer.sh spatia`.

Status: **Runner parity pending**.

<ModelRelatedRecipes modelId="spatia" locale="en" />
