---
title: "Spatia"
description: "Spatia：相机可控视频、视频到视频；Runner 一致性待确认。"
modelId: "spatia"
pageSource: generated
---

Spatia 是相机控制的 video-to-video 世界模型：给定源视频与目标相机轨迹，沿新视角路径重新渲染场景。WorldFoundry 已将官方源码内置树内（revision f75b10c9），通过共享 pipeline runner 暴露， checkpoint 引用为 Jinjing713/Spatia，以 Apache-2.0 许可发布。

<ModelPaperFigures modelId="spatia" locale="zh" />

记录论文为[arXiv 2512.15716](https://arxiv.org/abs/2512.15716)。

项目页：[zhaojingjing713.github.io/Spatia](https://zhaojingjing713.github.io/Spatia/)。代码：[ZhaoJingjing713/Spatia](https://github.com/ZhaoJingjing713/Spatia)。代码：[facebookresearch/map-anything](https://github.com/facebookresearch/map-anything)。代码：[castacks/UniCeption](https://github.com/castacks/UniCeption)。代码：[facebookresearch/dinov2](https://github.com/facebookresearch/dinov2)。

<ModelCommandBuilder modelId="spatia" locale="zh" />

## 用来做什么

需要相机可控视频时选 `spatia`：相机轨迹进、受控视频出。视角必须跟随路径时，用它而不是无约束 T2V。

树内路由接入 Spatia 推理与稠密 world-to-camera（W2C）轨迹输入：逐帧相机外参驱动重渲染。除内置源码外的架构内部细节仓库未记录。

catalog 记录的任务包括 相机可控视频、视频到视频、世界模型。模态上，它接受视频，输出视频。

记录的评测入口包括[CameraBench](/zh/docs/evaluation/benchmark-hub/camerabench)（相机轨迹跟随与受控重渲染质量）。

## 运行时契约

必填 `prompt`、`image_or_video`、`w2c_trajectory_file`、`intrinsics`；产物 `spatia.mp4`。

记录的任务 profile：`camera-controlled-video`、`video-to-video`、`world-model`。绑定的 pipeline 类是 `SpatiaPipeline`。

系列状态：**Runner 一致性待确认**。

需提供源视频与逐帧稠密 W2C 相机轨迹；树内路由在执行前会校验二者。 runner parity 仍为 pending，在记录 parity 运行前请将输出视为未验证。

需要逐帧稠密相机轨迹，而非稀疏关键帧位姿。

## 在 WorldFoundry 中运行

启动 `worldfoundry-eval run spatia --pipeline.task-profile camera-controlled-video` 跟随相机路径。上方命令构建器按已记录任务切换 `--pipeline.task-profile`，发出的仍是 `worldfoundry-eval run`。

运行环境是统一 profile `worldfoundry-unified-cu128`（Python 3.11，CUDA 12.8）。

记录的权重仓库包括 `Jinjing713/Spatia`、`Wan-AI/Wan2.2-TI2V-5B`、`facebook/map-anything`。就位权重用 `bash scripts/inference/prepare_model_infer.sh spatia`。

状态：**Runner 一致性待确认**。

<ModelRelatedRecipes modelId="spatia" locale="zh" />
