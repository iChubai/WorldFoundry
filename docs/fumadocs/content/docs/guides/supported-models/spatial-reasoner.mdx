---
title: "SpatialReasoner"
description: "SpatialReasoner is a vision-language model from the JHU CCVL group focused on explicit, step-wise spatial reasoning over images and video."
pageSource: generated
---

**Publisher:** Johns Hopkins University (CCVL) · University

SpatialReasoner is a vision-language model from the JHU CCVL group focused on explicit, step-wise spatial reasoning over images and video. In this catalog it is an understanding entry — spatial and multimodal reasoning plus image/video QA — returning model_response artifacts.

<ModelCommandBuilder modelId="spatial-reasoner" locale="en" />

## Architecture

A vision-language model with explicit 3D-aware spatial reasoning stages (per the upstream release at ccvl/SpatialReasoner). This repository records the prompt/image/video contract; scale and training details are not recorded here.

## Usage notes

Runner parity is pending. Pair it with generation entries to audit spatial consistency of generated worlds — e.g. object positions and relative depths.

<ModelRelatedRecipes modelId="spatial-reasoner" locale="en" />
