---
title: "Spirit-VLA v1.5"
description: "Spirit-VLA v1.5 from Spirit AI: vla action prediction and vla policy rollout; Runner parity pending; dedicated `worldfoundry-vla-spirit-v1-5`."
modelId: "spirit-v1.5"
pageSource: generated
---

Spirit-VLA v1.5 is Spirit AI's vision-language-action policy on a Qwen3-VL backbone, released as the confirmed Spirit-AI-robotics/Spirit-v1.5 base checkpoint. WorldFoundry includes the native inference architecture and preprocessing in-tree; the runtime loads the official checkpoint and Qwen3-VL backbone with remote Python disabled.

<ModelPaperFigures modelId="spirit-v1.5" locale="en" />

Code: [Spirit-AI-Team/spirit-v1.5](https://github.com/Spirit-AI-Team/spirit-v1.5).

<ModelCommandBuilder modelId="spirit-v1.5" locale="en" />

## What it is for

Pick `spirit-v1.5` when you want the catalog VLA policy: instruction and observation in, an action trace out. Use it to roll the recorded policy, not to train a new one.

A Qwen3-VL vision-language backbone with Spirit's action prediction head. Architecture internals beyond the official repository pointer are not recorded in this repository.

Catalog tasks include vla action prediction and vla policy rollout. Catalog aliases include `spirit-vla`, `spirit-v1p5`, and `spirit_v1p5`.

## Runtime contract

Required `image`, `prompt`, and `state`; optional `model_id` (spirit-v1.5), `policy_family`, and `action_representation`; artifacts `action_trace` and `spirit-v1.5_action_trace.json`.

Recorded task profiles: `vla.action_prediction` and `vla.policy_rollout`. WorldFoundry binds `SpiritV15Pipeline`.

Family status: **Runner parity pending**.

Import and checkpoint gating are validated; numerical parity requires staging the 21.6 GB checkpoint locally.

Native inference architecture and preprocessing are included in tree.

## Running in WorldFoundry

Launch `worldfoundry-eval run spirit-v1.5 --pipeline.task-profile vla.action_prediction` to execute the policy. The command builder above switches `--pipeline.task-profile` among recorded tasks and still emits `worldfoundry-eval run`.

It runs in the dedicated environment `worldfoundry-vla-spirit-v1-5` (Python 3.11, CUDA 12.8, torch>=2.8).

Recorded weight repositories include `Spirit-AI-robotics/Spirit-v1.5` and `Qwen/Qwen3-VL-4B-Instruct`. Stage assets with `bash scripts/inference/prepare_model_infer.sh spirit-v1.5`.

Status: **Runner parity pending**.

<ModelRelatedRecipes modelId="spirit-v1.5" locale="en" />
