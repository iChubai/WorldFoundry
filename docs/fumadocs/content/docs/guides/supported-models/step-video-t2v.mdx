---
title: "Step-Video-T2V"
description: "Step-Video-T2V from StepFun: text-to-video with a verified 204-frame GPU run."
modelId: "step-video-t2v"
pageSource: generated
---

Step-Video-T2V is StepFun's large open text-to-video model (30B parameters upstream, MIT weights at stepfun-ai/stepvideo-t2v). In WorldFoundry the public pipeline is assembled from native role-based components with the shared diffusion runner — DiT, Step-LLM text encoder, CLIP, scheduler, latent initialization, and VAE decoding all execute in-process.

<ModelPaperFigures modelId="step-video-t2v" locale="en" />

The recorded paper is [arXiv 2502.10248](https://arxiv.org/abs/2502.10248).

Code: [stepfun-ai/Step-Video-T2V](https://github.com/stepfun-ai/Step-Video-T2V).

<ModelCommandBuilder modelId="step-video-t2v" locale="en" />

## What it is for

Pick `step-video-t2v` when you want this family's text-to-video line in WorldFoundry: a prompt in, a generated clip out. Use it as the catalog T2V start rather than an editor or a world-model stepper.

Upstream: a 30B DiT with a deeply compressed Video-VAE (16x16 spatial, 8x temporal) and bilingual text encoders. In this repository the native pipeline runs in-process without the former xFuser and caption/VAE HTTP services. A checkpoint-backed 204-frame GPU artifact is verified for one prompt.

Catalog tasks include text-to-video and video-generation. Catalog aliases include `step-video`, `stepvideo-t2v`, and `step_video_t2v`. Documented modalities: text in, video out.

Recorded evaluation entry points include [VBench](/docs/evaluation/benchmark-hub/vbench) (Standard open T2V quality suite).

## Runtime contract

Required `prompt`; optional `image`; artifacts `generated_video` and `step_video_t2v.mp4`.

Recorded task profiles: `text-to-video` and `video-generation`. WorldFoundry binds `StepVideoT2VPipeline`.

Family status: **Checkpoint-backed 204-frame GPU inference verified for one prompt**.

The public pipeline generated 204/204 decodable frames at 992×544, 25 FPS, with 50 steps, CFG 9, and time shift 13 on one H100. This confirms the recorded configuration and output, not official pixel parity or a benchmark score. Use id `step-video-t2v`; plan for datacenter-class VRAM at 30B scale.

Use id step-video-t2v. The public pipeline uses the canonical native diffusion assembler.

## Running in WorldFoundry

The catalog launch is `worldfoundry-eval run step-video-t2v --pipeline.task-profile text-to-video`. The command builder above switches `--pipeline.task-profile` among recorded tasks and still emits `worldfoundry-eval run`.

It runs in the unified environment `worldfoundry-unified-cu128` (Python 3.11, CUDA 12.8).

Recorded weight repositories include `stepfun-ai/stepvideo-t2v`. Stage assets with `bash scripts/inference/prepare_model_infer.sh step-video-t2v`.

Recorded licenses include MIT; the verified GPU result covers one controlled prompt.

<ModelRelatedRecipes modelId="step-video-t2v" locale="en" />
