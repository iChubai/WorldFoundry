---
title: "ThinkSound"
description: "ThinkSound from Alibaba (Tongyi FunAudioLLM): video-to-audio and audio-generation; Planned."
modelId: "thinksound"
pageSource: generated
---

ThinkSound is Alibaba FunAudioLLM's video-to-audio generation model that uses chain-of-thought style reasoning to decide what a scene should sound like before synthesizing audio. In this catalog it is currently Planned: the entry is hidden from Studio until the infer-only runtime and the ThinkSound diffusion checkpoint are both available.

<ModelPaperFigures modelId="thinksound" locale="en" />

Code: [FunAudioLLM/ThinkSound](https://huggingface.co/FunAudioLLM/ThinkSound).

<ModelCommandBuilder modelId="thinksound" locale="en" />

## What it is for

Pick `thinksound` when you need catalog audio generation: the recorded inputs in, audio out. Use it instead of a video-only sibling.

Upstream describes a reasoning stage (audio-focused chain of thought) feeding a flow/ diffusion audio generator. This repository records only the prompt/video contract and the generated_audio artifact — internals are not recorded here. It is published by Alibaba (Tongyi FunAudioLLM).

Catalog tasks include video-to-audio and audio-generation. Catalog aliases include `FunAudioLLM/ThinkSound`. Documented modalities: video in, audio out.

## Runtime contract

Required `prompt`; optional `video`; artifacts `thinksound.wav`.

Recorded task profiles: `video-to-audio` and `audio-generation`. WorldFoundry binds `ThinkSoundPipeline`.

Family status: **Planned**.

Not runnable yet: checkpoint-backed parity is blocked by the missing thinksound.ckpt diffusion checkpoint. Watch the integration status before planning any runs; MMAudio is the runnable video-to-audio alternative in this catalog.

Hidden from Studio until the infer-only runtime and ThinkSound diffusion checkpoint are both available in-tree/Hugging Face cache.

## Running in WorldFoundry

The catalog launch is `worldfoundry-eval run thinksound --pipeline.task-profile video-to-audio`. The command builder above switches `--pipeline.task-profile` among recorded tasks and still emits `worldfoundry-eval run`.

It runs in the unified environment `worldfoundry-unified-cu128` (Python 3.11, CUDA 12.8).

Recorded weight repositories include `FunAudioLLM/ThinkSound`. Stage assets with `bash scripts/inference/prepare_model_infer.sh thinksound`.

Status: **Planned**.

<ModelRelatedRecipes modelId="thinksound" locale="en" />
