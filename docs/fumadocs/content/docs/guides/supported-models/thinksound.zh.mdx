---
title: "ThinkSound"
description: "ThinkSound is Alibaba FunAudioLLM's video-to-audio generation model that uses chain-of-thought style reasoning to decide what a scene should sound like before synthesizing audio."
pageSource: generated
---

**发表机构:** 阿里巴巴（通义 FunAudioLLM） · 企业
video → audio

ThinkSound 是阿里通义 FunAudioLLM 的视频配音模型，用思维链式推理先判断场景应有的声音、再合成音频。本 catalog 中它目前为 metadata_only：在仅推理运行时与 ThinkSound 扩散 checkpoint 都就位之前，条目在 Studio 中隐藏。

<ModelCommandBuilder modelId="thinksound" locale="zh" />

## 架构

上游描述为「推理阶段（面向音频的思维链）+ 流/扩散音频生成器」。本仓库仅记录 prompt/video 契约与 generated_audio 产物——内部细节仓库未记录。

## 使用要点

目前不可运行：checkpoint 加载一致性被缺失的 thinksound.ckpt 扩散 checkpoint 阻塞。计划运行前请先确认集成状态；catalog 中可运行的视频配音替代是 MMAudio。

<ModelRelatedRecipes modelId="thinksound" locale="zh" />
