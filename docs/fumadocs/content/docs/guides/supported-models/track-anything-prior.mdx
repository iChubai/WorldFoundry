---
title: "Segment and Track Anything"
description: "Segment and Track Anything from Zhejiang University (ReLER Lab): video-object-segmentation and object-tracking; Runner parity pending."
modelId: "track-anything-prior"
pageSource: generated
---

Segment and Track Anything (SAM-Track) combines SAM segmentation with AOT/DeAOT tracking so objects can be segmented once and tracked across a whole video, with GroundingDINO enabling text-prompted selection. In this catalog it is an independent tracking prior consumed through the shared geometry-prior contract.

<ModelPaperFigures modelId="track-anything-prior" locale="en" />

The recorded paper is [arXiv 2305.06558](https://arxiv.org/abs/2305.06558).

Code: [z-x-yang/Segment-and-Track-Anything](https://github.com/z-x-yang/Segment-and-Track-Anything).

<ModelCommandBuilder modelId="track-anything-prior" locale="en" />

## What it is for

Use `track-anything-prior` for catalog geometry inference. Choose it when the artifact is a reconstruction, not a generated video.

The recorded structure combines three subcomponents — SAM for masks, GroundingDINO for text grounding, and AOT/DeAOT for propagation — reusing canonical WorldFoundry owners where available. Registered at base_models/perception_core/tracking/track_anything; upstream license is AGPL-3.0. It is published by Zhejiang University (ReLER Lab).

Catalog tasks include video-object-segmentation, object-tracking, and geometry-prior. Catalog aliases include `segment-and-track-anything`, `track-anything`, and `aot-deaot-l`.

## Runtime contract

Required `prompt`, `image`, and `video`; optional `actions`; artifacts `track-anything-mask.npz`.

Recorded task profiles: `video-object-segmentation`, `object-tracking`, and `geometry-prior`. WorldFoundry binds `TrackAnythingPriorPipeline`.

Family status: **Runner parity pending**.

It is integrated as an independent geometry prior under worldfoundry/base_models and exposed through the shared geometry-prior pipeline/operator contract. Higher-level pipelines (e.g. ViPE) import it from this canonical owner instead of keeping private copies. Config resolution and pipeline/operator import are expected to pass; real prior inference remains gated by checkpoints/assets.

Integrated as an independent tracking prior at worldfoundry/base_models/perception_core/tracking/track_anything.

## Running in WorldFoundry

The catalog launch is `worldfoundry-eval run track-anything-prior --pipeline.task-profile video-object-segmentation`. The command builder above switches `--pipeline.task-profile` among recorded tasks and still emits `worldfoundry-eval run`.

It runs in the unified environment `worldfoundry-unified-cu128` (Python 3.11, CUDA 12.8).

Recorded weight repositories include `ShilongLiu/GroundingDINO`. Stage assets with `bash scripts/inference/prepare_model_infer.sh track-anything-prior`.

Status: **Runner parity pending**.

<ModelRelatedRecipes modelId="track-anything-prior" locale="en" />
