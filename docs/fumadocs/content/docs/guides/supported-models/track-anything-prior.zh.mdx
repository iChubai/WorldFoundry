---
title: "Segment and Track Anything"
description: "Segment and Track Anything（Zhejiang University (ReLER Lab)）：object-tracking、几何先验；Runner 一致性待确认。"
modelId: "track-anything-prior"
pageSource: generated
---

Segment and Track Anything（SAM-Track）把 SAM 分割与 AOT/DeAOT 跟踪结合起来：对象只需分割一次即可在整段视频中被跟踪，GroundingDINO 支持文本提示选择。本 catalog 中它是经共享 geometry-prior 契约调用的独立跟踪先验。

<ModelPaperFigures modelId="track-anything-prior" locale="zh" />

记录论文为[arXiv 2305.06558](https://arxiv.org/abs/2305.06558)。

代码：[z-x-yang/Segment-and-Track-Anything](https://github.com/z-x-yang/Segment-and-Track-Anything)。

<ModelCommandBuilder modelId="track-anything-prior" locale="zh" />

## 用来做什么

用 `track-anything-prior` 做 catalog 几何推理。产物是重建而不是生成视频时选它。

记录的结构组合三个子组件——SAM 出掩码、GroundingDINO 做文本定位、AOT/DeAOT 做传播——并在可用时复用 WorldFoundry 的规范组件。注册于 base_models/perception_core/tracking/track_anything；上游许可为 AGPL-3.0。该模型由浙江大学（ReLER 实验室）发布。

catalog 记录的任务包括 video-object-segmentation、object-tracking、几何先验。别名包括 `segment-and-track-anything`、`track-anything`、`aot-deaot-l`。

## 运行时契约

必填 `prompt`、`image`、`video`；可选 `actions`；产物 `track-anything-mask.npz`。

记录的任务 profile：`video-object-segmentation`、`object-tracking`、`geometry-prior`。绑定的 pipeline 类是 `TrackAnythingPriorPipeline`。

系列状态：**Runner 一致性待确认**。

它作为独立几何先验集成在 worldfoundry/base_models 下，通过共享的 geometry-prior pipeline/operator 契约对外暴露。 上层管线（如 ViPE）从这一规范位置导入它，而不是各自保留私有副本。 配置解析与 pipeline/operator 导入预期可通过；真实先验推理仍受 checkpoint/资产限制。

AGPL-3.0 许可：下游适用 copyleft 条款。

## 在 WorldFoundry 中运行

catalog 启动命令是 `worldfoundry-eval run track-anything-prior --pipeline.task-profile video-object-segmentation`。上方命令构建器按已记录任务切换 `--pipeline.task-profile`，发出的仍是 `worldfoundry-eval run`。

运行环境是统一 profile `worldfoundry-unified-cu128`（Python 3.11，CUDA 12.8）。

记录的权重仓库包括 `ShilongLiu/GroundingDINO`。就位权重用 `bash scripts/inference/prepare_model_infer.sh track-anything-prior`。

状态：**Runner 一致性待确认**。

<ModelRelatedRecipes modelId="track-anything-prior" locale="zh" />
