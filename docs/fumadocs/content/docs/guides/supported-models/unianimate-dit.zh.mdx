---
title: "UniAnimate-DiT"
description: "UniAnimate-DiT（Alibaba (Tongyi Lab)）：图生视频、视频生成；Runner 一致性待确认。"
modelId: "unianimate-dit"
pageSource: generated
---

UniAnimate-DiT 是阿里 ali-vilab 基于 Wan2.1-I2V 的人物图像动画模型：让人物照片按驱动姿态序列动起来。树内路径已配好本地 checkpoint 资产，暴露 max_frames、分辨率、步数与 seed 控制。

<ModelPaperFigures modelId="unianimate-dit" locale="zh" />

记录论文为[arXiv 2504.11289](https://arxiv.org/abs/2504.11289)。

代码：[ali-vilab/unianimate-dit](https://github.com/ali-vilab/unianimate-dit)。

<ModelCommandBuilder modelId="unianimate-dit" locale="zh" />

## 用来做什么

用 `unianimate-dit` 跑本系列联合 T2V/I2V 推理路径。本页记录了这两种任务 profile。

构建在 Wan2.1 图生视频扩散 Transformer 之上，以姿态序列为条件驱动人物动画（checkpoint： ZheWang123/UniAnimate-DiT）。更深的条件机制细节仓库未记录。该模型由阿里巴巴（通义实验室）发布。

catalog 记录的任务包括 human-image-animation、图生视频、视频生成。模态上，它接受图像，输出视频。

## 运行时契约

必填 `prompt`、`image`；产物 `unianimate-dit.mp4`。

记录的任务 profile：`human-image-animation`、`image-to-video`、`video-generation`。绑定的 pipeline 类是 `UniAnimateDiTPipeline`。

系列状态：**Runner 一致性待确认**。

为获得最佳效果，请准备干净的人物照片与驱动姿态序列。

Checkpoint 属于外部资产——权重缺失时会在运行时报告，而不会自动下载。

## 在 WorldFoundry 中运行

catalog 启动命令是 `worldfoundry-eval run unianimate-dit --pipeline.task-profile human-image-animation`。上方命令构建器按已记录任务切换 `--pipeline.task-profile`，发出的仍是 `worldfoundry-eval run`。

运行环境是统一 profile `worldfoundry-unified-cu128`（Python 3.11，CUDA 12.8）。

记录的权重仓库包括 `ZheWang123/UniAnimate-DiT`。就位权重用 `bash scripts/inference/prepare_model_infer.sh unianimate-dit`。

状态：**Runner 一致性待确认**。

<ModelRelatedRecipes modelId="unianimate-dit" locale="zh" />
