---
title: "UniDepth V2"
description: "UniDepth V2 from ETH Zurich: monocular-depth-estimation and metric-depth-estimation; Verified."
modelId: "unidepth-v2-prior"
pageSource: generated
---

UniDepth V2 is a universal monocular metric-depth model: from a single image of any camera it predicts a metric 3D point cloud directly, without external intrinsics. In this catalog it is a checkpoint-verified geometry prior — real checkpoint inference and official point export are verified through the shared geometry-prior contract and Workspace route.

<ModelPaperFigures modelId="unidepth-v2-prior" locale="en" />

The recorded paper is [arXiv 2502.20110](https://arxiv.org/abs/2502.20110).

Project page: [lpiccinelli-eth.github.io/pub/unidepth](https://lpiccinelli-eth.github.io/pub/unidepth/). Code: [lpiccinelli-eth/UniDepth](https://github.com/lpiccinelli-eth/UniDepth).

<ModelCommandBuilder modelId="unidepth-v2-prior" locale="en" />

## What it is for

Use `unidepth-v2-prior` for catalog geometry inference. Choose it when the artifact is a reconstruction, not a generated video.

Per the upstream publication, a camera self-prompting module predicts a dense camera representation that conditions depth decoding, enabling cross-camera generalization. The default checkpoint resolves from lpiccinelli/unidepth-v2-vitl14 (or a local model_path override); registered at base_models/three_dimensions/depth/unidepth. It is published by ETH Zurich.

Catalog tasks include monocular-depth-estimation, metric-depth-estimation, and geometry-prior. Catalog aliases include `unidepth-v2`, `unidepth-v2-vitl14`, and `unidepth_v2_vitl14`. Documented modalities: image in, depth out.

## Runtime contract

Required `image`; optional `prompt`, `video`, and `actions`; artifacts `unidepth-v2-prior.npz`.

Recorded task profiles: `monocular-depth-estimation`, `metric-depth-estimation`, and `geometry-prior`. WorldFoundry binds `UniDepthV2PriorPipeline`.

Family status: **Verified**.

It is integrated as an independent geometry prior under worldfoundry/base_models and exposed through the shared geometry-prior pipeline/operator contract. Higher-level pipelines (e.g. ViPE) import it from this canonical owner instead of keeping private copies. Mind the license: the upstream release is CC-BY-NC-4.0, so respect non-commercial terms when enabling this prior.

Integrated as an independent geometric prior at worldfoundry/base_models/three_dimensions/depth/unidepth.

## Running in WorldFoundry

The catalog launch is `worldfoundry-eval run unidepth-v2-prior --pipeline.task-profile monocular-depth-estimation`. The command builder above switches `--pipeline.task-profile` among recorded tasks and still emits `worldfoundry-eval run`.

It runs in the unified environment `worldfoundry-unified-cu128` (Python 3.11, CUDA 12.8).

Recorded weight repositories include `lpiccinelli/unidepth-v2-vitl14`. Stage assets with `bash scripts/inference/prepare_model_infer.sh unidepth-v2-prior`.

Status: **Verified**.

<ModelRelatedRecipes modelId="unidepth-v2-prior" locale="en" />
