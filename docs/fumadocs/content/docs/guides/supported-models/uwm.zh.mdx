---
title: "Unified World Models"
description: "Unified World Models（University of Washington (WEIRD Lab)）：机器人世界模型、动作条件视频；Runner 一致性待确认。"
modelId: "uwm"
pageSource: generated
---

华盛顿大学 WEIRD 实验室的 Unified World Models（UWM）将视频预测与动作扩散耦合进同一个机器人世界模型，仓库记录了 LIBERO-90（uwm-libero90）与 DROID（uwm-droid）两个变体。WorldFoundry 树内注册了官方评测路由；checkpoint 与数据集为运行时门控。

<ModelPaperFigures modelId="uwm" locale="zh" />

记录论文为[arXiv 2504.02792](https://arxiv.org/abs/2504.02792)。

项目页：[weirdlabuw.github.io/uwm](https://weirdlabuw.github.io/uwm/)。代码：[WEIRDLabUW/unified-world-model](https://github.com/WEIRDLabUW/unified-world-model)。

<ModelCommandBuilder modelId="uwm" locale="zh" />

## 用来做什么

需要动作条件世界步进时选 `uwm`：记录动作与帧进、滚动生成出。用来做交互世界推理，不是把 prompt 倒进文生视频。

UWM 是视频-动作扩散设计：单个扩散过程联合建模未来观测与机器人动作，同一网络兼任预测与策略角色。官方仓库指针之外的细节仓库未记录。该模型由华盛顿大学（WEIRD 实验室）发布。

catalog 记录的任务包括 机器人世界模型、动作条件视频、video-action-diffusion。别名包括 `uwm-libero90`、`uwm-droid`。

记录的评测入口包括[LIBERO](/zh/docs/evaluation/benchmark-hub/libero)（uwm-libero90 变体面向 LIBERO 任务）、[EWMBench](/zh/docs/evaluation/benchmark-hub/ewmbench)（机器人世界模型滚动生成质量）。

## 运行时契约

必填 `image`、`prompt`、`video`；可选 `actions`；产物 `generated_video`、`uwm_runtime_plan.json`。

记录的任务 profile：`robot-world-model`、`action-conditioned-video`、`video-action-diffusion`。绑定的 pipeline 类是 `UWMPipeline`。

系列状态：**Runner 一致性待确认**。

配置解析与 pipeline 导入预期可通过；在所需 checkpoint/资产就位前，真实生成会输出运行时计划。 缺失资产由计划报告，不视为 catalog 阻塞。 请按评测套件选择 LIBERO-90 或 DROID 变体。

WorldFoundry tracks upstream source and checkpoint 计划中; no runnable pipeline is registered.

## 在 WorldFoundry 中运行

catalog 启动命令是 `worldfoundry-eval run uwm --pipeline.task-profile robot-world-model`。上方命令构建器按已记录任务切换 `--pipeline.task-profile`，发出的仍是 `worldfoundry-eval run`。

运行环境是统一 profile `worldfoundry-unified-cu128`（Python 3.11，CUDA 12.8）。

本卡片未记录公开 checkpoint id；按使用说明就位权重。就位权重用 `bash scripts/inference/prepare_model_infer.sh uwm`。

状态：**Runner 一致性待确认**。

<ModelRelatedRecipes modelId="uwm" locale="zh" />
