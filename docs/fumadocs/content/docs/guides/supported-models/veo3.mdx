---
title: "Veo3"
description: "Veo3 from Google DeepMind: text-to-video and image-to-video; Runner parity pending."
modelId: "veo3"
pageSource: generated
---

Veo3 is the catalog entry for Google DeepMind's Veo 3 hosted video model, recorded for text-to-video and image-to-video. WorldFoundry submits generation requests through Google's API and collects the rendered clips as artifacts, so Veo 3 results can be lined up against open models in the same evaluation harness. The model itself never leaves Google's infrastructure.

<ModelPaperFigures modelId="veo3" locale="en" />

Project page: [deepmind.google/technologies/veo](https://deepmind.google/technologies/veo/).

<ModelCommandBuilder modelId="veo3" locale="en" />

## What it is for

Pick `veo3` when this card is a hosted API rather than a local checkpoint: call the recorded provider route instead of staging weights.

Catalog tasks include text-to-video, image-to-video, and hosted-api. Documented modalities: text and image in, video out.

Recorded evaluation entry points include [VBench](/docs/evaluation/benchmark-hub/vbench) (Standard multi-dimension quality suite for comparing hosted text/image-to-video outputs), [Physics-IQ Original](/docs/evaluation/benchmark-hub/physics-iq) (Probes whether provider-side video models respect basic physical plausibility).

## Runtime contract

Inputs, knobs, and artifacts are not recorded on this card.

Recorded task profiles: `text-to-video`, `image-to-video`, and `hosted-api`. WorldFoundry binds `Veo3Pipeline`.

Family status: **Runner parity pending**.

The catalog marks this entry as integrated: the hosted-API Studio pipeline is available, but you must supply your own provider API key and account. No weights are downloaded and no GPU environment is provisioned locally — generation happens on the provider side, so cost, quota, and content policy follow your provider account. Veo access is provisioned through Google's AI developer offerings (e.g. the Gemini API); regional availability applies.

Hosted API Studio pipeline is available; users must provide their own provider API key.

## Running in WorldFoundry

The catalog launch is `worldfoundry-eval run veo3 --pipeline.task-profile text-to-video`. The command builder above switches `--pipeline.task-profile` among recorded tasks and still emits `worldfoundry-eval run`.

The environment profile is not recorded.

Stage assets with `bash scripts/inference/prepare_model_infer.sh veo3`.

Status: **Runner parity pending**.

<ModelRelatedRecipes modelId="veo3" locale="en" />
