---
title: "VerseCrafter"
description: "VerseCrafter（Tencent ARC Lab）：相机可控视频、图生视频；Runner 一致性待确认。"
modelId: "versecrafter"
pageSource: generated
---

腾讯 ARC 的 VerseCrafter 是基于 4D 控制的相机可控图生视频世界模型：几何感知渲染阶段产出 4D 控制信号，引导扩散阶段沿指定相机路径生成。WorldFoundry 将官方源码（revision 008693b5）与精确的 VideoX-Fun 子模块（revision 246a0e3a）内置树内，checkpoint 引用为 sxzheng/VerseCrafter。

<ModelPaperFigures modelId="versecrafter" locale="zh" />

记录论文为[arXiv 2601.05138](https://arxiv.org/abs/2601.05138)。

项目页：[sixiaozheng.github.io/VerseCrafter_page](https://sixiaozheng.github.io/VerseCrafter_page/)。代码：[TencentARC/VerseCrafter](https://github.com/TencentARC/VerseCrafter)。

<ModelCommandBuilder modelId="versecrafter" locale="zh" />

## 用来做什么

用 `versecrafter` 做相机条件视频推理。配方绑定了已记录的相机控制 profile。

树内接通两个阶段：仅相机的 4D 控制渲染，随后是扩散阶段。扩散骨干来自锁定版本的 VideoX-Fun 子模块。更多架构内部细节仓库未记录。该模型由腾讯 ARC 实验室发布。

catalog 记录的任务包括 相机可控视频、图生视频、世界模型。模态上，它接受图像，输出视频。

记录的评测入口包括[CameraBench](/zh/docs/evaluation/benchmark-hub/camerabench)（4D 控制生成的相机路径跟随）。

## 运行时契约

必填 `prompt`、`image`、`trajectory_npz`；产物 `versecrafter.mp4`。

记录的任务 profile：`camera-controlled-video`、`image-to-video`、`world-model`。绑定的 pipeline 类是 `VerseCrafterPipeline`。

系列状态：**Runner 一致性待确认**。

部署前务必注意许可：上游为仅研究、非商用，且排除面向欧盟的使用。 runner parity 仍为 pending，在记录 parity 运行前请将输出视为未验证。

仅研究、非商用许可，且排除面向欧盟的使用。

## 在 WorldFoundry 中运行

启动 `worldfoundry-eval run versecrafter --pipeline.task-profile camera-controlled-video` 跟随相机路径。上方命令构建器按已记录任务切换 `--pipeline.task-profile`，发出的仍是 `worldfoundry-eval run`。

运行环境是统一 profile `worldfoundry-unified-cu128`（Python 3.11，CUDA 12.8）。

记录的权重仓库包括 `TencentARC/VerseCrafter`、`Wan-AI/Wan2.1-T2V-14B`、`Ruicheng/moge-2-vitl-normal`。就位权重用 `bash scripts/inference/prepare_model_infer.sh versecrafter`。

状态：**Runner 一致性待确认**。

<ModelRelatedRecipes modelId="versecrafter" locale="zh" />
