---
title: "Vid2World"
description: "Vid2World（Tsinghua University (THUML)）：交互式世界模型、动作条件视频；Runner 一致性待确认。"
modelId: "vid2world"
pageSource: generated
---

清华大学 THUML 组的 Vid2World 将预训练视频扩散模型改造为交互式世界模型：把被动的视频生成器适配成动作条件的自回归预测器。WorldFoundry 树内注册了官方评测路由；checkpoint 与配置资产为运行时门控。

<ModelPaperFigures modelId="vid2world" locale="zh" />

记录论文为[arXiv 2505.14357](https://arxiv.org/abs/2505.14357)。

项目页：[knightnemo.github.io/vid2world](https://knightnemo.github.io/vid2world/)。代码：[thuml/Vid2World](https://github.com/thuml/Vid2World)。

<ModelCommandBuilder modelId="vid2world" locale="zh" />

## 用来做什么

`vid2world` 是记录的交互世界线。要用动作推进世界时从这里起步。

仓库记录的核心思想是视频扩散适配：将全序列扩散模型改造成因果、动作条件的滚动生成。官方仓库指针（thuml/Vid2World，锁定 head ae24077a）之外的架构内部细节仓库未记录。该模型由清华大学（THUML 机器学习组）发布。

catalog 记录的任务包括 交互式世界模型、video-diffusion-adaptation、动作条件视频。

记录的评测入口包括[WorldModelBench](/zh/docs/evaluation/benchmark-hub/worldmodelbench)（动作条件交互滚动生成质量）。

## 运行时契约

必填 `image`、`prompt`、`video`；可选 `actions` (action)；产物 `generated_video`、`vid2world_runtime_plan.json`。

记录的任务 profile：`interactive-world-model`、`video-diffusion-adaptation`、`action-conditioned-video`。绑定的 pipeline 类是 `Vid2WorldPipeline`。

系列状态：**Runner 一致性待确认**。

配置解析与 pipeline 导入预期可通过；在所需 checkpoint/资产就位前，真实生成会输出运行时计划。 缺失资产由计划报告，不视为 catalog 阻塞。

WorldFoundry tracks upstream source and checkpoint 计划中; no runnable pipeline is registered.

## 在 WorldFoundry 中运行

catalog 启动命令是 `worldfoundry-eval run vid2world --pipeline.task-profile interactive-world-model`。上方命令构建器按已记录任务切换 `--pipeline.task-profile`，发出的仍是 `worldfoundry-eval run`。

运行环境是统一 profile `worldfoundry-unified-cu128`（Python 3.11，CUDA 12.8）。

本卡片未记录公开 checkpoint id；按使用说明就位权重。就位权重用 `bash scripts/inference/prepare_model_infer.sh vid2world`。

状态：**Runner 一致性待确认**。

<ModelRelatedRecipes modelId="vid2world" locale="zh" />
