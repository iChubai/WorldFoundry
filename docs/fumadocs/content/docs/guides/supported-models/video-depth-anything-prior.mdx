---
title: "Video Depth Anything"
description: "Video Depth Anything from ByteDance: video-depth-estimation and temporal-depth-estimation; Runner parity pending."
modelId: "video-depth-anything-prior"
pageSource: generated
---

Video Depth Anything brings temporal consistency to the Depth Anything family: it estimates depth over arbitrarily long videos without the flicker of frame-by-frame estimation. In this catalog it is an independent temporal depth prior that VIPE pipeline profiles can select through shared runtime config without owning the code.

<ModelPaperFigures modelId="video-depth-anything-prior" locale="en" />

The recorded paper is [arXiv 2501.12375](https://arxiv.org/abs/2501.12375).

Project page: [videodepthanything.github.io](https://videodepthanything.github.io/). Code: [DepthAnything/Video-Depth-Anything](https://github.com/DepthAnything/Video-Depth-Anything).

<ModelCommandBuilder modelId="video-depth-anything-prior" locale="en" />

## What it is for

Pick `video-depth-anything-prior` for feed-forward geometry: views in, reconstruction / trajectory / point cloud out. Use it when you need the catalog 3D route rather than a depth-only prior.

Per the upstream release, a spatiotemporal head with a temporal-gradient consistency objective extends Depth Anything V2 to video, plus a keyframe strategy for long sequences. The recorded checkpoint is depth-anything/Video-Depth-Anything-Large (apache-2.0); registered at base_models/three_dimensions/depth/videodepthanything. It is published by ByteDance.

Catalog tasks include video-depth-estimation, temporal-depth-estimation, and geometry-prior. Catalog aliases include `video-depth-anything` and `vda-prior`. Documented modalities: image in, depth out.

## Runtime contract

Required `video`; optional `prompt`, `image`, and `actions`; artifacts `video-depth-anything-prior.npz`.

Recorded task profiles: `video-depth-estimation`, `temporal-depth-estimation`, and `geometry-prior`. WorldFoundry binds `VideoDepthAnythingPriorPipeline`.

Family status: **Runner parity pending**.

It is integrated as an independent geometry prior under worldfoundry/base_models and exposed through the shared geometry-prior pipeline/operator contract. Higher-level pipelines (e.g. ViPE) import it from this canonical owner instead of keeping private copies. Config resolution and pipeline/operator import are expected to pass; real prior inference remains checkpoint-gated.

Integrated as an independent temporal depth prior at worldfoundry/base_models/three_dimensions/depth/videodepthanything.

## Running in WorldFoundry

The catalog launch is `worldfoundry-eval run video-depth-anything-prior --pipeline.task-profile video-depth-estimation`. The command builder above switches `--pipeline.task-profile` among recorded tasks and still emits `worldfoundry-eval run`.

It runs in the unified environment `worldfoundry-unified-cu128` (Python 3.11, CUDA 12.8).

Recorded weight repositories include `depth-anything/Video-Depth-Anything-Large`. Stage assets with `bash scripts/inference/prepare_model_infer.sh video-depth-anything-prior`.

Status: **Runner parity pending**.

<ModelRelatedRecipes modelId="video-depth-anything-prior" locale="en" />
