---
title: "VLANeXt"
description: "VLANeXt from Nanyang Technological University (S-Lab): vla and vla action prediction; Static load only."
modelId: "vlanext"
pageSource: generated
---

VLANeXt is a next-generation VLA released under the S-Lab License 1.0 (source-available, research-oriented), with official LIBERO and LIBERO-plus checkpoints in the DravenALG/VLANeXt collection. WorldFoundry calls the official get_vla/get_processor/get_vla_action contract in-process from a vendored inference subset.

<ModelPaperFigures modelId="vlanext" locale="en" />

The recorded paper is [arXiv 2602.18532](https://arxiv.org/abs/2602.18532).

Project page: [dravenalg.github.io/VLANeXt](https://dravenalg.github.io/VLANeXt). Code: [DravenALG/VLANeXt](https://github.com/DravenALG/VLANeXt).

<ModelCommandBuilder modelId="vlanext" locale="en" />

## What it is for

Use `vlanext-libero` for vision-language-action inference on this card. The recipe exists so you can launch the recorded policy profile.

Public checkpoints cover the LIBERO suites (VLANeXt_libero_spatial.pt, _object.pt and more). Architecture internals beyond the official repository pointer are not recorded in this repository. It is published by Nanyang Technological University (S-Lab).

Catalog tasks include vla, vla action prediction, and vla policy rollout. Catalog aliases include `vlanext-libero` and `vla-next`.

Recorded evaluation entry points include [LIBERO](/docs/evaluation/benchmark-hub/libero) (Official LIBERO checkpoint collection), [LIBERO-Plus](/docs/evaluation/benchmark-hub/libero-plus) (LIBERO-plus contracts are recorded).

## Runtime contract

Required `image`, `video`, `prompt`, and `state`; optional `model_id` (vlanext), `camera_keys` (full_image, full_image_wrist), `history_keys` (image_history, image_history_wrist, state_history, action_history), `policy_family`, and `action_representation`; artifacts `action_trace` and `vlanext_action_trace.json`.

Recorded task profiles: `vla`, `vla.action_prediction`, and `vla.policy_rollout`. WorldFoundry binds `VLANeXtPipeline`.

Family status: **Static load only**.

Resolver, runtime import, and compileall checks pass without an external checkout; checkpoint-backed action-chunk validation remains required before demo parity is claimed. Note the S-Lab-1.0 license restricts commercial use.

WorldFoundry records VLANeXt LIBERO/LIBERO-plus action inference contracts with an independent synthesis entrypoint.

## Running in WorldFoundry

Launch `worldfoundry-eval run vlanext-libero --pipeline.task-profile vla.policy_rollout` to execute the policy. The command builder above switches `--pipeline.task-profile` among recorded tasks and still emits `worldfoundry-eval run`.

It runs in the unified environment `worldfoundry-unified-cu128` (Python 3.10, CUDA 12.8, torch==2.4.0).

Recorded weight repositories include `DravenALG/VLANeXt`. Stage assets with `bash scripts/inference/prepare_model_infer.sh vlanext-libero`.

Recorded licenses include s-lab-1.0; status: **Static load only**.

<ModelRelatedRecipes modelId="vlanext" locale="en" />
