---
title: "Wall-OSS"
description: "Wall-OSS is X Square Robot's open-source embodied foundation policy from the Wall-X line, with two confirmed checkpoints: wall-oss-flow (flow-matching action decoding) and wall-oss-fast (FAST-token decoding). WorldFoundry routes it through…"
pageSource: generated
---

**发表机构:** 自变量机器人（X Square Robot） · 企业

Wall-OSS 是自变量机器人（X Square Robot）Wall-X 线的开源具身基础策略，确认两个 checkpoint：wall-oss-flow（flow-matching 动作解码）与 wall-oss-fast（FAST token 解码）。WorldFoundry 针对具体 Wall-X 策略经树内动作运行时适配器路由；checkpoint 驱动 parity 仍受资产门控。

<ModelCommandBuilder modelId="wall-oss" locale="zh" />

## 架构

同一基础策略的两种解码变体：flow-matching 头（wall-oss-flow）与 FAST 离散 token 头（wall-oss-fast）。官方仓库指针之外的架构内部细节仓库未记录。

## 使用要点

checkpoint 缺失由运行时 missing_assets 报告，不视为 catalog 阻塞；权重就位前运行输出运行时计划。平滑连续控制选 flow，低延迟解码选 fast。

<ModelRelatedRecipes modelId="wall-oss" locale="zh" />
