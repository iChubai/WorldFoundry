---
title: "WonderJourney"
description: "WonderJourney（Stanford University / Google）：3D 世界生成、scene-expansion；Runner 一致性待确认; 独立环境 `wonderworld`。"
modelId: "wonderjourney"
pageSource: generated
---

WonderJourney 生成一段「旅程」：从文本或一张图出发，产出一串相互衔接、可穿行的 3D 场景——是持续的 3D 场景生成而非单个片段。本 catalog 中 WorldFoundry 通过树内适配器运行上游 run.py/example_config 流程；runner parity 待运行时资产就位。

<ModelPaperFigures modelId="wonderjourney" locale="zh" />

记录论文为[WonderJourney: Going from Anywhere to Everywhere](https://arxiv.org/abs/2312.03884)（2023）。

项目页：[kovenyu.com/WonderJourney](https://kovenyu.com/WonderJourney/)。代码：[KovenYu/WonderJourney](https://github.com/KovenYu/WonderJourney)。

<ModelCommandBuilder modelId="wonderjourney" locale="zh" />

## 用来做什么

`wonderjourney` 是记录的交互世界线。要用动作推进世界时从这里起步。

按论文（arXiv 2312.03884），LLM 提议后续场景描述，点云加图像修复的视觉模块从上一场景渲染新场景，VLM 负责校验结果。资产或运行时依赖缺失会在推理时报告。该模型由斯坦福大学 / Google发布。

catalog 记录的任务包括 3D 世界生成、scene-expansion、image-to-3d-world。模态上，它接受图像，输出3D scene、世界状态。

记录的评测入口包括[WorldScore](/zh/docs/evaluation/benchmark-hub/worldscore)（生成可游走场景序列的标准评分）。

## 运行时契约

必填 `prompt`、`image`；可选 `video`、`actions`；产物 `generated_world`、`wonderjourney.mp4`。

记录的任务 profile：`3d-world-generation`、`scene-expansion`、`image-to-3d-world`。绑定的 pipeline 类是 `WonderJourneyPipeline`。

系列状态：**Runner 一致性待确认**。

pipeline 导入与 plan-only 推理已接通；完整产物一致性需要本地就位上游资产（深度、修复、LLM 访问）之后才能生成真实旅程。

runner parity 待运行时资产就位。

## 在 WorldFoundry 中运行

catalog 启动命令是 `worldfoundry-eval run wonderjourney --pipeline.task-profile 3d-world-generation`。上方命令构建器按已记录任务切换 `--pipeline.task-profile`，发出的仍是 `worldfoundry-eval run`。

运行环境是独立 profile `wonderworld`（Python 3.10，CUDA 12.4）。

本卡片未记录公开 checkpoint id；按使用说明就位权重。就位权重用 `bash scripts/inference/prepare_model_infer.sh wonderjourney`。

状态：**Runner 一致性待确认**。

<ModelRelatedRecipes modelId="wonderjourney" locale="zh" />
