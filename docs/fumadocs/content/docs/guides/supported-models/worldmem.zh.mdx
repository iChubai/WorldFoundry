---
title: "WorldMem"
description: "WorldMem：交互式世界模型、Minecraft 世界模型；Runner 一致性待确认。"
modelId: "worldmem"
pageSource: generated
---

WorldMem 针对交互式世界模型的核心弱点——遗忘：为 Minecraft 式交互世界模型加装显式记忆，使重访地点在长时程下保持一致（论文：arXiv 2504.12369）。WorldFoundry 树内注册了路由；上游推理模型与数据资产为运行时门控。

<ModelPaperFigures modelId="worldmem" locale="zh" />

记录论文为[WorldMem: Long-term Consistent World Simulation with Memory](https://arxiv.org/abs/2504.12369)（2025）。

项目页：[worldmem.github.io](https://worldmem.github.io/)。代码：[xizaoqu/WorldMem](https://github.com/xizaoqu/WorldMem)。

<ModelCommandBuilder modelId="worldmem" locale="zh" />

## 用来做什么

`worldmem` 是记录的交互世界线。要用动作推进世界时从这里起步。

仓库记录的设计主题是基于记忆的长期一致性：存储并检索历史观测以条件化后续滚动生成。官方仓库与论文指针之外的架构内部细节仓库未记录。

catalog 记录的任务包括 交互式世界模型、Minecraft 世界模型、长期一致性。

记录的评测入口包括[MemoBench](/zh/docs/evaluation/benchmark-hub/memobench)（世界滚动生成的长期记忆一致性）、[WorldModelBench](/zh/docs/evaluation/benchmark-hub/worldmodelbench)（动作条件交互滚动生成质量）。

## 运行时契约

必填 `image`、`prompt`、`video`；可选 `actions` (action)；产物 `generated_video`、`worldmem_runtime_plan.json`。

记录的任务 profile：`interactive-world-model`、`minecraft-world-model`、`long-term-consistency`。绑定的 pipeline 类是 `WorldMemPipeline`。

系列状态：**Runner 一致性待确认**。

配置解析与 pipeline 导入预期可通过；在所需 checkpoint/资产就位前，真实生成会输出运行时计划。 缺失资产由计划报告，不视为 catalog 阻塞。

WorldFoundry tracks WorldMem source 计划中; no runnable pipeline is registered.

## 在 WorldFoundry 中运行

catalog 启动命令是 `worldfoundry-eval run worldmem --pipeline.task-profile interactive-world-model`。上方命令构建器按已记录任务切换 `--pipeline.task-profile`，发出的仍是 `worldfoundry-eval run`。

运行环境是统一 profile `worldfoundry-unified-cu128`（Python 3.11，CUDA 12.8）。

本卡片未记录公开 checkpoint id；按使用说明就位权重。就位权重用 `bash scripts/inference/prepare_model_infer.sh worldmem`。

状态：**Runner 一致性待确认**。

<ModelRelatedRecipes modelId="worldmem" locale="zh" />
