---
title: "X-WAM"
description: "X-WAM is a GPU-validated world-action model: one Wan 2.2 joint flow process predicts future RGB/depth, proprioception, and robot actions together. The 38.9 GB RoboCasa-SFT checkpoint restored strictly and passed both the official 10-step a…"
pageSource: generated
---

**Publisher:** Open-source research release (institution not recorded) · Research lab
image → depth

X-WAM is a GPU-validated world-action model: one Wan 2.2 joint flow process predicts future RGB/depth, proprioception, and robot actions together. The 38.9 GB RoboCasa-SFT checkpoint restored strictly and passed both the official 10-step action/proprioception path and a full 50-step future-RGB decode on an A100 (Apache-2.0).

<ModelCommandBuilder modelId="x-wam" locale="en" />

## Architecture

Built on Wan2.2-TI2V-5B: a joint flow model over visual futures and action streams. The official deployed policy uses asynchronous early stopping after 10 action steps and returns a 32-step action chunk; optional full world decoding continues to 50 steps. Both Hub repos are revision-pinned.

## Usage notes

Stage the X-WAM checkpoint states plus the Wan2.2-TI2V-5B base. The runtime is self-contained in-tree and never imports an external X-WAM checkout; training, simulators, ZeroMQ services, Lightning, and DeepSpeed orchestration are excluded.

## Variants & pipeline bindings

| Variant | Task | Runtime profile | Pipeline binding | Status |
| --- | --- | --- | --- | --- |
| `x-wam-robocasa-sft` | wam.world_action_modeling | `x-wam` | `x-wam` | in tree runtime selected checkpoint gpu validated |
| `x-wam-robotwin-sft` | wam.world_action_modeling | `x-wam` | `x-wam` | in tree runtime selected checkpoint gpu validated |
| `x-wam-pretrained` | wam.post_training_base | `x-wam` | `x-wam` | in tree runtime selected checkpoint gpu validated |

## Evaluation benchmarks

- [RoboCasa](/docs/evaluation/benchmark-hub/robocasa) — The GPU-validated checkpoint is RoboCasa-SFT.
- [EWMBench](/docs/evaluation/benchmark-hub/ewmbench) — Joint world-action prediction quality.
- [RoboTwin 2.0](/docs/evaluation/benchmark-hub/robotwin) — Referenced in this model's manifest evidence notes.

<ModelRelatedRecipes modelId="x-wam" locale="en" />
