---
title: Core infrastructure
description: The worldfoundry/core shared layer — loading, attention, distributed, VRAM, kernels, memory, and the rule that reusable primitives live here, not in family packages.
---

## Why this layer exists

`worldfoundry/core` is the shared inference infrastructure that every model family reuses and none of them owns. Checkpoint loading, state-dict remapping, attention backends, distributed collectives, VRAM placement and offload, kernels, configuration, media IO, memory contracts, and safety guardrails all live here once. A family package (`worldfoundry/base_models/*`, `worldfoundry/synthesis/*`, `worldfoundry/pipelines/*`) composes these primitives; it must not recreate them.

This is the layer to read when you are about to copy a utility into a model directory. The rule is explicit: if a capability can serve more than one family, it belongs in `core` first. See [Native diffusion](/docs/maintainers/architecture/native-diffusion) for how diffusion consumes this layer, and [Model runtime](/docs/maintainers/architecture/model-runtime) for how pipelines and operators do.

## Subpackage responsibilities

<div className="pi-kv-catalog not-prose">
  <section className="pi-kv-group">
    <h3>Subpackage</h3>
    <ul>
      <li className="pi-kv-row">
        <div className="pi-kv-head">
          <div className="pi-kv-names">
            <code>model_loading</code>
          </div>
        </div>
        <p>Checkpoint materialization, state-dict loading, LoRA merge, inference network construction.</p>
        <p className="pi-kv-read"><span>First file</span> <code>load_state_dict_from_safetensors</code> <code>model_loading/file.py:110</code>; <code>instantiate_inference_network</code> <code>inference_model.py:23</code>; <code>GeneralLoRAoader</code> <code>lora.py:15</code></p>
      </li>
      <li className="pi-kv-row">
        <div className="pi-kv-head">
          <div className="pi-kv-names">
            <code>checkpoint</code>
          </div>
        </div>
        <p>State-dict assignment, remap, distributed checkpoint (DCP), safe loading.</p>
        <p className="pi-kv-read"><span>First file</span> <code>{"checkpoint/{assignment,remap,dcp,safe_loading,load}.py"}</code></p>
      </li>
      <li className="pi-kv-row">
        <div className="pi-kv-head">
          <div className="pi-kv-names">
            <code>vram</code>
          </div>
        </div>
        <p>Disk-backed state maps, meta initialization, staged placement, layerwise CPU offload, memory accounting.</p>
        <p className="pi-kv-read"><span>First file</span> <code>{"vram/{disk_map,initialization,layers,layerwise_offload,memory}.py"}</code></p>
      </li>
      <li className="pi-kv-row">
        <div className="pi-kv-head">
          <div className="pi-kv-names">
            <code>attention</code>
          </div>
        </div>
        <p>Backend probing, dense SDPA, packed variable-length, sparse 3D, n-D/complex/projective RoPE, sequence-parallel composition. Package probes walk module search paths without initializing optional extensions or their parent packages.</p>
        <p className="pi-kv-read"><span>First file</span> <code>AttentionKernelCapability</code> <code>attention/backends.py:32</code>; <code>probe_attention_backends</code> <code>backends.py:144</code>; <code>complex_rope.py</code>, <code>causal_ulysses_attention.py</code></p>
      </li>
      <li className="pi-kv-row">
        <div className="pi-kv-head">
          <div className="pi-kv-names">
            <code>distributed</code>
          </div>
        </div>
        <p>NCCL options, masked orthogonal rank groups, model-parallel init, context/block FSDP, device-mesh collectives.</p>
        <p className="pi-kv-read"><span>First file</span> <code>initialize_model_parallel</code> <code>distributed/model_parallel_groups.py:238</code>; <code>RankGenerator</code> <code>:194</code>; <code>block_fsdp.py</code>, <code>context_parallel.py</code></p>
      </li>
      <li className="pi-kv-row">
        <div className="pi-kv-head">
          <div className="pi-kv-names">
            <code>nn</code>
          </div>
        </div>
        <p>Reusable transformer math: RMSNorm, shared MLPs, timestep/AdaLN/final-layer modulation, prefix gating, activation checkpointing, classic-diffusion blocks.</p>
        <p className="pi-kv-read"><span>First file</span> <code>{"nn/{activation_checkpointing,diffusion_schedulers,convolutional_mlp,...}.py"}</code></p>
      </li>
      <li className="pi-kv-row">
        <div className="pi-kv-head">
          <div className="pi-kv-names">
            <code>kernels</code>
          </div>
        </div>
        <p>Autotune cache, capability probing, diffusion kernels, MoE, quantized GEMM.</p>
        <p className="pi-kv-read"><span>First file</span> <code>{"kernels/{capabilities,autotune_cache,diffusion,moe,quantized_gemm}.py"}</code></p>
      </li>
      <li className="pi-kv-row">
        <div className="pi-kv-head">
          <div className="pi-kv-names">
            <code>memory</code>
          </div>
        </div>
        <p>Multi-modal, streaming, and multi-turn memory contracts.</p>
        <p className="pi-kv-read"><span>First file</span> <code>BaseMemory</code> <code>memory/base.py:9</code>; <code>conditioning.py</code>, <code>media.py</code>, <code>mosaic.py</code>, <code>retrieval.py</code></p>
      </li>
      <li className="pi-kv-row">
        <div className="pi-kv-head">
          <div className="pi-kv-names">
            <code>acceleration</code>
          </div>
        </div>
        <p>FP8 tensorwise quantization, low-precision gating, acceleration-technology reporting, NVFP4, token pruning.</p>
        <p className="pi-kv-read"><span>First file</span> <code>Float8Linear</code> <code>acceleration/quantization.py:47</code>; <code>AccelerationTechnology</code> <code>technology.py:15</code></p>
      </li>
      <li className="pi-kv-row">
        <div className="pi-kv-head">
          <div className="pi-kv-names">
            <code>configuration</code>
          </div>
        </div>
        <p>Hydra integration, model config, flags, Cosmos-style config.</p>
        <p className="pi-kv-read"><span>First file</span> <code>{"configuration/{hydra,model_config,flags,cosmos_config}.py"}</code></p>
      </li>
      <li className="pi-kv-row">
        <div className="pi-kv-head">
          <div className="pi-kv-names">
            <code>io</code>
          </div>
        </div>
        <p>Media and artifact IO: audio, video, depth-to-colormap, action tracks, cache, config utils.</p>
        <p className="pi-kv-read"><span>First file</span> <code>{"io/{artifacts,audio,action_tracks,cache}.py"}</code>; <code>save_depth_colormap</code> <code>io/artifacts.py:82</code></p>
      </li>
      <li className="pi-kv-row">
        <div className="pi-kv-head">
          <div className="pi-kv-names">
            <code>safety</code>
          </div>
        </div>
        <p>Video IO guardrails and safety filters.</p>
        <p className="pi-kv-read"><span>First file</span> <code>{"safety/{guardrails,video_io}.py"}</code></p>
      </li>
      <li className="pi-kv-row">
        <div className="pi-kv-head">
          <div className="pi-kv-names">
            <code>structures</code>
          </div>
        </div>
        <p>Typed validators, shape slicing, tree utils, predicates.</p>
        <p className="pi-kv-read"><span>First file</span> <code>Validator</code> <code>structures/validator.py:10</code>; <code>ShapeSlice</code> <code>shape_utils.py:63</code></p>
      </li>
      <li className="pi-kv-row">
        <div className="pi-kv-head">
          <div className="pi-kv-names">
            <code>utils</code>
          </div>
        </div>
        <p>Cross-cutting helpers: array/tensor, batch ops, CUDA graphs, feature extraction, functional utils.</p>
        <p className="pi-kv-read"><span>First file</span> <code>{"utils/{array_tensor_utils,batch_ops,cuda_graph,functional_utils}.py"}</code></p>
      </li>
      <li className="pi-kv-row">
        <div className="pi-kv-head">
          <div className="pi-kv-names">
            <code>visualization</code>
          </div>
        </div>
        <p>Action overlays and shared visualization helpers.</p>
        <p className="pi-kv-read"><span>First file</span> <code>visualization/action_overlay.py</code></p>
      </li>
    </ul>
  </section>
</div>

## Dependency rule

`core` may depend on PyTorch, `diffusers` model/operator primitives, and narrow third-party libraries. It must not import `worldfoundry.pipelines`, `worldfoundry.synthesis`, `worldfoundry.operators`, `worldfoundry.representations`, `worldfoundry.base_models` family packages, or `worldfoundry.evaluation`. The edge points *out of* family packages *into* `core`, never the reverse.

This keeps family packages thin: a model directory contains checkpoint-compatible tensor math and contract adapters, not a second copy of an offload manager, a distributed bootstrap, a kernel registry, or a quantization system. When you find yourself adding one of those to a family package, move it to `core` first and import it from there.

<Callout type="info">
  The diffusion package restates this rule for its own boundary in [Native diffusion](/docs/maintainers/architecture/native-diffusion). Generic attention, transformer, loading, and offload primitives live in `core`; diffusion-domain contracts, schedulers, and recipes live in `base_models/diffusion_model`.
</Callout>

## Where it is consumed

- **Diffusion** — `base_models/diffusion_model` loads checkpoints through `core/model_loading`, runs attention through `core/attention`, and offloads through `core/vram`. Its `optimizations/` re-exports core policy, not a parallel implementation.
- **Hosted and native runtimes** — `synthesis/` stages inputs through `core/io` and writes artifacts through the same media helpers.
- **Memory** — `core/memory/base.py` defines the contract; streaming and long-context state in `synthesis/*/memory` and action-policy trace state in `synthesis/action_generation/memory.py` implement it.
- **Distributed execution** — `core/distributed` is the only place model-parallel groups are initialized; no family package boots its own NCCL group.

## What does not belong here

Training entrypoints, datasets, dataloaders, losses, callbacks, augmentation-only modules, and experiment scripts do not belong in `core`, nor in `base_models`. `core` is inference infrastructure; if a module only matters during training, it should not be added here.

For the directory-level map of every package, see [Codebase map](/docs/maintainers/architecture/file-index). For how a model call flows through these primitives, see [Model runtime](/docs/maintainers/architecture/model-runtime) and [Runtime assembly](/docs/maintainers/architecture/runtime-assembly).
