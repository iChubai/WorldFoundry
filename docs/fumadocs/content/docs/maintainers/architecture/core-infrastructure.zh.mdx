---
title: 核心基础设施
description: worldfoundry/core 共享层 —— 加载、attention、分布式、VRAM、kernel、memory，以及“可复用原语只在此处”的规则。
---

## 本层为何存在

`worldfoundry/core` 是每个 model family 都复用、但都不拥有的共享推理基础设施。checkpoint 加载、state-dict remap、attention 后端、分布式 collectives、VRAM 放置与 offload、kernel、配置、媒体 IO、memory 契约、安全 guardrails 都只在此实现一次。family 包（`worldfoundry/base_models/*`、`worldfoundry/synthesis/*`、`worldfoundry/pipelines/*`）组合这些原语，但不得重新实现它们。

当你打算把某个工具复制进某个模型目录时，本层就是你要先读的。规则很明确：如果一个能力可服务多个 family，就先放进 `core`。扩散如何消费本层见 [原生扩散](/zh/docs/maintainers/architecture/native-diffusion)，pipeline/operator 如何消费见 [模型运行时](/zh/docs/maintainers/architecture/model-runtime)。

## 子包职责

<div className="pi-kv-catalog not-prose">
  <section className="pi-kv-group">
    <h3>子包</h3>
    <ul>
      <li className="pi-kv-row">
        <div className="pi-kv-head">
          <div className="pi-kv-names">
            <code>model_loading</code>
          </div>
        </div>
        <p>checkpoint 物化、state-dict 加载、LoRA 合并、推理网络构造。</p>
        <p className="pi-kv-read"><span>首读</span> <code>load_state_dict_from_safetensors</code> <code>model_loading/file.py:110</code>；<code>instantiate_inference_network</code> <code>inference_model.py:23</code>；<code>GeneralLoRALoader</code> <code>lora.py:15</code></p>
      </li>
      <li className="pi-kv-row">
        <div className="pi-kv-head">
          <div className="pi-kv-names">
            <code>checkpoint</code>
          </div>
        </div>
        <p>state-dict 赋值、remap、分布式 checkpoint (DCP)、安全加载。</p>
        <p className="pi-kv-read"><span>首读</span> <code>{"checkpoint/{assignment,remap,dcp,safe_loading,load}.py"}</code></p>
      </li>
      <li className="pi-kv-row">
        <div className="pi-kv-head">
          <div className="pi-kv-names">
            <code>vram</code>
          </div>
        </div>
        <p>磁盘 state map、meta 初始化、staged 放置、层式 CPU offload、显存核算。</p>
        <p className="pi-kv-read"><span>首读</span> <code>{"vram/{disk_map,initialization,layers,layerwise_offload,memory}.py"}</code></p>
      </li>
      <li className="pi-kv-row">
        <div className="pi-kv-head">
          <div className="pi-kv-names">
            <code>attention</code>
          </div>
        </div>
        <p>后端探测、dense SDPA、可变长 packed、sparse 3D、n-D/complex/projective RoPE、序列并行组合。</p>
        <p className="pi-kv-read"><span>首读</span> <code>AttentionKernelCapability</code> <code>attention/backends.py:32</code>；<code>probe_attention_backends</code> <code>backends.py:144</code>；<code>complex_rope.py</code>、<code>causal_ulysses_attention.py</code></p>
      </li>
      <li className="pi-kv-row">
        <div className="pi-kv-head">
          <div className="pi-kv-names">
            <code>distributed</code>
          </div>
        </div>
        <p>NCCL options、masked 正交 rank groups、模型并行初始化、context/block FSDP、device-mesh collectives。</p>
        <p className="pi-kv-read"><span>首读</span> <code>initialize_model_parallel</code> <code>distributed/model_parallel_groups.py:238</code>；<code>RankGenerator</code> <code>:194</code>；<code>block_fsdp.py</code>、<code>context_parallel.py</code></p>
      </li>
      <li className="pi-kv-row">
        <div className="pi-kv-head">
          <div className="pi-kv-names">
            <code>nn</code>
          </div>
        </div>
        <p>可复用 transformer 算子：RMSNorm、共享 MLP、timestep/AdaLN/final-layer modulation、prefix gating、activation checkpointing、classic-diffusion block。</p>
        <p className="pi-kv-read"><span>首读</span> <code>{"nn/{activation_checkpointing,diffusion_schedulers,convolutional_mlp,...}.py"}</code></p>
      </li>
      <li className="pi-kv-row">
        <div className="pi-kv-head">
          <div className="pi-kv-names">
            <code>kernels</code>
          </div>
        </div>
        <p>autotune 缓存、能力探测、扩散 kernel、MoE、量化 GEMM。</p>
        <p className="pi-kv-read"><span>首读</span> <code>{"kernels/{capabilities,autotune_cache,diffusion,moe,quantized_gemm}.py"}</code></p>
      </li>
      <li className="pi-kv-row">
        <div className="pi-kv-head">
          <div className="pi-kv-names">
            <code>memory</code>
          </div>
        </div>
        <p>多模态、流式、多轮 memory 契约。</p>
        <p className="pi-kv-read"><span>首读</span> <code>BaseMemory</code> <code>memory/base.py:9</code>；<code>conditioning.py</code>、<code>media.py</code>、<code>mosaic.py</code>、<code>retrieval.py</code></p>
      </li>
      <li className="pi-kv-row">
        <div className="pi-kv-head">
          <div className="pi-kv-names">
            <code>acceleration</code>
          </div>
        </div>
        <p>FP8 tensorwise 量化、低精度开关、加速技术报告、NVFP4、token pruning。</p>
        <p className="pi-kv-read"><span>首读</span> <code>Float8Linear</code> <code>acceleration/quantization.py:47</code>；<code>AccelerationTechnology</code> <code>technology.py:15</code></p>
      </li>
      <li className="pi-kv-row">
        <div className="pi-kv-head">
          <div className="pi-kv-names">
            <code>configuration</code>
          </div>
        </div>
        <p>Hydra 集成、模型配置、flags、Cosmos 风格配置。</p>
        <p className="pi-kv-read"><span>首读</span> <code>{"configuration/{hydra,model_config,flags,cosmos_config}.py"}</code></p>
      </li>
      <li className="pi-kv-row">
        <div className="pi-kv-head">
          <div className="pi-kv-names">
            <code>io</code>
          </div>
        </div>
        <p>媒体与 artifact IO：audio、video、depth colormap、action track、cache、config utils。</p>
        <p className="pi-kv-read"><span>首读</span> <code>{"io/{artifacts,audio,action_tracks,cache}.py"}</code>；<code>save_depth_colormap</code> <code>io/artifacts.py:82</code></p>
      </li>
      <li className="pi-kv-row">
        <div className="pi-kv-head">
          <div className="pi-kv-names">
            <code>safety</code>
          </div>
        </div>
        <p>video IO guardrail 与安全过滤。</p>
        <p className="pi-kv-read"><span>首读</span> <code>{"safety/{guardrails,video_io}.py"}</code></p>
      </li>
      <li className="pi-kv-row">
        <div className="pi-kv-head">
          <div className="pi-kv-names">
            <code>structures</code>
          </div>
        </div>
        <p>类型化 validator、shape 切片、tree utils、predicates。</p>
        <p className="pi-kv-read"><span>首读</span> <code>Validator</code> <code>structures/validator.py:10</code>；<code>ShapeSlice</code> <code>shape_utils.py:63</code></p>
      </li>
      <li className="pi-kv-row">
        <div className="pi-kv-head">
          <div className="pi-kv-names">
            <code>utils</code>
          </div>
        </div>
        <p>跨包工具：array/tensor、batch ops、CUDA graph、feature extraction、functional utils。</p>
        <p className="pi-kv-read"><span>首读</span> <code>{"utils/{array_tensor_utils,batch_ops,cuda_graph,functional_utils}.py"}</code></p>
      </li>
      <li className="pi-kv-row">
        <div className="pi-kv-head">
          <div className="pi-kv-names">
            <code>visualization</code>
          </div>
        </div>
        <p>动作 overlay 与共享可视化助手。</p>
        <p className="pi-kv-read"><span>首读</span> <code>visualization/action_overlay.py</code></p>
      </li>
    </ul>
  </section>
</div>

## 依赖规则

`core` 可依赖 PyTorch、`diffusers` 的 model/operator 原语与窄范围第三方库。它不得 import `worldfoundry.pipelines`、`worldfoundry.synthesis`、`worldfoundry.operators`、`worldfoundry.representations`、`worldfoundry.base_models` 的 family 包，或 `worldfoundry.evaluation`。依赖边是从 family 包指向 `core`，绝不反向。

这让 family 包保持精简：一个模型目录只含 checkpoint 兼容的张量算子与契约适配器，而不是第二份 offload manager、分布式 bootstrap、kernel registry 或量化系统。当你发现自己要在 family 包里加这些东西时，先把它移到 `core` 再 import。

<Callout type="info">
  扩散包在其自身边界上重申此规则，见 [原生扩散](/zh/docs/maintainers/architecture/native-diffusion)。通用 attention、transformer、加载、offload 原语在 `core`；扩散域契约、scheduler、recipe 在 `base_models/diffusion_model`。
</Callout>

## 被谁消费

- **扩散** —— `base_models/diffusion_model` 经 `core/model_loading` 加载 checkpoint，经 `core/attention` 跑 attention，经 `core/vram` 做 offload。其 `optimizations/` 只是 core policy 的再导出，不是并行实现。
- **Hosted 与 native runtime** —— `synthesis/` 经 `core/io` staging 输入，经同一套媒体助手写 artifact。
- **Memory** —— `core/memory/base.py` 定义契约；`synthesis/*/memory` 的流式/长上下文状态与 `synthesis/action_generation/memory.py` 的动作策略 trace 状态实现它。
- **分布式执行** —— `core/distributed` 是模型并行组初始化的唯一位置；任何 family 包都不得自启 NCCL group。

## 不属于此处的内容

训练入口、数据集、dataloader、loss、callback、仅增强模块、实验脚本都不属于 `core`，也不属于 `base_models`。`core` 是推理基础设施；若某模块只在训练时有用，不要加到这里。

全包目录级地图见 [代码地图](/zh/docs/maintainers/architecture/file-index)。模型调用如何流经这些原语见 [模型运行时](/zh/docs/maintainers/architecture/model-runtime) 与 [Runtime 组装](/zh/docs/maintainers/architecture/runtime-assembly)。
