# Torch wheel constraints for the cu124 CUDA bucket.
#
# These bounds must match worldfoundry.runtime.cuda_tiers.TIER_TORCH_SPECS
# and the releases published at https://download.pytorch.org/whl/cu124.
# This is a drift-check stub, not a full lockfile.

torch>=2.4,<2.7
torchvision>=0.19,<0.22
torchaudio>=2.4,<2.7
