## wan21-fun-1p3b-cam

Real room image with explicitly synthetic camera trajectory, identity rotation and camera-to-world positive z for forward/negative z for backward. Normalized intrinsics fx=fy1,cx=cy0.5. Full50steps; first81frames travel0.5units,121frames0.75units. Same seed/prompt opposite-direction1.3B pair tests camera response. Child raw/pre-serialization/load audits required; no pass inferred from file creation. Regression after root VAE encode/decode offload hook repair; original failure is preserved. Actual parity sentinel required before dispatch. Full generation after VAE offload fix. All final child components have zero missing/unexpected keys; actual decoder before-clamp tensors finite; complete video decode and dimensions/fps verified. TeaCache uses default threshold0.1 after first5steps: 50 scheduler steps is not 50 uncached DiT evaluations. Actual contact sheet reviewed: room/car identity persists, but a new bright light blooms in the middle and strongly changes illumination. Forward camera translation is weak/uncertain; same-seed backward comparison still pending.

## wan22-fun-5b-cam

Real room image with explicitly synthetic camera trajectory, identity rotation and camera-to-world positive z for forward/negative z for backward. Normalized intrinsics fx=fy1,cx=cy0.5. Full50steps; first81frames travel0.5units,121frames0.75units. Same seed/prompt opposite-direction1.3B pair tests camera response. Child raw/pre-serialization/load audits required; no pass inferred from file creation. Regression after root VAE encode/decode offload hook repair; original failure is preserved. Actual parity sentinel required before dispatch. Full generation after VAE offload fix. All final child components have zero missing/unexpected keys; actual decoder before-clamp tensors finite; complete video decode and dimensions/fps verified. TeaCache uses default threshold0.1 after first5steps: 50 scheduler steps is not 50 uncached DiT evaluations. Actual contact sheet reviewed: room/car identity and scene geometry remain coherent, with increasing foreground/car size and forward camera motion consistent with the supplied synthetic trajectory. Single image/trajectory/seed only; no quantitative pose accuracy claim.

## Camera direction pair

Same seed42/prompt/image and50scheduler steps with opposite synthetic camera z translation. Both81frames832x48016fps; TeaCache0.1 enabled after5steps. Final component loads zero missing/unexpected, raw decoder finite before clamp, full video decode. Actual forward/backward contact sheets visually compared: backward car/foreground become smaller and forward foreground larger, supporting qualitative trajectory response. Both hallucinate strong central light with severe illumination drift; quality_concern retained. No quantitative pose-accuracy claim.

## wan21-fun-14b-cam

After Wan VAE root-offload fix: all81frames832x48016fps,50scheduler steps; TeaCache default0.1 after5steps, not50uncached DiT calls. Actual final4components have zero missing/unexpected keys; raw decoder finite before clamp; complete decode matches dimensions/fps. Actual contact sheet reviewed: room/car identity retained, coherent forward travel with foreground expansion and steady scene lighting, consistent with explicitly synthetic camera-to-world positive-z trajectory. Single input/seed/trajectory only; no quantitative pose accuracy. Shared GPU1 external training overlapped early loading but this case completed successfully.
