# CogVideoX GPU 验证

2B T2V、5B T2V、5B I2V 均以 720×480、49 帧、8 FPS、50 步完成真实 GPU 推理。完整解码无错误，尺寸/帧数/帧率一致。两种 T2V 的森林、蕨类、阳光及前向运动符合提示；2B 画面对比度明显高于 5B。I2V 保持输入的木地板、桌椅、玩具车与窗光，缓慢前移，未见整片噪声、黑屏或突变。接触图与逐帧统计在各输出目录。

仅验证记录 checkpoint/config，CogVideoX1.5 与其他变体仍需单独验证。CLIP 同批因新版环境缺少 OpenCV 失败，后续环境修复单列，不将其计为通过。
