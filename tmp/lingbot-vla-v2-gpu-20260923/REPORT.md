# LingBot-VLA v2 GPU review, 2026-09-23

Status: **verified_inference_only**. The public `LingBotVLAV2Pipeline` loaded `robbyant/lingbot-vla-v2-6b` on H100 GPU 3 with the local Qwen3-VL base. Strict safetensors loading reported six files, 1708 loaded keys, zero missing keys, zero shape mismatches, and zero unexpected keys. The action artifact contains a finite 50×14 float32 action chunk, range approximately -1.664 to 2.744. See `lingbot-vla-v2-synthetic-state-pathfix/{status.json,result.json,action_trace.json}`.

The input was a real toy photograph reused for the required top, left wrist, and right wrist camera slots, plus a synthetic all-zero 14-dimensional robot state. This checks loading, preprocessing, action generation, shape, and finiteness. It cannot establish robot task semantics, camera calibration, or safe physical actions. The default base-model path in the runtime YAML was an unexpanded `${WORLDFOUNDRY_HFD_ROOT}` token; the in-tree runtime now resolves it through the local Hugging Face checkpoint resolver. The default-path retest under `lingbot-vla-v2-synthetic-state-defaultfix/` also succeeded, producing a finite 50×14 action chunk in 121 seconds without an explicit base-model override.
