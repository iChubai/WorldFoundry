# 本地 GPU 续跑结果（2026-09-21）

原会话 `01a079fc-1b61-7dc2-9d0d-aa6c757461b5` 的 CLI 恢复因账户额度限制退出；当前会话直接接手完成以下新增验证。

| 模型 | 实际输出 | 验证 |
|---|---|---|
| UniDepth V2 | 480×832 深度图、5,000 点 PLY | 深度全为正且有限；权重零缺失/多余键；深度预览通过 |
| DVLT | 三帧重建，211,329 点 GLB | GLB 独立读取通过、全部顶点有限；权重零缺失/多余键 |
| Splatt3R | 294,912 个高斯点 PLY | 全字段有限、无缺失推理参数；修复颜色后点云预览通过 |

## 修复与检查

- 为 DVLT、Splatt3R、几何先验管线正确传递 `model_path`。
- 几何先验预检查尊重显式权重路径；UniDepth 支持仓库名前缀目录。
- 旧版 UniDepth 配置给出明确的不兼容提示与所需版本。下载匹配版本，SHA-256 已与 Hub 元数据核对，原权重未覆盖。
- Splatt3R 将编码器归一化图像恢复到 RGB 范围后再计算颜色；几何保持逐项相同。负 RGB 通道比例从 93.36% 降至 15.86%（模型颜色残差仍可超出显示范围）。
- 补齐忽略目录中的推理依赖，保持现有分发排除规则；修正本地 DVLT 的几何函数导入。
- 10 项内部检查、3 项公开加载入口检查、颜色回归与 5 个修改文件语法检查通过。

## 产物与交付

- [完整 JSON 报告](validation-report.json)
- [UniDepth 深度图](unidepth-v2/scene/depth_preview.png)
- [Splatt3R 点云预览](splatt3r/preview.png)
- [DVLT 点云预览](dvlt/preview.png) 与 [结果信息](dvlt/result.json)
- [本轮 5 文件增量补丁](task-only.patch)
- 各模型目录内有 `status.json`、`result.json` 和输出；根目录的模型日志保留失败定位与成功重跑过程。

`gg` 已恢复到 GPU 0、1、2、3，PID `394878`。本轮未提交或推送：当前工作区存在其他会话的修改和未跟踪 UniDepth 集成，已保留独立补丁，避免混入提交。测试及结果保存在忽略的 `tmp/` 中。

结论仅覆盖上述 3 个模型的记录配置；未声称完成整个模型目录的 GPU 验证，也未进行量化重建精度或上游渲染一致性验证。
