"""DreamX-World: causal Wan transformer used by DreamX recipes.

Submodules
----------
- :mod:`.transformer` — packed / official DreamX DiT blocks.
- :mod:`.causal` — causal attention and chunk cache.

Adds causal hooks.  No VACE or linear-attention processors.
"""
