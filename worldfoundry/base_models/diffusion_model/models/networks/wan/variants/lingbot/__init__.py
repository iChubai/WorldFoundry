"""LingBot / LingBot-World: causal-fast Wan 2.2 DiT variants.

Submodules
----------
- :mod:`.model` — official-layout Wan 2.2 stem.
- :mod:`.fast` — Self-Forcing-style causal-fast block (chunk KV cache).
- :mod:`.attention` — LingBot attention kernels.

Adds causal / fast-attention hooks.  See also
:mod:`..lingbot_world_v2` for the World-V2 packed graph.
"""
