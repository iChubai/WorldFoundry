schema_version: 2
binding_id: cut3r
model_id: cut3r
runner: worldfoundry.pipeline
pipeline:
  target: worldfoundry.pipelines.cut3r.pipeline_cut3r:CUT3RPipeline
  loading:
    method: from_pretrained
    signature: unified
  invocation:
    mode: native_or_standard_invocation
