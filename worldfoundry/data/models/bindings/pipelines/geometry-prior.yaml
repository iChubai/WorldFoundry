schema_version: 2
binding_id: geometry-prior
model_id: geometry-prior
runner: worldfoundry.pipeline
pipeline:
  target: worldfoundry.pipelines.geometry_priors.pipeline_geometry_priors:GeometryPriorPipeline
  loading:
    method: from_pretrained
    signature: unified
  invocation:
    mode: native_or_standard_invocation
