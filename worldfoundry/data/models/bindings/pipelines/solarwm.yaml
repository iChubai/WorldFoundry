schema_version: 2
binding_id: solarwm
model_id: solarwm
runner: worldfoundry.pipeline
pipeline:
  target: worldfoundry.pipelines.world_model.pipeline_runtime_manifest:SolarWMPipeline
  loading:
    method: from_pretrained
    signature: unified
  invocation:
    mode: native_or_standard_invocation
