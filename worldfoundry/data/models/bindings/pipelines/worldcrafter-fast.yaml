schema_version: 2
binding_id: worldcrafter-fast
model_id: worldcrafter-fast
runner: worldfoundry.pipeline
pipeline:
  target: worldfoundry.pipelines.worldcrafter.pipeline_worldcrafter:WorldCrafterPipeline
  loading: {method: from_pretrained, signature: unified}
  invocation: {mode: native_or_standard_invocation}
