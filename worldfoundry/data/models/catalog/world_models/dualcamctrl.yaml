runner_target: worldfoundry.evaluation.models.runners.pipeline:WorldFoundryPipelineRunner
availability: open_source
checkpoints:
- FayeHongfeiZhang/DualCamCtrl
- alibaba-pai/Wan2.1-Fun-V1.1-1.3B-Control-Camera
- Wan-AI/Wan2.1-T2V-1.3B
- Wan-AI/Wan2.1-I2V-14B-480P
family: Camera-Control Video
id: dualcamctrl
integration:
  notes:
  - Runtime code is integrated in-tree; checkpoints use the default Hugging Face/local cache loading path.
  - The WorldFoundry runner follows the official RGB/depth/camera demo contract.
  status: integrated
license: unknown
modalities:
- image-to-video
- depth-conditioned-video
- camera-control
- video-diffusion
name: DualCamCtrl
official_sources:
  paper: https://arxiv.org/abs/2511.23127
  project_page: https://soyouthinkyoucantell.github.io/dualcamctrl-page/
  github:
    confirmed_by: local checkout from official repository
    confirmed_ref: f4f064f39c84b0e9597e99979ec56361570d8bef
    url: https://github.com/EnVision-Research/DualCamCtrl
  huggingface:
  - repo_id: FayeHongfeiZhang/DualCamCtrl
    type: model
pipeline_binding: dualcamctrl
release_type: code_and_weights
runtime_profile: runtime-profile:dualcamctrl
demo_parity:
  demo_command:
  - python
  - -m
  - worldfoundry.studio.workspace_job
  - infer
  - --model-id
  - dualcamctrl
  - --call-json
  - '{"demo_name":"seaside","num_frames":61,"height":320,"width":480,"num_inference_steps":50,"fps":10,"seed":42,"cfg_scale":5.0,"original_height":360,"original_width":640,"tiled":true,"return_control_latents":true}'
  expected_artifacts:
  - path: seaside.mp4
    min_size_bytes: 100000
    video_probe:
      min_frames: 61
      min_width: 480
      min_height: 320
      min_fps: 9
      max_fps: 11
  notes:
  - Follows the official seaside RGB/depth/camera demo contract with in-tree fixtures.
  status: verified
runner_parity:
  notes:
  - Per-run artifact checks are tracked outside the model catalog.
  status: verified
catalog_category: world_models
catalog_scope: world-model/camera-control models
schema_version: 2
docs:
  publisher: The Hong Kong University of Science and Technology (Guangzhou) (EnVision Lab)
  publisher_kind: university
  publisher_zh: 香港科技大学（广州）EnVision 实验室
  overview: 'DualCamCtrl controls video generation with a camera trajectory plus a depth image: the RGB and depth conditions together pin down scene geometry so the generated camera move stays physically grounded. In this catalog the runner is verified and follows the official RGB/depth/camera demo contract.'
  overview_zh: DualCamCtrl 用「相机轨迹 + 深度图」控制视频生成：RGB 与深度条件共同确定场景几何，使生成的相机运动保持物理可信。本 catalog 中 runner 已验证，遵循官方 RGB/深度/相机 demo 契约。
  architecture: 'The default demo contract follows the official recipe: an image, a depth image, a camera .torch trajectory file, 61 frames at 320x480, 50 steps. The in-tree inference runtime lives under synthesis/visual_generation/dualcamctrl; internals beyond the recorded contract are not stored here.'
  architecture_zh: 默认 demo 契约遵循官方配方：一张图、一张深度图、一个相机 .torch 轨迹文件，61 帧 320x480、50 步。树内推理运行时位于 synthesis/visual_generation/dualcamctrl；除记录契约外的内部细节此处未存。
  usage_notes: Open-source users should rely on the default Hugging Face cache/download path; pre-staged local assets can be passed through checkpoint_path, base_model_path, or WORLDFOUNDRY_MODEL_DIR.
  usage_notes_zh: 开源用户默认使用 Hugging Face 缓存/下载路径；已就位的本地资产可通过 checkpoint_path、 base_model_path 或 WORLDFOUNDRY_MODEL_DIR 传入。
  limitations:
  - Needs a depth image alongside the RGB input — pair with a depth prior when you only have RGB.
  - 'Fixed demo contract (61 frames, 320x480): other shapes are untested here.'
  limitations_zh:
  - 除 RGB 输入外还需要深度图——只有 RGB 时请配合深度先验使用。
  - demo 契约固定（61 帧、320x480）：其它输出形状此处未测试。
  recommended_benchmarks:
  - id: camerabench
    reason: Camera-trajectory adherence of controlled generation.
    reason_zh: 受控生成的相机轨迹贴合度。
