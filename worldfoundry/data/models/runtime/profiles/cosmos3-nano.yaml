schema_version: 2
model_id: cosmos3-nano
display_name: Cosmos 3 Nano
task_family: world_model
groups:
- world
- video
source_repos:
- url: https://github.com/NVIDIA/cosmos
checkpoints:
- repo_id: nvidia/Cosmos3-Nano
  revision: 411f42a8fdfb8c5b2583cb8786e0938f49796eaa
  role: cosmos3_checkpoint
input_schema:
  prompt: true
  image: true
  video: true
  actions:
  - policy
  - forward_dynamics
  - inverse_dynamics
artifact_kind: generated_world
artifact_filename: cosmos3-nano.mp4
execution:
  backend_stage: native_diffusion_inference
  checkpoint_revision_policy: exact_sha_for_verified_runs
  load_sound_tokenizer: true
  runtime_status: native_structure_and_scheduler_parity_validated_gpu_parity_pending
  integration_status: integrated
  pipeline_binding: cosmos3-nano
  environment: cosmos3
integration:
  status: integrated
notes:
- The in-tree generator supports optional synchronized sound and structured policy,
  forward-dynamics, and inverse-dynamics action inference. Reasoner, training, and serving
  workflows are not part of this infer-only profile.
- The native transformer, shared Wan VAE38, and AVAE decoder match all published Nano
  checkpoint keys and shapes. Native flow-UniPC is numerically identical to the official scheduler.
- End-to-end GPU output parity for the new framework-owned runner remains pending; historical
  Diffusers smoke evidence is not attributed to the native path.
- Verified runs must use revision 411f42a8fdfb8c5b2583cb8786e0938f49796eaa explicitly; directory
  presence without matching .hfd metadata or an exact HF snapshot is not current-revision evidence.
- Safety screening is intentionally separate from the infer-only model recipe.
