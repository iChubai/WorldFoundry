# Makes `frustum` an importable package so submodules such as
# `frustum.scene_quality` can be run with `python -m frustum.scene_quality`.
# Intentionally empty: existing standalone scripts in this directory keep working.
